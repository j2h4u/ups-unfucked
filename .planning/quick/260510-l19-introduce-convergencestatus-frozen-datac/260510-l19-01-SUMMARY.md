---
phase: 260510-l19
plan: "01"
subsystem: model
tags: [refactor, type-safety, dataclass]
dependency_graph:
  requires: []
  provides: [ConvergenceStatus-dataclass]
  affects: [src/model.py, src/discharge_handler.py, src/virtual_ups_exporter.py]
tech_stack:
  added: []
  patterns: [frozen-dataclass, typed-return]
key_files:
  created: []
  modified:
    - src/model.py
    - src/discharge_handler.py
    - src/virtual_ups_exporter.py
    - tests/test_model.py
    - tests/test_monitor.py
    - tests/test_monitor_integration.py
decisions:
  - "frozen=True enforces immutability without any backward-compat shim"
  - "Dict[str,Any] import retained in model.py — still used by _default_vrla_lut"
metrics:
  duration_minutes: 7
  completed_date: "2026-05-10"
  tasks_completed: 3
  files_changed: 6
---

# Phase 260510-l19 Plan 01: Introduce ConvergenceStatus Frozen Dataclass Summary

**One-liner:** Replaced the 8-key `Dict[str, Any]` returned by `BatteryModel.get_convergence_status()` with a `@dataclass(frozen=True) ConvergenceStatus`, eliminating `.get()` lookups and making the contract explicit across all 3 production callers and 11 test mock/assertion sites.

## Tasks Completed

| Task | Name | Commit | Files |
|------|------|--------|-------|
| 1 | Define ConvergenceStatus dataclass + update model.py | 456676d | src/model.py, tests/test_model.py |
| 2 | Migrate production callers to attribute access | 30ad893 | src/discharge_handler.py, src/virtual_ups_exporter.py |
| 3 | Update test mocks to ConvergenceStatus instances | aea0b0d | tests/test_monitor.py, tests/test_monitor_integration.py |

## Files Touched

| File | Change |
|------|--------|
| src/model.py | +12 lines: `ConvergenceStatus` dataclass; updated return type + both return branches |
| src/discharge_handler.py | +3 lines import, 4 dict accesses → attribute access, param type annotation updated |
| src/virtual_ups_exporter.py | 5 `.get()` calls → direct attribute access |
| tests/test_model.py | 3 tests updated to attribute access; 1 immutability test added |
| tests/test_monitor.py | ConvergenceStatus import; 7 dict mocks replaced |
| tests/test_monitor_integration.py | ConvergenceStatus import; 2 dict mocks replaced (1 discovered beyond plan count) |

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Extra dict mock discovered in test_monitor_integration.py**
- **Found during:** Task 3 full-suite run
- **Issue:** `test_journald_event_filtering` had a *second* `mock_convergence.return_value = {...}` block (line 449) that the plan's line-reference count of "1 mock" did not account for. The test triggered `AttributeError: 'dict' object has no attribute 'sample_count'` on the second `_handle_discharge_complete()` call within the same test.
- **Fix:** Replaced the second dict block with `ConvergenceStatus(...)` in the same pattern as the first.
- **Files modified:** tests/test_monitor_integration.py
- **Commit:** aea0b0d (included in Task 3 commit)

## Verification Results

All grep checks pass as of final run:

```
rg 'convergence(_status)?\.(get\(|\[)' src/    → 0 hits (exit 1)
rg "get_convergence_status.*Dict\[str" src/     → 0 hits (exit 1)
rg 'class ConvergenceStatus' src/model.py       → 1 hit (line 28)
rg 'get_convergence_status\.return_value\s*=\s*\{' tests/  → 0 hits (exit 1)
rg 'capacity_ah_ref' tests/test_monitor.py tests/test_monitor_integration.py → remaining refs are SoH history entries, NOT convergence status mocks
```

Frozen check: `ConvergenceStatus(0, 0.0, None, 7.2, False, None, 0.0, 0.0)` raises `FrozenInstanceError` on `s.sample_count = 1`.

## Test Results

- **Before:** 569 passing (suite was green except for Task 3 dict-type failures)
- **After:** 569 passing, 0 failures, 0 errors

## Daemon Restart Note

Per `feedback_always_restart_daemon.md`: after deployment, `sudo systemctl restart ups-battery-monitor` is required. This is a pure refactor with no behavior change — the daemon will continue normally after restart.

## Known Stubs

None.

## Threat Flags

None — this plan touches no network endpoints, auth paths, file access patterns, or schema changes.

## Self-Check: PASSED

- src/model.py: FOUND (ConvergenceStatus class at line 28)
- src/discharge_handler.py: FOUND (ConvergenceStatus import, attribute access)
- src/virtual_ups_exporter.py: FOUND (attribute access, no .get() calls)
- Commit 456676d: FOUND
- Commit 30ad893: FOUND
- Commit aea0b0d: FOUND
- 569 tests passing: CONFIRMED
