---
phase: 01-foundation-nut-integration-core-infrastructure
plan: 04
type: auto
wave: 1
depends_on: [01]
files_modified:
  - src/model.py
autonomous: true
requirements: [MODEL-01, MODEL-02, MODEL-03, MODEL-04]
user_setup: []

must_haves:
  truths:
    - "model.json persists to disk on demand (not constantly)"
    - "LUT initialized from standard VRLA curve on first run"
    - "LUT tracks source of each point: standard/measured/anchor"
    - "Atomic write pattern prevents corruption on power loss"
    - "Malformed JSON triggers fallback to default curve, not crash"
    - "SoH history is initialized with {date, soh} entry on first run"
  artifacts:
    - path: "src/model.py"
      provides: "Battery model persistence and VRLA LUT initialization"
      exports: ["BatteryModel", "atomic_write_json"]
  key_links:
    - from: "BatteryModel.save()"
      to: "model.json on disk"
      via: "Atomic write: tempfile + fsync + os.replace"
      pattern: "tempfile.NamedTemporaryFile.*os.fsync.*os.replace"
    - from: "Standard VRLA curve"
      to: "LUT initialization"
      via: "13.4V→100%, 10.5V→0% anchors"
      pattern: "lut.*13\\.4.*1\\.00"
    - from: "SoH history"
      to: "model.json"
      via: "List of {date, soh} tracking battery degradation"
      pattern: "soh_history.*date.*soh"

---

<objective>
Implement battery model persistence with atomic JSON writes and standard VRLA curve initialization. Establish model.json as the single source of truth for battery state, LUT (lookup table), and SoH history. Ensure crash-safe writes using tempfile + fsync + atomic rename pattern.

Purpose: Fulfill MODEL-01, MODEL-02, MODEL-03, MODEL-04 requirements. Model storage is critical for accurate state estimation across daemon restarts and discharge events.

Output: BatteryModel class with load/save, atomic_write_json() helper, and VRLA LUT initialization. All MODEL tests passing.
</objective>

<execution_context>
@/home/j2h4u/.claude/get-shit-done/workflows/execute-plan.md
@/home/j2h4u/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/CONTEXT.md
@.planning/phases/01-foundation-nut-integration-core-infrastructure/01-RESEARCH.md

From RESEARCH (Pattern 3: Atomic JSON Model Persistence):
- Tempfile pattern: create temp file in same directory as target
- Fsync forces kernel to disk
- os.replace() provides atomic rename
- Never write model.json on every sample (SSD wear)

From CONTEXT.md (model.json structure):
- LUT: list of {v, soc, source} entries
- Sources: "standard" (from VRLA datasheet), "measured" (from real discharge), "anchor" (10.5V=0%)
- SoH history: list of {date, soh} points
- full_capacity_ah_ref: 7.2 (estimated from UT850EG 425W nominal)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement atomic_write_json() helper and BatteryModel class structure</name>
  <files>src/model.py</files>
  <action>
    Create src/model.py with atomic write function and BatteryModel skeleton:

    ```python
    import json
    import os
    import tempfile
    import logging
    from pathlib import Path
    from typing import Dict, Any

    logger = logging.getLogger(__name__)

    def atomic_write_json(filepath, data):
        """
        Safely write JSON to filepath with atomic guarantees.

        Uses tempfile + fsync + os.replace pattern to prevent corruption
        on power loss or crash during write.

        Args:
            filepath: Target file path (str or Path)
            data: Python dict to serialize as JSON

        Raises:
            IOError: If write or fsync fails
        """
        filepath = Path(filepath)
        filepath.parent.mkdir(parents=True, exist_ok=True)

        # Write to temporary file in same directory (ensures same filesystem)
        with tempfile.NamedTemporaryFile(
            mode='w',
            dir=str(filepath.parent),
            delete=False,
            suffix='.tmp'
        ) as tmp:
            json.dump(data, tmp, indent=2)
            tmp_path = Path(tmp.name)

        try:
            # Flush to disk (force kernel to write buffers)
            fd = os.open(str(tmp_path), os.O_RDONLY)
            try:
                os.fsync(fd)
            finally:
                os.close(fd)

            # Atomic rename (unlink + link on POSIX)
            tmp_path.replace(filepath)
            logger.info(f"Atomically wrote {filepath}")

        except Exception as e:
            # Clean up temp file on error
            tmp_path.unlink(missing_ok=True)
            logger.error(f"Atomic write failed: {e}")
            raise

    class BatteryModel:
        """
        Battery model persistence and VRLA LUT management.

        Stores:
        - LUT: voltage → SoC lookup table with source tracking
        - SoH history: list of (date, SoH) points for degradation tracking
        - Metadata: capacity, current SoH estimate
        """

        def __init__(self, model_path=None):
            """
            Initialize battery model from file or create default.

            Args:
                model_path: Path to model.json (str or Path)
                           If None, defaults to ~/.config/ups-battery-monitor/model.json
            """
            if model_path is None:
                model_path = Path.home() / '.config' / 'ups-battery-monitor' / 'model.json'
            else:
                model_path = Path(model_path)

            self.model_path = model_path
            self.data = {}
            self.load()

        def load(self):
            """
            Load model.json from disk or initialize with standard VRLA curve.

            If file exists: parse JSON
            If missing: create default VRLA curve
            If malformed: log error, initialize with default curve (B3 fix: don't re-raise)
            """
            if self.model_path.exists():
                try:
                    with open(self.model_path, 'r') as f:
                        self.data = json.load(f)
                    logger.info(f"Loaded model from {self.model_path}")
                except json.JSONDecodeError as e:
                    logger.error(f"Malformed model.json: {e}; initializing with default VRLA curve")
                    self.data = self._default_vrla_lut()
            else:
                logger.info("Model file not found; initializing with standard VRLA curve")
                self.data = self._default_vrla_lut()

        def _default_vrla_lut(self) -> Dict[str, Any]:
            """
            Standard VRLA 12V discharge curve (7.2Ah reference capacity).

            Returns dict with LUT, SoH, and metadata.
            These are initial values; measured points replace standard entries
            as real discharge data accumulates.

            Reference: Typical sealed lead-acid (AGM/GEL) 12V battery
            - 13.4V: full charge (float voltage)
            - 12.4V: ~64% remaining (datasheet knee point)
            - 11.0V: very low (~6%)
            - 10.5V: cutoff anchor (0%, physical limit)
            """
            return {
                'full_capacity_ah_ref': 7.2,  # Estimated from UT850EG 425W
                'soh': 1.0,  # State of Health (100% = new battery)
                'lut': [
                    {'v': 13.4, 'soc': 1.00, 'source': 'standard'},
                    {'v': 12.8, 'soc': 0.85, 'source': 'standard'},
                    {'v': 12.4, 'soc': 0.64, 'source': 'standard'},
                    {'v': 12.1, 'soc': 0.40, 'source': 'standard'},
                    {'v': 11.6, 'soc': 0.18, 'source': 'standard'},
                    {'v': 11.0, 'soc': 0.06, 'source': 'standard'},
                    {'v': 10.5, 'soc': 0.00, 'source': 'anchor'},
                ],
                'soh_history': [
                    {'date': '2026-03-13', 'soh': 1.0}
                ]
            }

        def save(self):
            """
            Atomically write model to disk.

            Use only at discharge event completion, not on every sample.
            """
            atomic_write_json(self.model_path, self.data)

        def get_lut(self):
            """Return the lookup table."""
            return self.data.get('lut', [])

        def get_soh(self):
            """Return current SoH estimate (0.0 to 1.0)."""
            return self.data.get('soh', 1.0)

        def get_capacity_ah(self):
            """Return reference full capacity (Ah)."""
            return self.data.get('full_capacity_ah_ref', 7.2)

        def add_soh_history_entry(self, date, soh):
            """Add a SoH history entry for degradation tracking."""
            if 'soh_history' not in self.data:
                self.data['soh_history'] = []
            self.data['soh_history'].append({'date': date, 'soh': soh})
            self.data['soh'] = soh  # Update current SoH

        def get_soh_history(self):
            """Return list of {date, soh} entries."""
            return self.data.get('soh_history', [])

        def get_anchor_voltage(self):
            """Return anchor point voltage (physical cutoff, should always be 10.5V)."""
            lut = self.get_lut()
            # Find entry with soc==0.0 and source=='anchor'
            for entry in lut:
                if entry['soc'] == 0.0 and entry['source'] == 'anchor':
                    return entry['v']
            return 10.5  # Default if not found

        def has_measured_data(self):
            """True if LUT contains any 'measured' source entries."""
            lut = self.get_lut()
            return any(entry['source'] == 'measured' for entry in lut)
    ```

    Key points:
    - atomic_write_json() handles tempfile, fsync, and atomic rename
    - BatteryModel loads from file or initializes with standard curve
    - B3 fix: On JSONDecodeError, log error loudly but DO NOT re-raise; initialize with default instead
    - LUT structure: list of {v, soc, source} dicts
    - source field tracks: standard (datasheet), measured (real data), anchor (10.5V=0%)
    - SoH history initialized with first entry (2026-03-13, SoH=1.0)
    - Helper methods: add_soh_history_entry, get_soh_history, get_anchor_voltage, has_measured_data
    - Logging at key points (load, save, error)
  </action>
  <verify>
    <automated>
      pytest tests/test_model.py::test_model_load_save -v && pytest tests/test_model.py::test_atomic_write -v && echo "PASS: Model save/load tests pass"
    </automated>
  </verify>
  <done>
    - atomic_write_json() function implemented (tempfile + fsync + os.replace)
    - BatteryModel class created with __init__, load(), save()
    - Standard VRLA curve initialization in _default_vrla_lut()
    - B3 fix: load() catches JSONDecodeError and initializes default, does NOT re-raise
    - LUT properly structured with v, soc, source fields
    - SoH history initialized with first entry and helper methods
    - test_model_load_save passes (loads valid JSON, handles malformed gracefully)
    - test_atomic_write passes (file written, no .tmp files left)
    - All MODEL tests passing
  </done>
</task>

</tasks>

<verification>
After Wave 1 plan 04 completes:
- [ ] `pytest tests/test_model.py -v` shows all 5 tests passing
- [ ] model.json loads from file; parses malformed JSON as error (logs, initializes default)
- [ ] Standard VRLA curve initializes with correct voltage points (13.4, 12.4, 11.0, 10.5)
- [ ] Atomic write creates temp file, syncs, and atomically renames
- [ ] No .tmp files left after successful write
- [ ] LUT structure verified: all entries have v, soc, source fields
- [ ] SoH history initialized as list of {date, soh} entries
- [ ] B3 fix verified: corrupted model.json does not crash daemon, initializes with defaults instead
</verification>

<success_criteria>
- BatteryModel class fully implements load/save with atomic guarantees
- Standard VRLA LUT initialized with correct voltage→SoC mapping
- LUT tracks source of each point (standard/measured/anchor)
- Anchor point (10.5V, 0%) is immutable
- SoH history tracking implemented for battery degradation monitoring (MODEL-03)
- All MODEL-01, MODEL-02, MODEL-03, MODEL-04 tests passing
- Malformed JSON handled gracefully (log error, use defaults, no re-raise)
- Ready for Wave 2 (daemon orchestration)
</success_criteria>

<output>
After execution, create `.planning/phases/01-foundation-nut-integration-core-infrastructure/01-04-SUMMARY.md` with:
- BatteryModel class structure (methods, properties)
- VRLA LUT initialization (voltage points, SoC mapping)
- Atomic write verification (tempfile + fsync + os.replace confirmed)
- SoH history structure (date, soh) and helper methods
- Test results (pytest tests/test_model.py -v)
- Default capacity estimate (7.2 Ah for UT850EG)
- B3 fix: JSONDecodeError handling verified (does not re-raise)
- Next wave dependencies (Wave 2 can begin consuming BatteryModel)
</output>
