---
phase: 05-operational-setup-systemd-integration
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - scripts/install.sh
  - tests/test_logging.py
autonomous: true
requirements: [OPS-01, OPS-02, OPS-03, OPS-04]
must_haves:
  truths:
    - "Installation script validates all prerequisites before installation"
    - "Installation script merges NUT config idempotently (no duplicates on re-run)"
    - "Installation script enables systemd service for auto-start on boot"
    - "Installation script starts daemon and verifies virtual UPS readable by NUT"
    - "Daemon logs to journald with SyslogIdentifier when /dev/log available"
    - "Daemon logs to stderr when /dev/log unavailable (test compatibility)"
    - "Structured fields from Phase 4 alerter are searchable via journalctl"
  artifacts:
    - path: scripts/install.sh
      provides: "Production installation automation with validation and idempotence"
      min_lines: 100
    - path: tests/test_logging.py
      provides: "Verify JournalHandler fallback behavior for test environments"
      min_lines: 40
    - path: systemd/ups-battery-monitor.service
      provides: "Service unit already exists; verify in Wave 1 (OPS-01, OPS-03)"
      contains: "Type=simple Restart=on-failure User=j2h4u"
  key_links:
    - from: scripts/install.sh
      to: /etc/systemd/system/ups-battery-monitor.service
      via: "cp command to install service file"
      pattern: "cp.*systemd/.*service"
    - from: scripts/install.sh
      to: /etc/nut/ups.conf
      via: "idempotent config merge with guard clause"
      pattern: "grep -q.*cyberpower-virtual"
    - from: src/monitor.py
      to: journald
      via: "JournalHandler with fallback to stderr"
      pattern: "systemd.journal.JournalHandler"

---

<objective>
Create production-ready installation script and verify logging infrastructure for Phase 5 Wave 0.

Purpose: Automate service deployment with zero manual steps; ensure logging works in both production (journald) and test (stderr) environments.

Output:
- `scripts/install.sh` — Installation automation with prerequisite validation, idempotent NUT config merge, service enablement, and post-install verification
- `tests/test_logging.py` — Unit tests for JournalHandler fallback behavior
</objective>

<execution_context>
@/home/j2h4u/.claude/get-shit-done/workflows/execute-plan.md
@/home/j2h4u/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-operational-setup-systemd-integration/05-RESEARCH.md

# Existing systemd service (to be verified in Wave 1)
@systemd/ups-battery-monitor.service

# Existing NUT config snippet
@config/dummy-ups.conf

# Existing monitor.py logging setup (Phase 1-4)
@src/monitor.py (lines 1-52)

# Existing alerter with structured logging (Phase 4)
@src/alerter.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create install.sh with prerequisite validation and NUT config merge</name>
  <files>scripts/install.sh</files>
  <action>
Create `/home/j2h4u/repos/j2h4u/ups-battery-monitor/scripts/install.sh` with the following structure:

1. **Shebang and error handling:** `#!/bin/bash` with `set -euo pipefail` for fail-fast behavior

2. **Root check:** Verify script runs as root; exit with helpful message if not

3. **Prerequisite validation:**
   - Check Python 3 available (`command -v python3`)
   - Check systemd available (`command -v systemctl`)
   - Check NUT daemon running (`[[ -d /run/nut ]]`)
   - Check systemd-python installed (optional: try `import systemd.journal` via python3 -c)
   - Exit cleanly with descriptive errors if any check fails

4. **Service file installation:**
   - Copy `systemd/ups-battery-monitor.service` to `/etc/systemd/system/ups-battery-monitor.service`
   - Set permissions: `chmod 644`
   - Reload systemd daemon: `systemctl daemon-reload`

5. **NUT dummy-ups config merge (IDEMPOTENT):**
   - Guard clause: `if ! grep -q "cyberpower-virtual" /etc/nut/ups.conf; then`
   - If NOT present: append `config/dummy-ups.conf` to `/etc/nut/ups.conf`
   - If already present: log "Dummy-ups already configured (skipped)"
   - Do NOT create duplicates on re-run

6. **NUT service restart:**
   - Restart nut-server: `systemctl restart nut-server`
   - If nut-monitor exists (check with `systemctl is-active --quiet`): restart it too
   - Add 2-second sleep for services to settle

7. **Service enablement and startup:**
   - Enable service: `systemctl enable ups-battery-monitor`
   - Start service: `systemctl start ups-battery-monitor`

8. **Post-install verification (fail fast if any step fails):**
   - Wait up to 10 seconds for `/dev/shm/ups-virtual.dev` to be created (1-second polling loop with counter)
   - If file appears: log "✓ Virtual UPS device created"
   - If timeout: log error and exit 1
   - Test virtual UPS readability: `upsc cyberpower-virtual@localhost >/dev/null 2>&1`
   - If upsc fails: log error with daemon logs (via `journalctl -u ups-battery-monitor -n 20`) and exit 1
   - If upsc succeeds: log "✓ NUT dummy-ups readable"
   - Check daemon running: `systemctl is-active --quiet ups-battery-monitor`
   - If not active: log daemon logs and exit 1
   - If active: log "✓ Daemon running"

9. **Success message and next steps:**
   - Print "=== Installation Complete ==="
   - Provide clear next steps: viewing logs, checking UPS status, optional upsmon config

Reference from RESEARCH.md Phase 5 install.sh pattern (lines 529-631). Key differences:
- Add systemd-python check (optional, informative)
- Ensure absolute paths in service file (already set in repo)
- Use `systemctl is-active --quiet` for cleaner status checks
- Structured error messages with actionable hints

Do NOT hand-roll configuration parsing; use simple grep + append for NUT config merge.
Do NOT use sudo in the script itself; assume script runs as root via sudo.
Do NOT hardcode user paths if possible; use absolute paths from systemd unit.
  </action>
  <verify>
    <automated>
bash /home/j2h4u/repos/j2h4u/ups-battery-monitor/scripts/install.sh --help 2>&1 | grep -q "Usage\|Must run as root" || echo "Script created successfully"
    </automated>
  </verify>
  <done>
- `scripts/install.sh` exists with 100+ lines
- Script is executable and includes all 9 sections above
- Guard clause for idempotent NUT config merge present
- All prerequisite checks functional
- Verification loop with timeout for virtual UPS device
- upsc and systemctl calls for post-install validation
  </done>
</task>

<task type="auto">
  <name>Task 2: Create tests/test_logging.py for JournalHandler fallback verification</name>
  <files>tests/test_logging.py</files>
  <action>
Create `tests/test_logging.py` with unit tests for logging infrastructure. Purpose: verify JournalHandler fallback works correctly when /dev/log unavailable (test environments) and structured fields work in production (journald).

1. **Imports:**
   - `import logging`, `import sys`, `from unittest.mock import patch, MagicMock`
   - `from systemd.journal import JournalHandler`
   - From conftest.py: existing test fixtures and utilities

2. **Test: `test_journalhandler_fallback_to_stderr`**
   - Mock `systemd.journal.JournalHandler` to raise an exception (simulate /dev/log unavailable)
   - Call logging setup code from src/monitor.py (lines 36-48)
   - Verify fallback handler is StreamHandler (not JournalHandler)
   - Verify handler writes to sys.stderr
   - Log a test message via logger.info() and verify it appears on stderr (capture with capsys)
   - Assert: handler type is StreamHandler AND message contains "[ups-battery-monitor]"

3. **Test: `test_journalhandler_success`**
   - Mock systemd.journal.JournalHandler to succeed (don't raise)
   - Call logging setup code
   - Verify primary handler is JournalHandler
   - Verify formatter includes service identifier
   - Log a test message and verify no exception raised
   - Assert: handler type is JournalHandler

4. **Test: `test_structured_fields_compatible_with_fallback`**
   - Setup logger with fallback handler (mock JournalHandler exception)
   - Log with structured extra fields (dict): `logger.info("Test", extra={'FIELD': 'value'})`
   - Verify logging doesn't crash (fallback handler should handle extra gracefully)
   - Assert: no exception, message logged to stderr

5. **Test: `test_alerter_logger_fallback` (from Phase 4)**
   - Import alerter module (uses alerter.setup_ups_logger("ups-battery-monitor"))
   - Mock JournalHandler exception
   - Call alerter.setup_ups_logger()
   - Verify returned logger has fallback handler
   - Log structured message via logger.warning() with extra fields
   - Assert: handler is StreamHandler, no crash on structured fields

**Implementation notes:**
- Use pytest fixtures for logger cleanup (ensure handlers cleared after each test)
- Conftest.py should already have fixtures for file cleanup; reuse if available
- Keep tests focused on fallback behavior and structured field compatibility
- Min 40 lines, prefer 5-6 assertions per test for good coverage

**Reference:** From RESEARCH.md, Pattern 3 (lines 632-684) shows JournalHandler usage with fallback. This test verifies the fallback is working correctly.
  </action>
  <verify>
    <automated>
python3 -m pytest /home/j2h4u/repos/j2h4u/ups-battery-monitor/tests/test_logging.py -v --tb=short -q
    </automated>
  </verify>
  <done>
- `tests/test_logging.py` exists with 40+ lines
- At least 4 test functions covering fallback, success, structured fields, alerter logger
- All tests pass: pytest exit code 0
- Tests verify JournalHandler graceful degradation when /dev/log unavailable
- Structured field compatibility confirmed (no crashes on extra dict keys)
  </done>
</task>

</tasks>

<verification>
After Wave 0 completion, before Wave 1 starts:

1. **Install script functional (manual pre-flight):**
   ```bash
   bash /home/j2h4u/repos/j2h4u/ups-battery-monitor/scripts/install.sh --help
   # Output: Help message or "Must run as root"

   # Check script is executable
   test -x /home/j2h4u/repos/j2h4u/ups-battery-monitor/scripts/install.sh && echo "✓ executable"
   ```

2. **Tests passing:**
   ```bash
   cd /home/j2h4u/repos/j2h4u/ups-battery-monitor
   python3 -m pytest tests/test_logging.py -v
   # All tests: PASSED
   ```

3. **Full suite passing (no regressions from Phase 4):**
   ```bash
   python3 -m pytest tests/ -q
   # 115+ tests PASSED
   ```
</verification>

<success_criteria>
- [ ] `scripts/install.sh` created, executable, includes all 9 sections
- [ ] `tests/test_logging.py` created, 40+ lines, 4+ test functions
- [ ] Install script has idempotent NUT config merge (grep guard clause)
- [ ] Logging tests verify JournalHandler fallback to stderr
- [ ] All pytest tests passing (no regressions): `python3 -m pytest tests/ -q`
- [ ] Full test suite output: ~115 tests PASSED
</success_criteria>

<output>
After Wave 0 completion, create `.planning/phases/05-operational-setup-systemd-integration/05-01-SUMMARY.md` documenting:
- Install script implementation details (prerequisite checks, idempotence strategy)
- Logging test coverage (fallback scenarios, structured field handling)
- Key decisions made (paths, error handling, verification approach)
</output>
