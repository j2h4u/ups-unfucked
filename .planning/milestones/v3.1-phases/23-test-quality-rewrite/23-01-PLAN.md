---
phase: 23-test-quality-rewrite
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pytest.ini
  - tests/test_capacity_estimator.py
  - tests/test_motd.py
autonomous: true
requirements:
  - TEST-06
  - TEST-07

must_haves:
  truths:
    - "Monte Carlo test is marked @pytest.mark.slow and can be excluded with -m 'not slow'"
    - "All test_motd.py tests are marked @pytest.mark.integration with environment-dependency comment"
    - "No PytestUnknownMarkWarning for 'slow' marker in test output"
  artifacts:
    - path: "pytest.ini"
      provides: "slow marker registration"
      contains: "slow:"
    - path: "tests/test_capacity_estimator.py"
      provides: "@pytest.mark.slow on Monte Carlo test"
      contains: "pytest.mark.slow"
    - path: "tests/test_motd.py"
      provides: "@pytest.mark.integration on all 4 test functions"
      contains: "pytest.mark.integration"
  key_links:
    - from: "pytest.ini"
      to: "tests/test_capacity_estimator.py"
      via: "marker registration enables @pytest.mark.slow without warning"
      pattern: "slow:"
---

<objective>
Register @pytest.mark.slow marker in pytest.ini, apply it to the Monte Carlo test, and mark all test_motd.py tests as @pytest.mark.integration.

Purpose: Enable test suite filtering — fast CI runs exclude slow tests, integration tests can be deselected when running unit tests only.
Output: pytest.ini updated, two test files updated with markers.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-test-quality-rewrite/23-CONTEXT.md
@.planning/phases/23-test-quality-rewrite/23-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register slow marker and apply TEST-06 + TEST-07 markers</name>
  <files>pytest.ini, tests/test_capacity_estimator.py, tests/test_motd.py</files>
  <read_first>
    - pytest.ini (current marker registrations)
    - tests/test_capacity_estimator.py (lines 396-437 — Monte Carlo test)
    - tests/test_motd.py (all 4 test functions)
  </read_first>
  <action>
1. **pytest.ini** — Add `slow` marker registration after the existing `integration` marker:
```ini
markers =
    integration: marks tests requiring full daemon integration (deselect with '-m "not integration"')
    slow: marks tests as slow-running (deselect with '-m "not slow"')
```

2. **tests/test_capacity_estimator.py** — Add `@pytest.mark.slow` decorator to `test_monte_carlo_convergence` method (line 396). Add to the existing docstring a note about seed and runtime:
```
Seed: random.seed(42) — deterministic, ~2-3s for 100 trials.
Mark: @pytest.mark.slow — exclude from fast CI with '-m "not slow"'.
```
Ensure `import pytest` is present at the top of the file.

3. **tests/test_motd.py** — Add `@pytest.mark.integration` decorator to ALL 4 test functions:
   - `test_motd_capacity_displays`
   - `test_motd_convergence_display`  (find by name)
   - `test_motd_new_battery_detection` (find by name)
   - `test_motd_sulfation_display` (find by name)

   Add comment to each: `# Environment-dependent: requires bash, scripts/motd/51-ups.sh, subprocess execution`
   Ensure `import pytest` is present at the top of the file (may already be there from fixtures).
  </action>
  <verify>
    <automated>python3 -m pytest tests/ -x -q --tb=short 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - pytest.ini contains `slow: marks tests as slow-running`
    - `grep -c "pytest.mark.slow" tests/test_capacity_estimator.py` returns 1
    - `grep -c "pytest.mark.integration" tests/test_motd.py` returns at least 4
    - `python3 -m pytest tests/ -m "not slow" -q --co 2>&1 | tail -1` shows fewer tests than total (Monte Carlo excluded)
    - `python3 -m pytest tests/ -x -q --tb=short` exits 0 (all 555 tests pass)
    - No `PytestUnknownMarkWarning` in output
  </acceptance_criteria>
  <done>Monte Carlo test excluded by -m "not slow", all MOTD tests filterable by -m "not integration", all 555 tests still pass</done>
</task>

</tasks>

<verification>
- `python3 -m pytest tests/ -x -q --tb=short` — all 555 tests pass
- `python3 -m pytest tests/ -m "not slow" --co -q 2>&1 | tail -1` — shows 554 tests (1 excluded)
- `python3 -m pytest tests/ -m "not integration" --co -q 2>&1 | tail -1` — MOTD tests excluded
- No pytest warnings about unknown markers
</verification>

<success_criteria>
- TEST-06: Monte Carlo test has @pytest.mark.slow with seed=42 and runtime documented
- TEST-07: All 4 test_motd.py tests have @pytest.mark.integration with environment comment
- pytest.ini has slow marker registered
- Full test suite green (555 passed)
</success_criteria>

<output>
After completion, create `.planning/phases/23-test-quality-rewrite/23-01-SUMMARY.md`
</output>
