---
phase: 22-naming-docs-sweep
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/model.py
  - src/discharge_handler.py
  - src/scheduler_manager.py
  - src/monitor.py
  - tests/test_model.py
  - tests/test_monitor.py
  - tests/test_sulfation_persistence.py
  - tests/test_discharge_event_logging.py
  - tests/test_monitor_integration.py
  - tests/test_scheduler_manager.py
  - tests/test_dispatch.py
  - tests/test_discharge_handler.py
autonomous: true
requirements:
  - NAME-01
  - NAME-03

must_haves:
  truths:
    - "BatteryModel instances use .state everywhere — no .data attribute references remain in src/ or tests/"
    - "_sync_physics_from_data and _sync_physics_to_data are renamed to _sync_physics_from_state and _sync_physics_to_state"
    - "Local variables rls and d in _sync_physics_from_state are renamed to rls_state and stored_params"
    - "All 555 tests pass after the rename sweep"
  artifacts:
    - path: "src/model.py"
      provides: "BatteryModel with self.state, _sync_physics_from_state, _sync_physics_to_state, rls_state, stored_params"
      contains: "self.state"
    - path: "src/discharge_handler.py"
      provides: "All BatteryModel refs use .state"
      contains: "self.battery_model.state"
    - path: "src/scheduler_manager.py"
      provides: "All BatteryModel refs use .state"
      contains: "battery_model.state"
    - path: "src/monitor.py"
      provides: "All BatteryModel refs use .state"
      contains: "self.battery_model.state"
  key_links:
    - from: "src/model.py"
      to: "model.json"
      via: "json.load() assigns to self.state (was self.data)"
      pattern: "self\\.state\\s*="
    - from: "tests/test_model.py"
      to: "src/model.py"
      via: "test assertions use .state attribute"
      pattern: "\\.state\\["
---

<objective>
Rename BatteryModel.data to state across all source and test files, rename _sync_physics_from_data/_sync_physics_to_data methods, and clean up rls/d local variable names.

Purpose: Eliminate the misleading `.data` attribute name (it holds runtime state, not raw data) and replace opaque local variable names with descriptive ones.
Output: All 12 affected files updated, zero stale references, 555 tests green.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-naming-docs-sweep/22-CONTEXT.md
@.planning/phases/22-naming-docs-sweep/22-RESEARCH.md

<interfaces>
<!-- BatteryModel attribute being renamed. All consumers access this attribute. -->

From src/model.py:
```python
class BatteryModel:
    def __init__(self, model_path=None, config=None):
        ...
        self.data = {}  # <-- rename to self.state
        ...
    def _sync_physics_from_data(self):  # <-- rename to _sync_physics_from_state
        ...
    def _sync_physics_to_data(self):    # <-- rename to _sync_physics_to_state
        ...
```

Consumers of .data attribute (all must change to .state):
- src/model.py (80 occurrences) — self.data
- src/discharge_handler.py (10) — self.battery_model.data
- src/scheduler_manager.py (3) — self.battery_model.data or battery_model.data
- src/monitor.py (8) — self.battery_model.data
- tests/test_model.py (37) — model.data, bm.data, battery_model.data
- tests/test_monitor.py (23)
- tests/test_sulfation_persistence.py (17)
- tests/test_discharge_event_logging.py (16)
- tests/test_monitor_integration.py (13)
- tests/test_scheduler_manager.py (10)
- tests/test_dispatch.py (6)
- tests/test_discharge_handler.py (7)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename BatteryModel.data to state across all source and test files</name>
  <files>src/model.py, src/discharge_handler.py, src/scheduler_manager.py, src/monitor.py, tests/test_model.py, tests/test_monitor.py, tests/test_sulfation_persistence.py, tests/test_discharge_event_logging.py, tests/test_monitor_integration.py, tests/test_scheduler_manager.py, tests/test_dispatch.py, tests/test_discharge_handler.py</files>
  <read_first>src/model.py, src/discharge_handler.py, src/scheduler_manager.py, src/monitor.py, tests/test_model.py, tests/test_monitor.py, tests/test_sulfation_persistence.py, tests/test_discharge_event_logging.py, tests/test_monitor_integration.py, tests/test_scheduler_manager.py, tests/test_dispatch.py, tests/test_discharge_handler.py</read_first>
  <action>
Rename `BatteryModel.data` to `BatteryModel.state` across the entire codebase. This is a Python-only rename — model.json schema is NOT affected (there is no JSON key called "data"; `self.data` is the Python variable holding the loaded dict).

**Step 1 — Pre-rename baseline:**
Run `rg '\.data\b' src/ tests/ --type py --count` to confirm ~230 total occurrences (101 src, 129 tests).

**Step 2 — Rename attribute in src/ (4 files):**
In each file, replace all occurrences of `.data` that refer to BatteryModel instances with `.state`:
- `src/model.py` (80 occurrences): `self.data` -> `self.state` everywhere. This includes assignments (`self.data = {}`), dict access (`self.data['lut']`), method calls (`self.data.get(...)`), and in docstrings/comments that reference `self.data`.
- `src/discharge_handler.py` (10 occurrences): `self.battery_model.data` -> `self.battery_model.state`
- `src/scheduler_manager.py` (3 occurrences): `self.battery_model.data` or `battery_model.data` -> `.state`
- `src/monitor.py` (8 occurrences): `self.battery_model.data` -> `self.battery_model.state`

**Step 3 — Rename attribute in tests/ (8 files):**
Apply the same `.data` -> `.state` rename to all test files. Patterns vary: `model.data`, `bm.data`, `battery_model.data`, `daemon.battery_model.data`, etc.

**Step 4 — Rename methods in src/model.py:**
- `_sync_physics_from_data` -> `_sync_physics_from_state` (definition at ~line 205, plus all call sites within model.py)
- `_sync_physics_to_data` -> `_sync_physics_to_state` (definition at ~line 232, plus all call sites within model.py)
- Update the docstrings of both methods: `self.data` references -> `self.state`

**Step 5 — Post-rename verification:**
Run `rg '\.data\b' src/ tests/ --type py` — expect ZERO hits from BatteryModel contexts. (There may be hits on unrelated objects — review each. The key files model.py, discharge_handler.py, scheduler_manager.py, monitor.py must have zero `.data` hits.)
Also run `rg '_sync_physics_from_data\|_sync_physics_to_data' src/ tests/` — expect zero hits.

**Step 6 — Test suite:**
Run `python3 -m pytest -x -q` — expect 555 passed.

**Important:** Do NOT modify model.json or its schema. Do NOT rename any JSON keys. The rename is purely the Python attribute name.
  </action>
  <verify>
    <automated>rg '\.data\b' src/model.py src/discharge_handler.py src/scheduler_manager.py src/monitor.py --type py 2>&1 | grep -c '.' | xargs test 0 -eq && rg '_sync_physics_from_data\|_sync_physics_to_data' src/ tests/ --type py 2>&1 | grep -c '.' | xargs test 0 -eq && python3 -m pytest -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `rg 'self\.data\b' src/model.py` returns zero lines
    - `rg '\.data\b' src/discharge_handler.py src/scheduler_manager.py src/monitor.py` returns zero lines
    - `rg '_sync_physics_from_data' src/` returns zero lines
    - `rg '_sync_physics_to_data' src/` returns zero lines
    - `rg 'self\.state' src/model.py --count` returns >= 80
    - `rg '_sync_physics_from_state' src/model.py` returns >= 1 line (definition)
    - `rg '_sync_physics_to_state' src/model.py` returns >= 1 line (definition)
    - `python3 -m pytest -x -q` exits 0 with 555 passed
  </acceptance_criteria>
  <done>All .data references on BatteryModel replaced with .state in 12 files. Method names updated. Zero stale references. 555 tests green.</done>
</task>

<task type="auto">
  <name>Task 2: Rename rls/d local variables in _sync_physics_from_state</name>
  <files>src/model.py</files>
  <read_first>src/model.py</read_first>
  <action>
In `_sync_physics_from_state` (formerly `_sync_physics_from_data`, renamed in Task 1), rename local variables at ~lines 205-230:

**Rename `rls` (local dict, ~line 211) to `rls_state`:**
```python
# Before:
rls = {}
...
rls[name] = RLSParams(...)
...
rls_state=rls,

# After:
rls_state = {}
...
rls_state[name] = RLSParams(...)
...
rls_state=rls_state,
```

**Rename `d` (loop variable, ~line 213) to `stored_params`:**
```python
# Before:
d = rls_data.get(name, {})
rls[name] = RLSParams(
    theta=d.get('theta', default_theta),
    P=d.get('P', 1.0),
    sample_count=d.get('sample_count', 0),
    forgetting_factor=d.get('forgetting_factor', 0.97),
)

# After:
stored_params = rls_data.get(name, {})
rls_state[name] = RLSParams(
    theta=stored_params.get('theta', default_theta),
    P=stored_params.get('P', 1.0),
    sample_count=stored_params.get('sample_count', 0),
    forgetting_factor=stored_params.get('forgetting_factor', 0.97),
)
```

**Do NOT rename `rls_data` (line 209)** — it is already descriptive.

Also update the `PhysicsParams` constructor call at ~line 229:
```python
# Before:
rls_state=rls,
# After:
rls_state=rls_state,
```

Run `python3 -m pytest -x -q` — expect 555 passed.
  </action>
  <verify>
    <automated>python3 -m pytest -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `rg '^\s+rls = \{\}' src/model.py` returns zero lines (old `rls` dict init gone)
    - `rg '^\s+d = rls_data' src/model.py` returns zero lines (old `d` loop var gone)
    - `rg 'rls_state = \{\}' src/model.py` returns 1 line (new dict init)
    - `rg 'stored_params = rls_data' src/model.py` returns 1 line (new loop var)
    - `rg 'stored_params\.get' src/model.py` returns >= 4 lines (theta, P, sample_count, forgetting_factor)
    - `python3 -m pytest -x -q` exits 0 with 555 passed
  </acceptance_criteria>
  <done>Local variables rls and d replaced with rls_state and stored_params. Code reads clearly. 555 tests green.</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `rg '\.data\b' src/model.py src/discharge_handler.py src/scheduler_manager.py src/monitor.py` — zero BatteryModel .data references
2. `rg '_sync_physics_from_data\|_sync_physics_to_data' src/ tests/` — zero old method name references
3. `rg '^\s+rls = \{\}' src/model.py` and `rg '^\s+d = rls_data' src/model.py` — zero old variable names
4. `python3 -m pytest -q` — 555 passed
</verification>

<success_criteria>
- NAME-01 complete: BatteryModel.data is BatteryModel.state everywhere, including method renames
- NAME-03 complete: rls/d replaced with rls_state/stored_params
- 555 tests pass with no regressions
- Zero stale references verified by grep
</success_criteria>

<output>
After completion, create `.planning/phases/22-naming-docs-sweep/22-01-SUMMARY.md`
</output>
