---
phase: 07-safety-critical-metrics
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/test_monitor.py
autonomous: true
requirements: []
must_haves:
  truths:
    - "Test stubs exist for all 4 integration tests referenced in 07-01-PLAN.md Task 2"
  artifacts:
    - path: "tests/test_monitor.py"
      provides: "4 test function stubs with proper signatures and docstrings"
      min_lines: 4
  key_links: []
---

<objective>
Create test function stubs in tests/test_monitor.py to satisfy Nyquist Rule before Task 2 implementation.

Purpose: Task 2 in Plan 01 has `<verify>` commands that reference test functions that must exist before implementation begins. Wave 0 creates the stubs (empty implementations), allowing Task 2 to populate them with actual assertions.

Output: 4 empty test functions with proper signatures, docstrings, and pass statements.
</objective>

<execution_context>
@/home/j2h4u/.claude/get-shit-done/workflows/execute-plan.md
@/home/j2h4u/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/07-safety-critical-metrics/07-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 0: Create test function stubs for Nyquist compliance</name>
  <files>tests/test_monitor.py</files>
  <action>
Add 4 empty test function stubs to tests/test_monitor.py (after line 190, before other test functions):

```python
def test_per_poll_writes_during_blackout(make_daemon):
    """SAFE-01: Virtual UPS metrics written every poll during OB, not every 6th."""
    pass


def test_handle_event_transition_per_poll_during_ob(make_daemon):
    """SAFE-02: LB flag decision (_handle_event_transition) executes every poll during OB."""
    pass


def test_no_writes_during_online_state(make_daemon):
    """SAFE-01: No spurious writes during OL state — only on poll % 6 boundary."""
    pass


def test_lb_flag_signal_latency(make_daemon):
    """SAFE-02: LB flag written to virtual UPS within <10s of OB transition."""
    pass
```

**Actions:**
1. Locate the line in tests/test_monitor.py where test functions begin (around line 190)
2. Insert the 4 stubs above existing tests
3. Save file — do NOT run tests yet (stubs will fail)

**Why stubs first:** Nyquist Rule requires that verify commands reference tests that exist on disk. We create the stubs now, Task 2 in Plan 01 populates them with assertions.
  </action>
  <verify>
    <automated>cd /home/j2h4u/repos/j2h4u/ups-battery-monitor && grep -n "def test_per_poll_writes_during_blackout\|def test_handle_event_transition_per_poll_during_ob\|def test_no_writes_during_online_state\|def test_lb_flag_signal_latency" tests/test_monitor.py</automated>
  </verify>
  <done>
All 4 test stubs present in tests/test_monitor.py with correct names, docstrings, and pass statements. File committed to git.
  </done>
</task>

</tasks>

<verification>
**Wave 0 Verification:**
- [ ] 4 test functions exist in tests/test_monitor.py with correct signatures
- [ ] Each function has a docstring matching its requirement (SAFE-01 or SAFE-02)
- [ ] Each function contains `pass` statement (no implementation yet)
- [ ] File saved and committed

**Nyquist Compliance After Wave 0:**
- Wave 0 creates stubs on disk → Task 2's `<verify>` commands can now reference them
- Task 2 (Plan 01, Wave 1) populates stubs with actual assertions
- Verify command paths: `pytest tests/test_monitor.py::test_per_poll_writes_during_blackout ...` all valid
</verification>

<success_criteria>
Wave 0 complete when:
- [ ] `test_per_poll_writes_during_blackout` stub exists (SAFE-01)
- [ ] `test_handle_event_transition_per_poll_during_ob` stub exists (SAFE-02)
- [ ] `test_no_writes_during_online_state` stub exists (SAFE-01)
- [ ] `test_lb_flag_signal_latency` stub exists (SAFE-02)
- [ ] All stubs have `pass` body (no logic)
- [ ] File committed to git
- [ ] VALIDATION.md Wave 0 requirements satisfied

**Context for Task 2:** Stubs now ready for population with full test logic.
</success_criteria>

<output>
After completion, create `.planning/phases/07-safety-critical-metrics/07-00-SUMMARY.md` documenting:
- Test stubs created (function names, line numbers)
- Nyquist compliance status
- Ready for Plan 01 Task 2 execution
</output>
