---
phase: 15-foundation
plan: 05
type: execute
wave: 4
depends_on: [01, 02, 03, 04]
files_modified: []
autonomous: true
requirements: []

must_haves:
  truths:
    - "All v2.0 regression tests pass after importing new sulfation/ROI modules"
    - "Daemon main loop imports battery_math modules without breaking existing functionality"
    - "Zero new errors in daemon startup, polling, event handling"
  artifacts:
    - path: "tests/test_monitor.py"
      provides: "Existing daemon tests (regression detection)"
      must_pass: true
    - path: "tests/test_monitor_integration.py"
      provides: "Existing integration tests (regression detection)"
      must_pass: true
    - path: "tests/test_year_simulation.py"
      provides: "Existing year-simulation tests (regression detection)"
      must_pass: true
  key_links:
    - from: "src/monitor.py"
      to: "src/battery_math/sulfation.py"
      via: "import (no code changes in monitor.py)"
      pattern: "from src.battery_math import"
    - from: "src/monitor.py"
      to: "src/battery_math/cycle_roi.py"
      via: "import (no code changes in monitor.py)"
      pattern: "from src.battery_math import"
---

<objective>
Run full test suite to confirm zero regressions after importing sulfation and ROI modules. All existing daemon functionality must pass v2.0 tests unchanged. This validates that Phase 15 pure function modules integrate cleanly without breaking current behavior.

Purpose: Gate-check before handing off to Phase 16; confirm daemon stability
Output: Full test suite green (337+ tests), zero regressions
</objective>

<execution_context>
@/home/j2h4u/.claude/get-shit-done/workflows/execute-plan.md
@/home/j2h4u/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md (Phase 15 success criteria #4: zero daemon regressions)
@.planning/phases/15-foundation/15-VALIDATION.md (regression detection tests)

# Existing test suite from v2.0

- tests/test_monitor.py — Daemon startup, polling, event handling (existing)
- tests/test_monitor_integration.py — Full daemon lifecycle (existing)
- tests/test_year_simulation.py — Battery simulation (existing)
- Expected: 337+ total tests from v2.0 phases
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full regression test suite (all tests from v2.0 + new Phase 15 tests)</name>
  <files></files>
  <read_first>
    - .planning/ROADMAP.md (Phase 15 success criteria)
    - .planning/phases/15-foundation/15-VALIDATION.md (test framework, sampling rate)
  </read_first>
  <action>
Run complete test suite to verify zero regressions:

1. **Run full test suite**:
   ```bash
   python3 -m pytest tests/ -v --tb=short
   ```
   This runs ALL tests including:
   - Existing v2.0 tests (test_monitor.py, test_monitor_integration.py, test_year_simulation.py, etc.)
   - New Phase 15 tests (test_sulfation.py, test_cycle_roi.py, test_nut_client.py::TestINSTCMD, test_sulfation_offline_harness.py)

2. **Verify results**:
   - Exit code must be 0 (all tests pass)
   - No "FAILED" lines in output
   - Count passed tests: expected ≥337 existing + 19 new = 356+ total
   - No skipped tests unless intentional

3. **Check for regressions**:
   - test_monitor.py: All daemon-level tests pass
   - test_monitor_integration.py: Full lifecycle unchanged
   - test_year_simulation.py: Battery simulation unaffected by new modules
   - test_nut_client.py (existing): Connection/polling tests still pass

4. **Document results**:
   - Note total test count
   - Note any test that behaves differently than before (expected: none)
   - Confirm daemon imports sulfation/cycle_roi without errors

5. **No code changes**: This is a verification-only task. If tests fail, report the failure; don't modify tests to make them pass.
  </action>
  <verify>
    <automated>python3 -m pytest tests/ -v --tb=short 2>&1 | tee /tmp/phase15_test_results.txt && echo "TEST SUITE PASSED" || echo "TEST SUITE FAILED"</automated>
  </verify>
  <acceptance_criteria>
    - Command: python3 -m pytest tests/ -v exits with code 0
    - Output contains "passed" count ≥356 (337 existing + 19 new)
    - No "FAILED" in output
    - test_monitor.py: all tests pass
    - test_monitor_integration.py: all tests pass
    - test_year_simulation.py: all tests pass
    - test_sulfation.py: all 8 tests pass
    - test_cycle_roi.py: all 6 tests pass
    - test_nut_client.py (TestINSTCMD): all 3 tests pass
    - test_sulfation_offline_harness.py: all 2 tests pass
  </acceptance_criteria>
  <done>Full test suite passes, zero regressions detected, Phase 15 ready for verification</done>
</task>

</tasks>

<verification>
After task complete:

```bash
# Parse test results
python3 -m pytest tests/ -v --tb=line 2>&1 | tail -20

# Count tests by category
echo "=== Test Breakdown ==="
echo "v2.0 Regression Tests:"
python3 -m pytest tests/test_monitor.py tests/test_monitor_integration.py tests/test_year_simulation.py -v --co -q 2>/dev/null | wc -l

echo "Phase 15 New Tests:"
python3 -m pytest tests/test_sulfation.py tests/test_cycle_roi.py tests/test_nut_client.py::TestINSTCMD tests/test_sulfation_offline_harness.py -v --co -q 2>/dev/null | wc -l

# Final status
python3 -m pytest tests/ -q && echo "✓ ALL TESTS PASS" || echo "✗ SOME TESTS FAIL"
```

Expected output:
- All tests pass (exit 0)
- Total: 356+ tests
- Zero failures, zero regressions
</verification>

<success_criteria>
Phase 15 Foundation complete when:
- [ ] All v2.0 tests pass (337+ tests, existing functionality intact)
- [ ] All Phase 15 new tests pass (19 tests: 8 sulfation unit + 6 ROI unit + 3 INSTCMD unit + 2 integration)
- [ ] Total: 356+ tests all green
- [ ] Zero regressions in daemon behavior
- [ ] Ready for Phase 16 Persistence & Observability
</success_criteria>

<output>
After completion, create `.planning/phases/15-foundation/COMPLETION.md` documenting:
- Test results summary: 356+ tests all passing
- Requirements traceability: SULF-06 and SCHED-02 fully addressed
- Artifacts created: sulfation.py, cycle_roi.py, send_instcmd(), test harnesses, live validation script
- Phase 15 Foundation complete, ready to proceed to Phase 16
</output>
