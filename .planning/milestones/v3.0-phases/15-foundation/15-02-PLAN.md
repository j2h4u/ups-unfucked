---
phase: 15-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_sulfation.py
  - tests/test_cycle_roi.py
  - tests/test_nut_client.py
autonomous: true
requirements: [SULF-06]

must_haves:
  truths:
    - "User can run unit tests for sulfation scoring — test_compute_sulfation_score_* tests pass"
    - "User can run unit tests for recovery delta — test_estimate_recovery_delta_* tests pass"
    - "User can run unit tests for cycle ROI — test_compute_cycle_roi_* tests pass"
    - "All pure function tests work offline (no daemon, no NUT connection, no I/O)"
    - "Test suite confirms both healthy and degraded battery scenarios"
  artifacts:
    - path: "tests/test_sulfation.py"
      provides: "Unit test suite for sulfation.py functions"
      test_cases: ["test_sulfation_score_healthy_battery", "test_sulfation_score_old_battery_idle_high_temp", "test_recovery_delta_good_desulfation", "test_recovery_delta_poor_desulfation", "test_recovery_delta_no_drop"]
    - path: "tests/test_cycle_roi.py"
      provides: "Unit test suite for cycle_roi.py functions"
      test_cases: ["test_cycle_roi_high_benefit", "test_cycle_roi_negative_roi_low_cycles", "test_cycle_roi_break_even", "test_cycle_roi_edge_zero_signals"]
    - path: "tests/test_nut_client.py"
      provides: "Extended unit tests for send_instcmd() method"
      test_cases: ["test_send_instcmd_success", "test_send_instcmd_command_not_supported", "test_send_instcmd_access_denied"]
  key_links:
    - from: "tests/test_sulfation.py"
      to: "src/battery_math/sulfation.py"
      via: "import and test"
      pattern: "from src.battery_math.sulfation import"
    - from: "tests/test_cycle_roi.py"
      to: "src/battery_math/cycle_roi.py"
      via: "import and test"
      pattern: "from src.battery_math.cycle_roi import"
    - from: "tests/test_nut_client.py"
      to: "src/nut_client.py"
      via: "extend existing test file"
      pattern: "from src.nut_client import NUTClient"
---

<objective>
Create comprehensive unit tests for sulfation, cycle ROI, and NUT INSTCMD modules. All tests work offline (no daemon, no UPS hardware, no I/O). Tests validate pure functions with realistic battery scenarios and edge cases.

Purpose: Provide fast feedback loop for pure function correctness before integration testing
Output: Three test files with 10+ unit tests total, all passing
</objective>

<execution_context>
@/home/j2h4u/.claude/get-shit-done/workflows/execute-plan.md
@/home/j2h4u/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/15-foundation/15-RESEARCH.md

# Existing test patterns (from tests/)

From tests/test_soh_calculator.py:
- Use pytest 7.0+ with class-based tests (TestClassName)
- Helper functions for test data setup (e.g., _make_battery_model)
- Assertions with descriptive failure messages: `assert X, f"Reason: {value}"`
- Realistic battery scenarios: healthy partial discharge, degraded battery, edge cases

From tests/test_nut_client.py:
- Mock socket using patch('src.nut_client.socket.socket')
- MagicMock for socket fixtures
- Test both success (OK response) and error (ERR response) paths
- Verify socket lifecycle (connect, recv, close)

From pyproject.toml:
- Test framework: pytest 7.0+
- Run tests: python3 -m pytest tests/ -v
- Config: pyproject.toml [tool.pytest.ini_options]
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create test_sulfation.py unit tests</name>
  <files>tests/test_sulfation.py</files>
  <read_first>
    - tests/test_soh_calculator.py (test class patterns, assertion style, helpers)
    - src/battery_math/sulfation.py (newly created, understand function signatures)
    - .planning/phases/15-foundation/15-RESEARCH.md (Example 1: test structure)
  </read_first>
  <action>
Create tests/test_sulfation.py with pytest-style unit tests:

1. **Module docstring**: "Unit tests for sulfation.py — compute_sulfation_score() and estimate_recovery_delta()"

2. **Test compute_sulfation_score()** (4 test methods):

   a. **test_sulfation_score_healthy_battery_low_idle**:
      - days_since_deep=5.0, ir_trend_rate=0.0, recovery_delta=0.08, temp_celsius=35.0
      - Assert score < 0.3 (healthy battery should have low sulfation)
      - Assert 0.0 <= score <= 1.0

   b. **test_sulfation_score_old_battery_idle_high_temp**:
      - days_since_deep=60.0, ir_trend_rate=0.05, recovery_delta=0.02, temp_celsius=40.0
      - Assert score > 0.6 (idle battery with poor recovery → high sulfation)
      - Assert 0.0 <= score <= 1.0

   c. **test_sulfation_score_high_ir_drift**:
      - days_since_deep=10.0, ir_trend_rate=0.15, recovery_delta=0.05, temp_celsius=35.0
      - Assert score > 0.4 (high IR drift = significant sulfation signal)
      - Assert 0.0 <= score <= 1.0

   d. **test_sulfation_score_clamped_to_range**:
      - days_since_deep=1000.0, ir_trend_rate=10.0, recovery_delta=1.0, temp_celsius=50.0
      - Assert 0.0 <= score <= 1.0 (score never exceeds bounds even with extreme inputs)

3. **Test estimate_recovery_delta()** (3 test methods):

   a. **test_recovery_delta_good_desulfation**:
      - soh_before=0.95, soh_after=0.96 (SoH improved → good recovery)
      - Assert delta > 0.5 (strong desulfation signal)
      - Assert 0.0 <= delta <= 1.0

   b. **test_recovery_delta_poor_desulfation**:
      - soh_before=0.95, soh_after=0.93 (SoH dropped → wear > recovery)
      - Assert delta < 0.3 (low desulfation signal)
      - Assert 0.0 <= delta <= 1.0

   c. **test_recovery_delta_no_drop_no_recovery**:
      - soh_before=0.95, soh_after=0.95 (no SoH change → unclear signal)
      - Assert delta == 0.0 (returns 0 for no drop case)

4. **Realistic scenarios** (optional, 1 test):

   a. **test_sulfation_score_seasonal_variation**:
      - Same battery (days=30, ir=0.03, recovery=0.04) tested at temp 25°C vs 40°C
      - Assert score(40°C) > score(25°C) (higher temp increases sulfation rate)

5. **No mocking**: All tests use pure functions directly with synthetic data
6. **Assertion messages**: Each assert includes descriptive message (e.g., f"Expected score < 0.3, got {score}")
  </action>
  <verify>
    <automated>python3 -m pytest tests/test_sulfation.py -v</automated>
  </verify>
  <acceptance_criteria>
    - File exists: tests/test_sulfation.py
    - Contains 8+ test methods (4 compute_sulfation_score + 3 estimate_recovery_delta + 1 seasonal)
    - All tests use assert statements with descriptive failure messages
    - No mocking, no external I/O
    - All tests pass (exit code 0)
    - Test class or module-level tests using pytest conventions
  </acceptance_criteria>
  <done>test_sulfation.py created with 8 unit tests covering normal + edge cases</done>
</task>

<task type="auto">
  <name>Task 2: Create test_cycle_roi.py unit tests</name>
  <files>tests/test_cycle_roi.py</files>
  <read_first>
    - tests/test_soh_calculator.py (test class pattern)
    - src/battery_math/cycle_roi.py (newly created, understand function signature)
    - .planning/phases/15-foundation/15-RESEARCH.md (Example 2: ROI test scenarios)
  </read_first>
  <action>
Create tests/test_cycle_roi.py with pytest-style unit tests:

1. **Module docstring**: "Unit tests for cycle_roi.py — compute_cycle_roi() pure function"

2. **Test compute_cycle_roi()** (6 test methods):

   a. **test_cycle_roi_high_benefit_low_cost**:
      - days_since_deep=60.0, depth_of_discharge=0.7, cycle_budget_remaining=80, ir_trend_rate=0.1, sulfation_score=0.8
      - Assert roi > 0.5 (high sulfation, many cycles left → strong positive ROI)
      - Assert -1.0 <= roi <= 1.0

   b. **test_cycle_roi_negative_roi_few_cycles**:
      - days_since_deep=5.0, depth_of_discharge=0.8, cycle_budget_remaining=3, ir_trend_rate=0.01, sulfation_score=0.1
      - Assert roi < -0.5 (few cycles left, low sulfation → negative ROI, don't test)
      - Assert -1.0 <= roi <= 1.0

   c. **test_cycle_roi_break_even**:
      - days_since_deep=30.0, depth_of_discharge=0.5, cycle_budget_remaining=50, ir_trend_rate=0.05, sulfation_score=0.5
      - Assert -0.1 < roi < 0.1 (balanced inputs → near break-even)
      - Assert -1.0 <= roi <= 1.0

   d. **test_cycle_roi_edge_no_signals**:
      - days_since_deep=0.0, depth_of_discharge=0.0, cycle_budget_remaining=100, ir_trend_rate=0.0, sulfation_score=0.0
      - Assert roi == 0.0 (zero benefit + zero cost → break-even)

   e. **test_cycle_roi_clamped_to_range**:
      - Extreme values: days_since_deep=1000, ir_trend_rate=10.0, cycle_budget_remaining=1000, sulfation_score=1.0
      - Assert -1.0 <= roi <= 1.0 (never exceeds bounds)

   f. **test_cycle_roi_formula_sanity**:
      - Test that doubling sulfation_score increases ROI (if other params constant)
      - roi1 = compute_cycle_roi(30, 0.5, 50, 0.05, 0.3)
      - roi2 = compute_cycle_roi(30, 0.5, 50, 0.05, 0.6)
      - Assert roi2 > roi1 (higher sulfation = higher benefit = higher ROI)

3. **No mocking**: Pure functions with synthetic data only
4. **Assertion messages**: Descriptive failure messages (e.g., f"Expected roi > 0.5 for high benefit, got {roi}")
  </action>
  <verify>
    <automated>python3 -m pytest tests/test_cycle_roi.py -v</automated>
  </verify>
  <acceptance_criteria>
    - File exists: tests/test_cycle_roi.py
    - Contains 6 test methods covering: high benefit, negative ROI, break-even, edge cases, clamping, formula sanity
    - All tests use assert statements with descriptive messages
    - No mocking, no external I/O
    - All tests pass (exit code 0)
    - Test uses pytest conventions (class-based or module-level)
  </acceptance_criteria>
  <done>test_cycle_roi.py created with 6 unit tests covering ROI calculation scenarios</done>
</task>

<task type="auto">
  <name>Task 3: Extend test_nut_client.py with send_instcmd tests</name>
  <files>tests/test_nut_client.py</files>
  <read_first>
    - tests/test_nut_client.py (existing test file, understand mock patterns)
    - src/nut_client.py (understand send_instcmd() method to be tested)
    - .planning/phases/15-foundation/15-RESEARCH.md (Example 3: INSTCMD test structure)
  </read_first>
  <action>
Extend tests/test_nut_client.py with new test class TestINSTCMD:

1. **Keep existing tests**: Do NOT modify existing TestNUTClientCommunication or TestListVar classes

2. **Add new TestINSTCMD class** at end of file with 3 test methods:

   a. **test_send_instcmd_quick_test_success**:
      - Mock socket responses: 'OK\n' (USERNAME), 'OK\n' (PASSWORD), 'OK\n' (LOGIN), 'OK TRACKING 12345\n' (INSTCMD)
      - client = NUTClient()
      - success, msg = client.send_instcmd('test.battery.start.quick')
      - Assert success is True
      - Assert 'OK' in msg or 'TRACKING' in msg

   b. **test_send_instcmd_command_not_supported**:
      - Mock socket responses: 'OK\n', 'OK\n', 'OK\n', 'ERR CMD-NOT-SUPPORTED\n'
      - success, msg = client.send_instcmd('fake.command.invalid')
      - Assert success is False
      - Assert 'CMD-NOT-SUPPORTED' in msg

   c. **test_send_instcmd_access_denied**:
      - Mock socket responses: 'OK\n', 'OK\n', 'ERR ACCESS-DENIED\n'
      - success, msg = client.send_instcmd('test.battery.start.deep')
      - Assert success is False
      - Assert 'ACCESS-DENIED' in msg

3. **Use existing mock fixture**: Reuse @pytest.fixture mock_nut_socket from existing tests

4. **No new dependencies**: Only use unittest.mock (already imported in existing test file)

5. **Pattern**: Match existing test class style (class-based with mock fixture injection)
  </action>
  <verify>
    <automated>python3 -m pytest tests/test_nut_client.py::TestINSTCMD -v</automated>
  </verify>
  <acceptance_criteria>
    - File: tests/test_nut_client.py
    - Contains new TestINSTCMD class with 3 test methods
    - Existing test classes (TestNUTClientCommunication, TestListVar) still pass
    - All new tests pass (exit code 0)
    - Tests use mock_nut_socket fixture
    - Tests verify success path (OK response) and error paths (ERR responses)
    - grep "def test_send_instcmd" returns 3 results
  </acceptance_criteria>
  <done>test_nut_client.py extended with 3 INSTCMD tests, existing tests still pass</done>
</task>

</tasks>

<verification>
After all tasks complete:

```bash
# Run all new tests
python3 -m pytest tests/test_sulfation.py tests/test_cycle_roi.py tests/test_nut_client.py::TestINSTCMD -v

# Verify test counts
echo "test_sulfation.py:"
grep -c "def test_" tests/test_sulfation.py
echo "test_cycle_roi.py:"
grep -c "def test_" tests/test_cycle_roi.py
echo "test_nut_client.py (TestINSTCMD only):"
grep -A 50 "class TestINSTCMD" tests/test_nut_client.py | grep -c "def test_"
```

Expected results:
- test_sulfation.py: 8 test methods
- test_cycle_roi.py: 6 test methods
- test_nut_client.py (TestINSTCMD): 3 test methods
- All tests pass (0 failures)
</verification>

<success_criteria>
Wave 1 unit tests complete when:
- [ ] tests/test_sulfation.py exists with 8 test methods, all passing
- [ ] tests/test_cycle_roi.py exists with 6 test methods, all passing
- [ ] tests/test_nut_client.py extended with TestINSTCMD (3 methods), all passing
- [ ] Total: 17 new unit tests, all green
- [ ] No new external dependencies (pytest, mock already exist)
- [ ] All tests run offline (no daemon, no UPS hardware)
</success_criteria>

<output>
After completion, create `.planning/phases/15-foundation/15-02-SUMMARY.md` documenting:
- Test coverage for sulfation module (8 tests: 4 sulfation_score + 3 recovery_delta + 1 seasonal)
- Test coverage for cycle ROI module (6 tests covering benefit/cost scenarios)
- Test coverage for INSTCMD protocol (3 tests: success + 2 error paths)
- All tests offline (pure functions, mocked sockets)
- Ready for integration testing in next wave
</output>
