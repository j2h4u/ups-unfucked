---
phase: 14-capacity-reporting-metrics
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [scripts/motd/51-ups.sh, tests/test_motd.py]
autonomous: true
requirements: [RPT-01]

must_haves:
  truths:
    - "MOTD displays measured capacity in Ah with confidence percentage"
    - "MOTD shows sample count (N/3) for convergence tracking"
    - "MOTD shows convergence status badge (LOCKED or MEASURING)"
    - "MOTD gracefully handles missing capacity_estimates array"
  artifacts:
    - path: "scripts/motd/51-ups.sh"
      provides: "MOTD module for capacity display"
      min_lines: 90
    - path: "tests/test_motd.py"
      provides: "Unit and integration tests for MOTD capacity display"
      min_lines: 80
  key_links:
    - from: "scripts/motd/51-ups.sh"
      to: "~/.config/ups-battery-monitor/model.json"
      via: "jq capacity_estimates array extraction"
      pattern: "jq.*capacity_estimates"
    - from: "scripts/motd/51-ups.sh"
      to: "Python convergence_score calculation"
      via: "Python subprocess for CoV calculation"
      pattern: "python3.*PYTHON_EOF"
---

<objective>
Extend MOTD module (51-ups.sh) to display capacity measurement progress with convergence status and confidence percentage. Users see "Capacity: X.XAh (measured) vs Y.YAh (rated) · STATUS_BADGE · N/3 samples · NN% confidence" on every login.

Purpose: Real-time visibility into capacity measurement convergence without CLI queries
Output: Updated 51-ups.sh with capacity display + comprehensive test coverage
</objective>

<execution_context>
@/home/j2h4u/.claude/get-shit-done/workflows/execute-plan.md
@/home/j2h4u/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/14-capacity-reporting-metrics/14-RESEARCH.md
@.planning/phases/14-capacity-reporting-metrics/14-VALIDATION.md

# Existing MOTD patterns (source of truth for format)
@scripts/motd/51-ups.sh
@scripts/motd/runner.sh

# Model schema (capacity_estimates array structure)
@src/model.py
@src/battery_model.py

# Test infrastructure
@tests/test_motd.py
@pytest.ini
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend 51-ups.sh with convergence status display</name>
  <files>scripts/motd/51-ups.sh</files>
  <read_first>
    - scripts/motd/51-ups.sh (current implementation, lines 1-80)
    - .planning/phases/14-capacity-reporting-metrics/14-RESEARCH.md (Example 3: MOTD Integration pattern)
    - src/model.py (capacity_estimates array schema, timestamp and ah_estimate fields)
  </read_first>
  <action>
Extend the existing MOTD module (lines 27-76) to display convergence status alongside capacity measurements.

Current output (from existing code):
  "Capacity: 6.95Ah (measured) vs 7.2Ah (rated), 2/3 deep discharges, 45% confidence"

Target output (Phase 14):
  "  Capacity: 6.95Ah (measured) vs 7.2Ah (rated) · ✓ LOCKED · 3/3 samples · 92% confidence"

Implementation specifics:
1. After existing jq extraction of capacity_estimates array (line 45-68), add Python subprocess that computes:
   - sample_count = len(capacity_estimates)
   - convergence_status = "locked" if (CoV < 0.10 AND count >= 3) else "measuring"
   - convergence_score = int((1 - CoV) * 100) clamped to 0–100

2. Output format from Python: "{status},{sample_count},{confidence_pct}" (comma-separated for bash IFS parsing)

3. Add status_badge formatting logic in bash after IFS parsing:
   - If status == "locked": status_badge="✓ LOCKED" + GREEN color
   - If status == "measuring": status_badge="⟳ MEASURING" + YELLOW color
   - If status == "unknown": status_badge="? UNKNOWN" + DIM color

4. Replace old echo line with new multi-element format:
   OLD: echo "  Capacity: ${latest_ah}Ah (measured) vs ${rated_ah}Ah (rated), ${sample_count}/3 deep discharges, ${confidence_percent}% confidence"
   NEW: echo "  Capacity: ${latest_ah}Ah (measured) vs ${rated_ah}Ah (rated) · ${status_color}${status_badge}${NC} · ${sample_count}/3 samples · ${confidence_pct}% confidence"

5. Preserve graceful degradation: if capacity_estimates is missing, output nothing (exit 0, allow other MOTD modules to run)

6. Add error handling: if Python subprocess times out or fails, output "? UNKNOWN" badge but don't crash

7. Source color variables at top of section if not already defined (GREEN, YELLOW, DIM, NC):
   GREEN="\033[0;32m"
   YELLOW="\033[1;33m"
   DIM="\033[2m"
   NC="\033[0m"  # No Color

Testing: Do not modify test file in this task (Task 2 creates tests)
  </action>
  <verify>
    <automated>
      bash -n scripts/motd/51-ups.sh && echo "Syntax OK"
      grep -n "status_badge" scripts/motd/51-ups.sh | wc -l | grep -q "[1-3]"
      grep -n "convergence_score" scripts/motd/51-ups.sh | wc -l | grep -q "[1-3]"
      grep -n "LOCKED\|MEASURING\|UNKNOWN" scripts/motd/51-ups.sh | wc -l | grep -q "[1-3]"
    </automated>
  </verify>
  <acceptance_criteria>
    - scripts/motd/51-ups.sh has no syntax errors (bash -n passes)
    - Python subprocess in 51-ups.sh computes CoV and returns "{status},{count},{pct}" format
    - Status badge variables (status_badge, status_color) are defined after IFS parsing
    - Output line contains all elements: measured Ah, rated Ah, status_badge, sample_count, confidence_pct
    - Graceful handling: if capacity_estimates missing, script exits 0 (grep "exit 0" or "return 0" visible in error path)
    - Color variables (GREEN, YELLOW, DIM, NC) defined in script or sourced from parent
  </acceptance_criteria>
  <done>51-ups.sh updated with capacity convergence display, handles edge cases, passes syntax check</done>
</task>

<task type="auto">
  <name>Task 2: Create comprehensive MOTD test coverage</name>
  <files>tests/test_motd.py</files>
  <read_first>
    - tests/test_motd.py (existing test structure, if any)
    - tests/ (overall test structure, fixture patterns)
    - .planning/phases/14-capacity-reporting-metrics/14-VALIDATION.md (Wave 0 test list)
    - src/model.py (BatteryModel.load(), capacity_estimates schema)
  </read_first>
  <action>
Create three test functions in tests/test_motd.py to validate MOTD capacity display:

1. test_motd_capacity_displays:
   - Setup: Create temporary model.json with 3 capacity_estimates entries:
     - {timestamp: "2026-03-16T10:00:00Z", ah_estimate: 6.9, confidence: 0.88, ...}
     - {timestamp: "2026-03-16T11:00:00Z", ah_estimate: 7.0, confidence: 0.90, ...}
     - {timestamp: "2026-03-16T12:00:00Z", ah_estimate: 6.95, confidence: 0.92, ...}
   - Execute: Run `bash scripts/motd/51-ups.sh` as subprocess with HOME env pointing to temp model.json directory
   - Assert: Output contains "Capacity:", "6.95Ah", "7.2Ah", "3/3 samples", "LOCKED" or "✓" badge
   - Requirement: RPT-01

2. test_motd_handles_empty_estimates:
   - Setup: Create temporary model.json with empty capacity_estimates array ([] or missing)
   - Execute: Run 51-ups.sh with HOME override, capture stdout/stderr
   - Assert: Exit code is 0 (no crash), output is empty or graceful (no error messages)
   - Requirement: RPT-01

3. test_motd_convergence_status_badge:
   - Setup: Create model.json with 2 estimates (count < 3, state = "measuring"):
     - {ah_estimate: 6.8, confidence: 0.45}
     - {ah_estimate: 7.1, confidence: 0.50}
   - Execute: Run 51-ups.sh with HOME override
   - Assert: Output contains "⟳ MEASURING" or "MEASURING" badge (not "LOCKED")
   - Setup: Add 3rd estimate {ah_estimate: 6.95, confidence: 0.92}, re-run
   - Assert: Output contains "✓ LOCKED" or "LOCKED" badge
   - Requirement: RPT-01

Test utilities:
- Use pytest tmpdir fixture to create temporary .config/ups-battery-monitor/ directories
- Use subprocess.run() with capture_output=True to execute 51-ups.sh
- Override HOME environment variable to point to tmpdir for each test
- For Python subprocess reliability: add 5-second timeout to subprocess calls
- For CoV calculation accuracy: import math.statistics.stdev directly in test to verify against bash output

Fixture pattern (if creating new fixture):
```python
@pytest.fixture
def model_json_temp(tmp_path):
    """Create temporary model.json with capacity_estimates in tmpdir config."""
    config_dir = tmp_path / ".config" / "ups-battery-monitor"
    config_dir.mkdir(parents=True)
    model_file = config_dir / "model.json"
    model_file.write_text(json.dumps({
        "full_capacity_ah_ref": 7.2,
        "capacity_estimates": [...]
    }))
    return tmp_path  # Return tmpdir so HOME can be set to it
```

Error handling in tests:
- If bash subprocess fails to execute: pytest.fail(f"51-ups.sh failed: {stderr}")
- If jq parsing fails: assert "Capacity:" not in output (graceful fallback expected)
- If Python CoV calculation times out: assert exit code 0 and badge is "UNKNOWN"
  </action>
  <verify>
    <automated>
      pytest tests/test_motd.py::test_motd_capacity_displays -v
      pytest tests/test_motd.py::test_motd_handles_empty_estimates -v
      pytest tests/test_motd.py::test_motd_convergence_status_badge -v
    </automated>
  </verify>
  <acceptance_criteria>
    - tests/test_motd.py contains three test functions with exact names (grep -c "def test_motd_" matches 3+)
    - test_motd_capacity_displays asserts output contains "Capacity:", "6.95Ah", "7.2Ah", sample count format
    - test_motd_handles_empty_estimates checks exit code 0 and graceful fallback
    - test_motd_convergence_status_badge verifies status badge changes from "MEASURING" to "LOCKED"
    - All three tests run via pytest and produce green output
    - Test imports include subprocess, json, pytest (grep -E "^import|^from" shows these modules)
    - Tests override HOME environment variable to use tmpdir config directory
  </acceptance_criteria>
  <done>Three comprehensive MOTD tests created, all passing, requirements RPT-01 validated</done>
</task>

</tasks>

<verification>
Phase 14 Plan 01 verification:
1. 51-ups.sh syntax: `bash -n scripts/motd/51-ups.sh` exits 0
2. MOTD format: manual test shows "Capacity: X.XAh (measured) vs Y.YAh (rated) · STATUS · N/3 samples · NN% confidence"
3. Test coverage: `pytest tests/test_motd.py -v` shows 3 green tests
4. Edge cases: MOTD handles empty capacity_estimates, missing model.json, and malformed JSON gracefully
5. HOME override: Tests can run in isolation with HOME pointing to tmpdir (respects MODEL_FILE path)
</verification>

<success_criteria>
- scripts/motd/51-ups.sh updated with convergence status display ✓
- MOTD output format matches Phase 14 spec: measured Ah, rated Ah, status badge, sample count, confidence % ✓
- Three unit/integration tests created and passing ✓
- Requirement RPT-01 satisfied: "MOTD displays rated vs measured capacity and confidence percentage" ✓
- All existing MOTD tests remain green (no regressions) ✓
- Graceful degradation: MOTD exits 0 even if capacity_estimates missing ✓
- Tests properly override HOME environment to use temporary config directories ✓
</success_criteria>

<output>
After completion, create `.planning/phases/14-capacity-reporting-metrics/14-01-SUMMARY.md`
</output>
