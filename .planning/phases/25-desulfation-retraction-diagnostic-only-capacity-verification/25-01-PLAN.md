---
phase: 25-desulfation-retraction-diagnostic-only-capacity-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/battery_math/scheduler.py
  - src/scheduler_manager.py
  - tests/test_scheduler.py
  - tests/test_scheduler_manager.py
autonomous: true
requirements: [SCH-01, SCH-02, SCH-03]
must_haves:
  truths:
    - "With no prior test (days_since_last_test_success = inf, days_since_last_attempt = inf) and only short blackouts, the scheduler proposes a first diagnostic test (deadlock gone)"
    - "The scheduler proposes a test purely on a ~365-day cadence with zero dependency on sulfation score or cycle ROI"
    - "A daemon restart does not re-trigger a test: days_since_last_test_success just under the interval defers, not proposes"
    - "Safety gates hold: SoH < 60% blocks, recent test rate-limits, recent blackout defers on grid cooldown"
    - "Scheduler timing uses TWO independent inputs: days_since_last_test_success (last OK diagnostic, inf if never) drives the ~365-day cadence; days_since_last_attempt (last attempt regardless of OK/ERR, inf if never) drives the 7-day rate-limit. They are computed separately and never collapsed into one value."
    - "A failed test dispatch (last_upscmd_status != OK) does not reset the annual cadence clock: days_since_last_test_success stays inf (or the prior success age), so a transient error cannot defer the next diagnostic ~365 days"
    - "A failed test dispatch DOES count for rate-limiting: days_since_last_attempt reads the recent attempt timestamp, so a failed dispatch 1d ago is rate-limited (defer_test/rate_limit) and cannot be retried on the next daily run"
    - "The first/default proposed test_type is quick"
  artifacts:
    - path: src/battery_math/scheduler.py
      provides: "evaluate_test_scheduling reframed to diagnostic time-cadence, no sulfation/cycle_roi params"
      contains: "DIAGNOSTIC_TEST_INTERVAL_DAYS"
    - path: src/scheduler_manager.py
      provides: "scheduler inputs without sulfation_score/cycle_roi; cadence-driven evaluation; split days_since_last_test_success (cadence) and days_since_last_attempt (rate-limit)"
    - path: tests/test_scheduler.py
      provides: "rewritten scheduler unit tests for the cadence engine"
  key_links:
    - from: src/scheduler_manager.py
      to: src/battery_math/scheduler.py
      via: "evaluate_test_scheduling(...) call with reduced kwargs"
      pattern: "evaluate_test_scheduling\\("
    - from: src/scheduler_manager.py
      to: src/model.py
      via: "get_last_upscmd_timestamp() + last_upscmd_status → days_since_last_test_success (cadence) and days_since_last_attempt (rate-limit)"
      pattern: "get_last_upscmd_timestamp"
---

<objective>
Reframe `evaluate_test_scheduling` from a sulfation/cycle-ROI decision engine into a simple
diagnostic capacity/SoH verification scheduler driven by a ~365-day (IEEE-1188) time cadence,
and fix the confirmed bootstrap deadlock (SCH-01, SCH-02, SCH-03).

Purpose: Today the scheduler only proposes a test when `sulfation_score`/`cycle_roi` exceed
thresholds, but those inputs are 0.0 until a discharge completes — and no discharge happens
without a proposed test. The result is a permanent deadlock where no first test is ever
scheduled. Making the persistent time cadence the *propose* driver kills the deadlock and
aligns the daemon with honest, diagnostic-only behavior (no autonomous "desulfation").

Output: A reframed pure scheduler function, updated SchedulerManager wiring, and rewritten
scheduler unit tests. This plan does NOT delete sulfation.py/cycle_roi.py (Wave 2) — it only
stops the scheduler from depending on them, so Wave 2 can delete cleanly with no live callers.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/25-desulfation-retraction-diagnostic-only-capacity-verification/25-RESEARCH.md
@src/battery_math/scheduler.py
@src/scheduler_manager.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Reframe evaluate_test_scheduling to a diagnostic time-cadence engine</name>
  <files>src/battery_math/scheduler.py</files>
  <read_first>
    - src/battery_math/scheduler.py (current 7-gate engine, constants, `_decision` builder, `_parse_iso_or_warn`)
    - .planning/phases/25-desulfation-retraction-diagnostic-only-capacity-verification/25-RESEARCH.md (sections 1 and 3 — deadlock + new contract)
  </read_first>
  <behavior>
    - First test: `days_since_last_test_success = inf`, `days_since_last_attempt = inf`, soh 0.9, no blackout, cycle_budget 100 → propose_test, test_type="quick", reason_code="diagnostic_cadence"
    - On cadence: `days_since_last_test_success = 400` (≥365), `days_since_last_attempt = 400` → propose_test/quick/diagnostic_cadence
    - Restart no-retrigger: `days_since_last_test_success = 30` (≥7 attempt, <365), `days_since_last_attempt = 30` → defer_test, reason_code="within_cadence", next_eligible ≈ now + (365−30)d
    - Rate limit (any attempt): `days_since_last_attempt = 3` (<7), `days_since_last_test_success = inf` → defer_test/rate_limit (the rate-limit gate keys off the ATTEMPT, so a never-succeeded scheduler is still rate-limited)
    - Failed-attempt boundary 1: failed dispatch 1d ago → `days_since_last_attempt = 1`, `days_since_last_test_success = inf` → defer_test/rate_limit (NOT retried next daily run)
    - Failed-attempt boundary 2: failed dispatch 8d ago, never succeeded → `days_since_last_attempt = 8` (≥7), `days_since_last_test_success = inf` (≥365) → propose_test/quick/diagnostic_cadence (rate-limit cleared, cadence overdue)
    - Success boundary: successful test 30d ago → `days_since_last_test_success = 30`, `days_since_last_attempt = 30` → defer_test/within_cadence (a real success resets the cadence clock)
    - SoH floor: soh 0.55 → block_test/soh_floor regardless of cadence
    - Grid cooldown: last_blackout 1h ago, cooldown 4h, success/attempt 400 → defer_test/grid_unstable
    - Cycle budget: cycle_budget_remaining 3 (<5) → block_test/critical_cycle_budget
  </behavior>
  <action>
    Rewrite `evaluate_test_scheduling` signature to keyword-only params:
    `soh_fraction`, `days_since_last_test_success`, `days_since_last_attempt`,
    `last_blackout_timestamp`, `cycle_budget_remaining`, `grid_stability_cooldown_hours=4.0`.
    The TWO timing inputs are independent and MUST NOT be collapsed: `days_since_last_test_success`
    is the age of the last OK diagnostic (inf if never succeeded) and drives only the cadence gate;
    `days_since_last_attempt` is the age of the last attempt regardless of OK/ERR status (inf if
    never attempted) and drives only the rate-limit gate. REMOVE params `sulfation_score`,
    `cycle_roi`, `active_blackout_credit`, and the old single `days_since_last_test`.
    Keep the `SchedulerDecision` dataclass and `_decision`/`_parse_iso_or_warn` helpers. Remove
    constants `ROI_THRESHOLD`, `DEEP_SULFATION_THRESHOLD`, `QUICK_SULFATION_THRESHOLD`; keep
    `SOH_FLOOR`, `MIN_DAYS_BETWEEN_TESTS`, `CRITICAL_CYCLE_BUDGET`; add
    `DIAGNOSTIC_TEST_INTERVAL_DAYS = 365.0` with an IEEE-1188 annual-capacity-cadence comment.
    New gate order: (1) SoH floor block, (2) rate-limit defer
    (`days_since_last_attempt < MIN_DAYS_BETWEEN_TESTS` — keyed off ANY attempt so a failed
    dispatch is still rate-limited), (3) grid-stability defer (unchanged blackout-cooldown logic),
    (4) cycle-budget block (`< CRITICAL_CYCLE_BUDGET`),
    (5) cadence: if `days_since_last_test_success >= DIAGNOSTIC_TEST_INTERVAL_DAYS` → propose_test
    with test_type="quick", reason_code="diagnostic_cadence", reason_detail like
    f"{days_since_last_test_success:.0f}d since last successful test"; else defer_test,
    reason_code="within_cadence", reason_detail with days remaining, eligible_in =
    timedelta(days=interval − days_since_last_test_success). Because gate 2 (attempt) runs before
    gate 5 (success), a failed dispatch 1d ago is caught by rate-limit before the cadence gate ever
    sees the inf success age — so the two requirements (failure doesn't defer cadence; failure is
    still rate-limited) both hold. Drop the blackout-credit gate entirely (Wave 2 removes the credit
    machinery). Update the module docstring and the `Guard clause order` docstring to describe
    diagnostic cadence and the success-vs-attempt timing split, not sulfation. Engine emits only
    test_type="quick" (deep stays in the Literal but is not autonomously proposed, per SCH-03).
  </action>
  <verify>
    <automated>set -o pipefail; uv run pytest tests/test_scheduler.py -x</automated>
  </verify>
  <done>evaluate_test_scheduling has no sulfation_score/cycle_roi/active_blackout_credit params; takes split `days_since_last_test_success` (cadence) and `days_since_last_attempt` (rate-limit) params with no single `days_since_last_test`; rate-limit gate (2) uses `days_since_last_attempt`, cadence gate (5) uses `days_since_last_test_success`; proposes on ~365d cadence; first-test-from-inf proposes quick; a failed-attempt-1d-ago case (attempt=1, success=inf) defers via rate_limit; all new test_scheduler.py cases pass; `grep -n "sulfation\|cycle_roi" src/battery_math/scheduler.py` is empty.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Update SchedulerManager wiring + rewrite scheduler tests</name>
  <files>src/scheduler_manager.py, tests/test_scheduler.py, tests/test_scheduler_manager.py</files>
  <read_first>
    - src/scheduler_manager.py (`_gather_scheduler_inputs`, `run_daily` verbose log, `_execute_scheduler_decision`, `evaluate_test_scheduling` call site, `_get_last_natural_blackout`, `_calculate_days_since_last_test`)
    - src/model.py (`update_upscmd_result` writes last_upscmd_timestamp+last_upscmd_type+last_upscmd_status together on EVERY attempt OK or error, ~line 870; `get_last_upscmd_timestamp` ~line 885; `append_discharge_event` writes discharge_events with event_reason incl. "test_initiated" ~line 578-588)
    - tests/test_scheduler.py (existing 30 cases — full rewrite target)
    - tests/test_scheduler_manager.py (patched `evaluate_test_scheduling` return + precondition/dispatch tests)
  </read_first>
  <behavior>
    - SchedulerManager.run_daily proposes a test on a fresh model (no last_upscmd_timestamp) with soh 0.9 and online metrics — proves the deadlock fix end-to-end through the manager
    - _gather_scheduler_inputs returns a dict with NO sulfation_score/cycle_roi/active_credit keys, and with BOTH days_since_last_test_success and days_since_last_attempt keys (no single days_since_last_test key)
    - test_scheduler_manager precondition + dispatch tests still pass with the slimmed decision
    - Two-input timing split (resolves cycle-2 HIGH — rate-limit vs cadence cannot both hold with one value):
      `_calculate_days_since_last_attempt` reads last_upscmd_timestamp regardless of status (inf if absent/corrupt) → drives rate-limit.
      `_calculate_days_since_last_test_success` returns the age of the last OK diagnostic (inf if the last status is not "OK", or if never attempted) → drives cadence.
      Because update_upscmd_result writes timestamp+status together on every attempt, a failed dispatch
      (last_upscmd_status="ERR_SOCKET: ...", recent last_upscmd_timestamp) yields
      days_since_last_attempt=small (rate-limited, NOT retried next daily run) AND
      days_since_last_test_success=inf (cadence NOT deferred ~365d). Both requirements hold simultaneously.
  </behavior>
  <action>
    In `_gather_scheduler_inputs`, remove the `sulfation_score`, `cycle_roi`, and `active_credit`
    keys (drop the `discharge_handler.last_sulfation_score`/`last_cycle_roi` reads and the
    `battery_model.get_blackout_credit()` read). Replace the single `days_since_last_test` key with
    TWO keys: `days_since_last_test_success` (from `_calculate_days_since_last_test_success()`) and
    `days_since_last_attempt` (from `_calculate_days_since_last_attempt()`). Keep `soh_fraction`,
    `last_blackout`, `cycle_budget`. Update the `evaluate_test_scheduling(...)` call at ~line 244
    to pass the new kwargs (`soh_fraction`, `days_since_last_test_success`, `days_since_last_attempt`,
    `last_blackout_timestamp`, `cycle_budget_remaining`, `grid_stability_cooldown_hours`); remove
    `sulfation_score=`, `cycle_roi=`, `active_blackout_credit=`, and the old single
    `days_since_last_test=`. Remove the verbose-scheduling log lines that format
    `sulfation_score`/`cycle_roi` (run_daily ~235-236) and the `sulfation_score`/`roi` keys in the
    `_execute_scheduler_decision` structured log (~355-356). Update the class/`__init__` docstring
    that says "discharge_handler: ... for sulfation score, cycle ROI". Keep `_get_last_natural_blackout`
    (feeds grid stability).

    SPLIT `_calculate_days_since_last_test` (the restart-safe persistent trigger, SCH-02) into TWO
    methods that resolve the cycle-2 HIGH — a single value cannot satisfy both "failed attempt is
    rate-limited" and "failed attempt does not defer cadence":
      1. `_calculate_days_since_last_attempt(self) -> float` — the renamed/repurposed body of the
         current method: reads ONLY `get_last_upscmd_timestamp()`, regardless of status; returns inf
         if absent; catches ValueError/TypeError → inf (corrupt-timestamp gate preserved). This feeds
         the rate-limit gate, so ANY attempt (OK or ERR) rate-limits the next one.
      2. `_calculate_days_since_last_test_success(self) -> float` — reads `last_upscmd_timestamp` AND
         `last_upscmd_status` from `battery_model.state` (status via `state.get("last_upscmd_status")`;
         there is no dedicated getter — read the state key directly). When `last_upscmd_status != "OK"`
         (a transient dispatch error like "ERR_SOCKET: ..."), return inf so a failed dispatch — which
         still writes `last_upscmd_timestamp` via `update_upscmd_result` at model.py:870 — cannot defer
         the next annual diagnostic by ~365 days. When status == "OK", return the age of
         `last_upscmd_timestamp` (same corrupt-timestamp → inf handling). This feeds ONLY the cadence
         gate. (Note: there is no separate persisted "last successful test" timestamp; because
         update_upscmd_result overwrites timestamp+status together, "last status OK" is the correct
         success proxy — a subsequent failed attempt flips status to ERR and correctly forces the
         cadence input to inf until the next successful test.)

    Add THREE boundary tests in tests/test_scheduler_manager.py (the three the reviewer specified):
      a. Recent failed attempt is rate-limited: model with last_upscmd_timestamp = now−1d and
         last_upscmd_status="ERR_SOCKET: ..." → _calculate_days_since_last_attempt ≈ 1,
         _calculate_days_since_last_test_success = inf; assert the gathered inputs produce a
         defer_test/rate_limit decision (NOT propose) — the failed attempt is not retried next run.
      b. Old failed attempt does not defer cadence: last_upscmd_timestamp = now−8d,
         last_upscmd_status="ERR_SOCKET: ..." → days_since_last_attempt ≈ 8 (≥7, rate-limit cleared),
         days_since_last_test_success = inf (≥365) → propose_test/diagnostic_cadence.
      c. Successful test resets cadence: last_upscmd_timestamp = now−30d, last_upscmd_status="OK" →
         days_since_last_test_success ≈ 30, days_since_last_attempt ≈ 30 → defer_test/within_cadence.

    Then REWRITE tests/test_scheduler.py to cover the cadence engine per Task 1's
    behavior list using the two split params (first-test-from-inf, propose-on-cadence,
    within-cadence defer = restart-no-retrigger, rate-limit-via-attempt, the failed-attempt boundaries,
    soh floor, grid cooldown, cycle budget). In tests/test_scheduler_manager.py, drop
    sulfation_score/cycle_roi from any constructed inputs/patched decisions; keep precondition and
    dispatch coverage.
  </action>
  <verify>
    <automated>set -o pipefail; uv run pytest tests/test_scheduler.py tests/test_scheduler_manager.py -x</automated>
  </verify>
  <done>SchedulerManager no longer reads last_sulfation_score/last_cycle_roi/get_blackout_credit; `_calculate_days_since_last_test` is split into `_calculate_days_since_last_attempt` (status-agnostic, feeds rate-limit) and `_calculate_days_since_last_test_success` (inf when last_upscmd_status != "OK", feeds cadence); `_gather_scheduler_inputs` returns both days_since_last_test_success and days_since_last_attempt and no single days_since_last_test; the evaluate_test_scheduling call passes the split kwargs; the three boundary tests (recent-failed→rate_limit, old-failed→diagnostic_cadence, success-30d→within_cadence) pass; `grep -n "sulfation\|cycle_roi" src/scheduler_manager.py` is empty; both test files green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| model.json → scheduler | Persistent `last_upscmd_timestamp` is operator/daemon-written state read to gate an autonomous UPS test command |
| scheduler decision → UPS instcmd | A propose_test decision can dispatch a real `test.battery.start.quick` to hardware |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-01 | Tampering | corrupted `last_upscmd_timestamp` in model.json | mitigate | both `_calculate_days_since_last_attempt` and `_calculate_days_since_last_test_success` catch ValueError/TypeError → treat as never-tested (inf); SoH floor + rate-limit gates still bound dispatch |
| T-25-02 | Denial of Service | repeated FAILED dispatch retried every daily run | mitigate | rate-limit gate (2) keys off `days_since_last_attempt`, which counts ANY attempt (OK or ERR) via `update_upscmd_result` writing `last_upscmd_timestamp` on error too; a failed dispatch 1d ago therefore defers the next via `MIN_DAYS_BETWEEN_TESTS`. The cadence gate's separate `days_since_last_test_success` input keeps the failure from suppressing the next annual diagnostic — the two-input split closes the cycle-2 rate-limit-bypass HIGH |
| T-25-03 | Elevation of Privilege | scheduler proposes test on a weak/unstable battery | accept/mitigate | SoH floor (≥60%), grid-stability cooldown, cycle-budget gates retained ahead of propose; first test forced to low-risk `quick` |
| T-25-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan (pure refactor of existing stdlib code) |
</threat_model>

<verification>
- `grep -rn "sulfation\|cycle_roi" src/battery_math/scheduler.py src/scheduler_manager.py` returns nothing.
- `grep -n "days_since_last_test\b" src/battery_math/scheduler.py src/scheduler_manager.py` returns nothing (the single collapsed param/key is gone; only the two split names remain).
- `set -o pipefail; uv run pytest tests/test_scheduler.py tests/test_scheduler_manager.py` green.
- Deadlock proof: a SchedulerManager test on a fresh model (no last_upscmd_timestamp, soh 0.9, online) results in a propose_test/quick decision.
- Restart-safety proof: with `days_since_last_test_success` just under the interval, the engine defers (does not re-propose).
- Two-input split proof (resolves cycle-2 HIGH) — three boundary cases must hold simultaneously:
  (a) recent failed attempt: last_upscmd_timestamp = now−1d, last_upscmd_status="ERR_SOCKET: ..." → days_since_last_attempt ≈ 1, days_since_last_test_success = inf → defer_test/rate_limit (failed dispatch NOT retried next daily run);
  (b) old failed attempt: last_upscmd_timestamp = now−8d, status ERR → days_since_last_attempt ≈ 8, days_since_last_test_success = inf → propose_test/diagnostic_cadence (a transient error did not defer cadence ~365d);
  (c) successful test: last_upscmd_timestamp = now−30d, status "OK" → days_since_last_test_success ≈ 30 → defer_test/within_cadence (success resets the cadence clock).
</verification>

<success_criteria>
- evaluate_test_scheduling is sulfation/cycle_roi-free and proposes on a ~365-day cadence.
- First diagnostic test is scheduled from a cold start (deadlock gone); restart does not re-trigger.
- SoH-floor / rate-limit / grid-stability / cycle-budget safety gates remain; first test is quick.
- scheduler.py and scheduler_manager.py no longer reference the soon-to-be-deleted modules, unblocking Wave 2 deletion.
</success_criteria>

<output>
Create `.planning/phases/25-desulfation-retraction-diagnostic-only-capacity-verification/25-01-SUMMARY.md` when done.
</output>
