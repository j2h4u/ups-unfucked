---
phase: 25-desulfation-retraction-diagnostic-only-capacity-verification
plan: 02
type: execute
wave: 2
depends_on: [25-01]
files_modified:
  - src/battery_math/sulfation.py
  - src/battery_math/cycle_roi.py
  - src/battery_math/__init__.py
  - src/discharge_handler.py
  - src/model.py
  - src/monitor.py
  - src/monitor_config.py
  - src/virtual_ups_exporter.py
  - src/motd_status.py
  - scripts/motd/55-sulfation.sh
  - scripts/install.sh
  - tests/test_sulfation.py
  - tests/test_sulfation_persistence.py
  - tests/test_sulfation_offline_harness.py
  - tests/test_cycle_roi.py
  - tests/test_discharge_handler.py
  - tests/test_discharge_event_logging.py
  - tests/test_model.py
  - tests/test_health_endpoint_v16.py
  - tests/test_motd_status.py
  - tests/test_monitor.py
  - tests/test_dispatch.py
  - tests/test_year_simulation.py
autonomous: true
requirements: [RET-01, RET-02, RET-03, RET-04]
must_haves:
  truths:
    - "src/battery_math/sulfation.py and src/battery_math/cycle_roi.py no longer exist"
    - "grep for 'sulfation' and 'cycle_roi' across src, tests, and scripts returns nothing EXCEPT the single factual aging comment at scripts/battery-health.py:109 (lead-sulfate physics, not a product claim — explicitly exempted)"
    - "scripts/install.sh no longer references 55-sulfation.sh; the MOTD install loop installs only 51-ups.sh and 51-ups-health.sh"
    - "sulfation_score / cycle_roi / sulfation_confidence / recovery_delta fields are absent from the health endpoint, model.json schema/validation, MOTD output, and journald discharge events"
    - "discharge_event persistence survives the rewrite: append_discharge_event is still called every completed discharge with timestamp/event_reason/duration_seconds/depth_of_discharge/measured_capacity_ah (cycle_roi key dropped)"
    - "blackout-credit-as-desulfation and recovery_delta-as-evidence logic is removed from the discharge path and scheduler"
    - "full pytest is green and vulture reports no dead code from the removal"
  artifacts:
    - path: src/battery_math/__init__.py
      provides: "battery_math exports with no sulfation/cycle_roi symbols"
    - path: src/discharge_handler.py
      provides: "discharge pipeline with SoH/Peukert/IR/replacement but no sulfation scoring or blackout credit"
    - path: src/monitor_config.py
      provides: "HealthSnapshot/CurrentMetrics + write_health_endpoint without sulfation/cycle_roi/recovery fields"
  key_links:
    - from: src/virtual_ups_exporter.py
      to: src/monitor_config.py
      via: "HealthSnapshot construction without sulfation fields"
      pattern: "HealthSnapshot\\("
    - from: src/discharge_handler.py
      to: src/model.py
      via: "append_discharge_event without cycle_roi"
      pattern: "append_discharge_event"
---

<objective>
Remove all disproven-premise sulfation/cycle-ROI machinery: delete the two kernel modules,
strip their callers and persisted/exported fields, remove blackout-credit-as-desulfation and
recovery-delta-as-evidence logic, and delete or surgically clean the tests that exercised them
(RET-01, RET-02, RET-03, RET-04).

Purpose: The v3.0 active-desulfation premise is disproven (discharge forms sulfate; charging
reverses it; the daemon has no charge control). Carrying the scoring code, persisted history,
health/MOTD/journald fields, and blackout-credit machinery makes the daemon lie about what it
does. This plan makes the codebase honest. Wave 1 already removed the scheduler's dependency on
these symbols, so deletion leaves no live callers.

Output: Two deleted kernel files, a slimmed discharge pipeline + model + health/exporter/MOTD
surface, and a clean test suite. Final grep/vulture gates prove completeness.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/25-desulfation-retraction-diagnostic-only-capacity-verification/25-RESEARCH.md
@src/discharge_handler.py
@src/model.py
@src/monitor_config.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete kernel modules and strip the discharge pipeline + blackout credit (RET-01, RET-02, RET-03)</name>
  <files>src/battery_math/sulfation.py, src/battery_math/cycle_roi.py, src/battery_math/__init__.py, src/discharge_handler.py, src/model.py, src/monitor.py</files>
  <read_first>
    - src/battery_math/__init__.py (imports/exports of compute_sulfation_score, estimate_recovery_delta, SulfationState, compute_cycle_roi)
    - src/discharge_handler.py (imports lines 18-24; DischargeMetrics dataclass — 16 fields at 44-67: now_iso, sulfation_state, roi, sulfation_score_r, days_since_deep_r, ir_trend_r, recovery_delta_r, discharge_duration, dod_r, depth_of_discharge, roi_r, soh_new, soh_delta, discharge_trigger, capacity_ah_ref, confidence_level; last_* fields 110-117; `_score_and_persist_sulfation` 273; `_compute_sulfation_metrics` 293; `_persist_sulfation_and_discharge` 397 — note its `append_discharge_event({timestamp,event_reason,duration_seconds,depth_of_discharge,measured_capacity_ah,cycle_roi})` call; `_assess_sulfation_confidence`; `_grant_blackout_credit` 460; `_log_discharge_complete` 440-458 keys; `update_battery_health` 119-150 calls `_score_and_persist_sulfation(soh_after, soh_delta, discharge_buffer, discharge_trigger, capacity_ah_ref)` then `_log_discharge_prediction` then `safe_save`)
    - src/model.py (setdefaults 299/301/308; `append_sulfation_history` 561-576; `append_discharge_event` cycle_roi 588; set/get/clear_blackout_credit 835-852/889-891; blackout_credit validation 367-373; `_validate_and_clamp_fields` list-check + `save()` cap list 606-607; required/missing-keys list ~353)
    - src/monitor.py (lines 370-380 — the `clear_blackout_credit()` call on battery replacement)
    - .planning/phases/25-desulfation-retraction-diagnostic-only-capacity-verification/25-RESEARCH.md (section 4)
  </read_first>
  <action>
    Delete files src/battery_math/sulfation.py and src/battery_math/cycle_roi.py (`git rm`).
    In src/battery_math/__init__.py remove the `from .cycle_roi import compute_cycle_roi` and
    `from .sulfation import SulfationState, compute_sulfation_score, estimate_recovery_delta`
    lines and their four `__all__` entries.
    In src/discharge_handler.py: remove the cycle_roi/sulfation imports; delete the
    `_score_and_persist_sulfation`, `_compute_sulfation_metrics`, `_persist_sulfation_and_discharge`,
    `_assess_sulfation_confidence`, and `_grant_blackout_credit` methods; remove the
    `last_sulfation_score`, `last_sulfation_confidence`, `last_recovery_delta`, `last_cycle_roi`
    instance fields (keep `last_cycle_budget_remaining`, `last_days_since_deep`,
    `last_ir_trend_rate`, `last_discharge_timestamp`).

    DischargeMetrics decision (resolves the reviewers' AMBIGUOUS keep/remove gap): the source
    dataclass has 16 fields. ELIMINATE the dataclass entirely — it exists only to carry data
    between the now-deleted `_compute_sulfation_metrics` → `_persist...`/`_log...` trio, and 8 of
    its 16 fields (sulfation_state, roi, sulfation_score_r, recovery_delta_r, roi_r, plus the
    sulfation/roi framing of the rest) are disproven-premise. Replace the whole compute→persist→log
    trio with one inline slim path inside `update_battery_health`. The two fields the reviews
    flagged — `capacity_ah_ref` and `confidence_level` — are handled as follows: `capacity_ah_ref`
    is ALREADY a local in `update_battery_health` (returned by `_compute_soh` as `capacity_ah_ref`)
    and MUST be persisted as `measured_capacity_ah` (do NOT drop it — it is the measured-capacity
    context SoH/replacement prediction depends on); `confidence_level` (sulfation-confidence
    assessment) is REMOVED with the sulfation machinery.

    Rewrite `update_battery_health` so the EXACT surviving pipeline is, in order (enumerated
    checklist — executor must preserve every step):
      1. `soh_before = self.battery_model.get_soh()`  (unchanged)
      2. `soh_result = self._compute_soh(discharge_buffer)`; early-return on None  (unchanged)
      3. unpack `soh_after, capacity_ah_ref = soh_result`  (unchanged)
      4. `avg_load = self._avg_load(...)`  (unchanged)
      5. `replacement_prediction = self._predict_replacement(soh_after, capacity_ah_ref)`  (unchanged)
      6. `self._check_alerts(...)`  (unchanged)
      7. `self._auto_calibrate_peukert(...)`  (unchanged)
      8. `discharge_trigger = self._classify_discharge_trigger(discharge_buffer)`  (unchanged)
      9. `soh_delta = soh_after - soh_before`  (unchanged)
     10. compute `now_iso`, `discharge_duration` (times[-1]-times[0]), and `dod` from the buffer
         (inline the few lines `_compute_sulfation_metrics` used for these non-sulfation values)
     11. update surviving last_* fields (last_days_since_deep, last_ir_trend_rate,
         last_cycle_budget_remaining, last_discharge_timestamp) — drop last_sulfation_*/last_cycle_roi/last_recovery_delta
     12. `self.battery_model.append_discharge_event({"timestamp": now_iso, "event_reason":
         discharge_trigger, "duration_seconds": discharge_duration, "depth_of_discharge": dod,
         "measured_capacity_ah": capacity_ah_ref})`  — KEEP this call; DROP only the `cycle_roi` key.
         Do NOT call `append_sulfation_history` (deleted) and do NOT grant blackout credit (deleted).
     13. emit the `discharge_complete` journald log via `logger.info(... extra={...})` with keys
         event_type, discharge_trigger, duration_seconds, depth_of_discharge, measured_capacity_ah,
         temperature_celsius, temperature_source, timestamp — DROP sulfation_score, sulfation_confidence,
         recovery_delta, cycle_roi.
     14. `self._log_discharge_prediction(discharge_buffer)`  (unchanged)
     15. `safe_save(self.battery_model)`  (unchanged)
    Net effect: SoH, Peukert calibration, replacement prediction, alerts, IR trend, measured-capacity
    persistence, and the discharge audit trail all survive; only sulfation scoring, cycle_roi, recovery
    delta, sulfation_history, and blackout credit are removed.
    In src/model.py: remove `setdefault("sulfation_history", [])`, `setdefault("roi_history", [])`,
    `setdefault("blackout_credit", None)`; delete `append_sulfation_history`,
    `set_blackout_credit`, `clear_blackout_credit`, `get_blackout_credit`; drop `cycle_roi` from
    the `append_discharge_event` docstring; remove `sulfation_history`/`roi_history` from the
    `_validate_and_clamp_fields` list-type check and from the `save()` `_cap_history_entries`
    calls and from the required/missing-keys list; delete the `blackout_credit` dict-validation block.
    In src/monitor.py: remove the `self.battery_model.clear_blackout_credit()` call (lines ~376-379)
    and its "desulfation credit" comment.

    Stale model.json keys (operator action — addresses the reviewers' HIGH "stale state persists"
    finding): removing the `setdefault()` calls and validation does NOT purge `sulfation_history`,
    `roi_history`, `blackout_credit`, or per-event `cycle_roi` already resident in a deployed
    `~/.config/ups-battery-monitor/model.json` — `save()` round-trips `self.state` as-is. Per the
    project no-backward-compat / single-host policy (REQUIREMENTS Out of Scope: "state regenerates"),
    the chosen disposition is NOT to add migration/`pop()` code. Instead the operator deletes the
    local model file so the daemon regenerates a clean one on next start. Add a short
    `## Deployment note` block at the END of this plan's eventual 25-02-SUMMARY.md AND a
    Consequences bullet in the ADR (handled in Plan 03 Task 1) stating: "After deploy, delete
    ~/.config/ups-battery-monitor/model.json (and re-run install if needed) so stale
    sulfation_history / roi_history / blackout_credit / cycle_roi keys are dropped — state
    regenerates from live discharges; there is no migration." Operational cost (executor/operator
    must know): deleting model.json also discards the learned SoH / measured-capacity / LUT state,
    so capacity estimation restarts from scratch and re-warms over subsequent discharges. This is
    accepted per the no-backward-compat policy, but is a real cost — record it in the SUMMARY's
    deploy note and mirror it as an ADR 0001 Consequences bullet (Plan 03 Task 1). Do not write
    migration code here.
    Use `git rm` for the two deleted files; use Edit for all in-place changes (never heredoc).
  </action>
  <verify>
    <automated>set -o pipefail; uv run pytest tests/test_discharge_handler.py tests/test_model.py -x && test ! -f src/battery_math/sulfation.py && test ! -f src/battery_math/cycle_roi.py && echo FILES_GONE</automated>
  </verify>
  <done>Both kernel files deleted; battery_math/__init__ exports no sulfation/cycle_roi symbols; discharge_handler has no sulfation scoring, no blackout credit, no recovery_delta evidence, the DischargeMetrics dataclass is eliminated, and update_battery_health still calls append_discharge_event every discharge with measured_capacity_ah (capacity_ah_ref) and no cycle_roi key; model.py has no sulfation_history/roi_history/blackout_credit methods or fields; monitor.py no longer calls clear_blackout_credit; the model.json operator-delete note is captured for the SUMMARY and ADR.</done>
</task>

<task type="auto">
  <name>Task 2: Strip sulfation/cycle_roi fields from health endpoint, exporter, MOTD, and installer (RET-04)</name>
  <files>src/monitor_config.py, src/virtual_ups_exporter.py, src/motd_status.py, scripts/motd/55-sulfation.sh, scripts/install.sh</files>
  <read_first>
    - src/monitor_config.py (HealthSnapshot fields 303-308; write_health_endpoint health_data keys 344-349; docstring line 75)
    - src/virtual_ups_exporter.py (write_health_snapshot HealthSnapshot construction, lines 53-58)
    - src/motd_status.py (line 12 doc + line 82 sulfation_pct mapping)
    - scripts/motd/55-sulfation.sh (the MOTD module rendering sulfation_pct + next-test)
    - scripts/install.sh (the MOTD install loop at line 240: `for motd_name in 51-ups.sh 51-ups-health.sh 55-sulfation.sh; do`)
  </read_first>
  <action>
    In src/monitor_config.py remove the HealthSnapshot fields `sulfation_score`,
    `sulfation_confidence`, `recovery_delta`, `cycle_roi` and their corresponding `health_data`
    entries `sulfation_score`, `sulfation_score_confidence`, `recovery_delta`, `cycle_roi`
    (keep `days_since_deep`, `ir_trend_rate`, `cycle_budget_remaining`, `scheduling_reason`,
    `next_test_timestamp`, `last_discharge_timestamp` — legitimate diagnostics). Update the
    line-75 docstring that mentions "ROI threshold, sulfation".
    In src/virtual_ups_exporter.py remove the `sulfation_score=`, `sulfation_confidence=`,
    `recovery_delta=`, `cycle_roi=` keyword args from the HealthSnapshot(...) construction in
    `write_health_snapshot`.
    In src/motd_status.py remove the `"sulfation_pct": _fraction_to_pct(health.get("sulfation_score"))`
    mapping (line 82) and the sulfation mention in the module docstring (line 12).
    Delete scripts/motd/55-sulfation.sh entirely (`git rm`) — RET-04 removes MOTD sulfation output;
    next-diagnostic-test visibility moves to battery-health.py in plan 03.
    In scripts/install.sh: in the MOTD install loop at line 240, remove `55-sulfation.sh` from the
    `for motd_name in 51-ups.sh 51-ups-health.sh 55-sulfation.sh; do` list so the loop becomes
    `for motd_name in 51-ups.sh 51-ups-health.sh; do`. (Critical: install.sh runs under
    `set -euo pipefail`; leaving the deleted script in the loop breaks `install.sh` on the real host
    when the source file is gone.) Remove any adjacent comment that names the sulfation MOTD module.
  </action>
  <verify>
    <automated>set -o pipefail; uv run pytest tests/test_health_endpoint_v16.py tests/test_motd_status.py -x && ! rg -n "sulfation|cycle_roi" src/monitor_config.py src/virtual_ups_exporter.py src/motd_status.py scripts/motd/ scripts/install.sh && echo CLEAN</automated>
  </verify>
  <done>health.json no longer carries sulfation_score/sulfation_score_confidence/recovery_delta/cycle_roi; HealthSnapshot has no sulfation fields; exporter passes none; motd_status drops sulfation_pct; 55-sulfation.sh is deleted; scripts/install.sh MOTD loop no longer lists 55-sulfation.sh; grep clean across these files (rg returns nothing).</done>
</task>

<task type="auto">
  <name>Task 3: Delete sulfation/cycle_roi tests and surgically clean mixed tests; prove grep + vulture clean (RET-04)</name>
  <files>tests/test_sulfation.py, tests/test_sulfation_persistence.py, tests/test_sulfation_offline_harness.py, tests/test_cycle_roi.py, tests/test_discharge_handler.py, tests/test_discharge_event_logging.py, tests/test_model.py, tests/test_health_endpoint_v16.py, tests/test_motd_status.py, tests/test_monitor.py, tests/test_dispatch.py, tests/test_year_simulation.py</files>
  <read_first>
    - tests/test_sulfation.py, tests/test_sulfation_persistence.py, tests/test_sulfation_offline_harness.py, tests/test_cycle_roi.py (delete candidates — confirm each exercises ONLY deleted code)
    - tests/test_discharge_handler.py, tests/test_discharge_event_logging.py, tests/test_model.py, tests/test_health_endpoint_v16.py, tests/test_motd_status.py, tests/test_monitor.py, tests/test_dispatch.py, tests/test_year_simulation.py (surgical — keep non-sulfation assertions)
    - .planning/phases/25-desulfation-retraction-diagnostic-only-capacity-verification/25-RESEARCH.md (section 5)
  </read_first>
  <action>
    `git rm` the four wholesale-delete test files: tests/test_sulfation.py,
    tests/test_sulfation_persistence.py, tests/test_sulfation_offline_harness.py,
    tests/test_cycle_roi.py. For the mixed files, remove only the assertions/fixtures/imports
    that reference deleted symbols or fields (sulfation_score, sulfation_confidence,
    recovery_delta, cycle_roi, compute_sulfation_score, compute_cycle_roi, estimate_recovery_delta,
    blackout_credit, append_sulfation_history, set/get/clear_blackout_credit), keeping the SoH /
    Peukert / discharge_event / health-key / MOTD / dispatch assertions intact. For
    test_year_simulation.py ensure the year-long simulation still drives discharges and asserts
    SoH/capacity behavior without sulfation. After edits run the full suite and the removal gates.
    If vulture flags any now-unreferenced helper left behind by the removal, delete it.
    (Test-edit failure mode, light touch: an incomplete surgical edit to test_dispatch.py /
    test_year_simulation.py — e.g. a dangling import of a deleted symbol or a half-removed fixture —
    surfaces as an import/collection error caught immediately by `uv run pytest`, so the gate fails
    loudly rather than passing silently.)

    Grep-clean gate (addresses the reviewers' HIGH "grep cannot pass" finding): there is exactly
    ONE legitimate remaining match — the factual lead-sulfate aging comment at
    `scripts/battery-health.py:109` ("rising R means aging (sulfation, grid corrosion)"). That is
    physics, not a product claim, and is exempted here; Plan 03 Task 3 separately rewords it to drop
    the literal token so the strict gate can pass repo-wide. Until Plan 03 runs, this plan's gate
    excludes that single line. Use ripgrep with an exclusion so the gate FAILS on any real leftover
    but PASSES on the known factual comment:
    `! rg -n "sulfation|cycle_roi" src tests scripts | rg -v "scripts/battery-health.py:.*aging \(sulfation"`
    (the GLOSSARY factual physics note is in docs/, outside the src/tests/scripts grep scope, so no
    docs exemption is needed here).
    Cross-reference: this exemption is intentionally TEMPORARY. The AUTHORITATIVE strict repo-wide
    grep (no exemptions) runs in Plan 03 Task 3 AFTER the line-109 reword removes the literal
    `sulfation` token from scripts/battery-health.py; this Wave 2 gate carries the single exemption
    only until that reword lands.
  </action>
  <verify>
    <automated>set -o pipefail; uv run pytest && uvx vulture src && { LEFT=$(rg -n "sulfation|cycle_roi" src tests scripts | rg -v "scripts/battery-health.py:.*aging \(sulfation" || true); if [ -z "$LEFT" ]; then echo GREP_CLEAN; else echo "LEFTOVERS_FOUND:"; echo "$LEFT"; exit 1; fi; }</automated>
  </verify>
  <done>The four sulfation/cycle_roi test files are deleted; mixed tests retain their non-sulfation coverage; the only remaining `sulfation` match under src/tests/scripts is the exempted factual comment at scripts/battery-health.py:109 (everything else returns clean); full `uv run pytest` is green; `uvx vulture src` reports no dead code from the removal.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| daemon → health.json/model.json | Removing persisted fields changes the schema consumed by Grafana/MOTD/battery-health.py |
| discharge event → journald | Structured discharge events lose sulfation/cycle_roi keys |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-04 | Information Disclosure | stale sulfation fields persisting in old model.json | accept | Single-host, no backward-compat by project policy (REQUIREMENTS Out of Scope). `save()` round-trips `self.state`, so stale `sulfation_history`/`roi_history`/`blackout_credit`/per-event `cycle_roi` are NOT auto-purged by removing `setdefault`/validation. Disposition: operator deletes `~/.config/ups-battery-monitor/model.json` at deploy so state regenerates clean — documented in 25-02-SUMMARY and ADR 0001 Consequences (Plan 03). No migration code is written. |
| T-25-05 | Denial of Service | removing a field still referenced by an unseen consumer crashes the daemon | mitigate | grep gate across src/tests/scripts + full pytest + vulture must pass before commit; HealthSnapshot construction is the single write path |
| T-25-06 | Repudiation | losing discharge audit trail | accept | discharge_event (timestamp, reason, DoD, capacity) is retained; only the disproven cycle_roi/sulfation fields are dropped |
| T-25-SC | Tampering | npm/pip/cargo installs | accept | No package installs — deletion/refactor of existing code only |
</threat_model>

<verification>
- `test ! -f src/battery_math/sulfation.py && test ! -f src/battery_math/cycle_roi.py`.
- `rg -n "sulfation|cycle_roi" src tests scripts` returns ONLY the exempted factual comment at
  `scripts/battery-health.py:109` (reworded to drop the token by Plan 03); every other match is gone.
- `set -o pipefail; uv run pytest` green; `uvx vulture src` clean (exit 0); `uvx ruff check src tests` clean.
- health.json schema (test_health_endpoint_v16) no longer asserts sulfation/cycle_roi keys.
- `append_discharge_event` is still invoked per discharge with `measured_capacity_ah` (and no `cycle_roi`),
  proving discharge-event/measured-capacity persistence survived the slim-path rewrite.
- `scripts/install.sh` MOTD loop no longer lists `55-sulfation.sh` (install survives the file deletion).
- Operator deploy note (delete local model.json) is present in 25-02-SUMMARY.md and ADR 0001.
</verification>

<success_criteria>
- sulfation.py and cycle_roi.py deleted with all production callers.
- No sulfation_score/cycle_roi/sulfation_confidence/recovery_delta anywhere in code, persisted state, health endpoint, MOTD, or journald.
- Blackout-credit-as-desulfation and recovery-delta-as-evidence logic gone.
- Dead tests removed; suite green; vulture/ruff clean.
</success_criteria>

<output>
Create `.planning/phases/25-desulfation-retraction-diagnostic-only-capacity-verification/25-02-SUMMARY.md` when done.
</output>
