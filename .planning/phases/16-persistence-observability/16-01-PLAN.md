---
phase: 16-persistence-observability
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/test_sulfation_persistence.py
  - tests/test_health_endpoint_v16.py
  - tests/test_journald_sulfation_events.py
  - tests/test_discharge_event_logging.py
autonomous: true
requirements: []
must_haves:
  truths:
    - "Test scaffolds exist with stub test cases for each Wave 1+ integration requirement"
    - "Each test file imports required modules (BatteryModel, write_health_endpoint, discharge_handler)"
    - "Test runner accepts all four test files without import errors"
  artifacts:
    - path: "tests/test_sulfation_persistence.py"
      provides: "Model.json persistence layer tests for SULF-05"
      min_lines: 30
    - path: "tests/test_health_endpoint_v16.py"
      provides: "Health.json export tests for RPT-01"
      min_lines: 30
    - path: "tests/test_journald_sulfation_events.py"
      provides: "Structured journald event tests for RPT-02"
      min_lines: 30
    - path: "tests/test_discharge_event_logging.py"
      provides: "Discharge event persistence tests for RPT-03"
      min_lines: 30
  key_links:
    - from: "test_sulfation_persistence.py"
      to: "src/model.py"
      via: "import BatteryModel"
      pattern: "from src.model import BatteryModel"
    - from: "test_health_endpoint_v16.py"
      to: "src/monitor_config.py"
      via: "import write_health_endpoint"
      pattern: "from src.monitor_config import write_health_endpoint"
---

<objective>
Create test scaffold files for Phase 16 integration tests (Nyquist Rule Wave 0).

Purpose: Establish test infrastructure for model.json persistence, health.json export, journald event logging, and discharge event tracking. These are blocking requirements for Wave 1+ implementations.

Output: Four test files with basic test structure (imports, fixtures, stub test cases) ready for implementation in Waves 1–5.
</objective>

<execution_context>
@/home/j2h4u/.claude/get-shit-done/workflows/execute-plan.md
@/home/j2h4u/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md — Phase 16 requirements (SULF-01 through RPT-03)
@.planning/phases/16-persistence-observability/16-RESEARCH.md — Architecture patterns and code examples
@.planning/phases/16-persistence-observability/16-VALIDATION.md — Test map and Nyquist requirements

# Key Reference Files
@src/model.py — BatteryModel class with existing save/load patterns
@src/monitor_config.py — write_health_endpoint() function signature
@src/discharge_handler.py — DischargeHandler class for integration hooks
@tests/test_monitor.py — Existing test patterns and fixtures
@tests/test_logging.py — JournalHandler mocking pattern for journald tests
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create test_sulfation_persistence.py scaffold</name>
  <files>tests/test_sulfation_persistence.py</files>
  <read_first>
    - src/model.py (understand BatteryModel class, save() method, existing history patterns)
    - tests/test_monitor.py (reference test structure and fixtures)
    - .planning/phases/16-persistence-observability/16-RESEARCH.md (see Example 1: model.json schema extension)
  </read_first>
  <action>
Create `tests/test_sulfation_persistence.py` with:

1. **Imports:**
   - `pytest`, `tempfile`, `json`, `pathlib.Path`
   - `from src.model import BatteryModel`
   - `from datetime import datetime, timezone`

2. **Fixtures:**
   - `battery_model_temp_file()` — creates temporary model.json file, returns BatteryModel instance
   - `sample_sulfation_entry()` — returns dict with: timestamp, event_type, sulfation_score, days_since_deep, ir_trend_rate, recovery_delta, temperature_celsius, confidence_level

3. **Test Cases (stub implementations, no assertions yet):**
   - `test_append_sulfation_history_single_entry()` — verify append_sulfation_history() method exists and accepts dict parameter
   - `test_append_sulfation_history_multiple_entries()` — verify multiple appends work
   - `test_sulfation_history_saved_to_model_json()` — verify model.json contains sulfation_history array after save()
   - `test_prune_sulfation_history_keeps_last_30()` — verify _prune_sulfation_history(keep_count=30) preserves only last 30 entries
   - `test_append_discharge_event()` — verify append_discharge_event() method exists and persists to model.json
   - `test_discharge_event_schema_correctness()` — verify discharge_event has all required fields (timestamp, event_reason, duration_seconds, depth_of_discharge, measured_capacity_ah, cycle_roi)
   - `test_backward_compatibility_missing_keys()` — verify BatteryModel loads v2.0 model.json (missing sulfation_history key) without error

4. **Markers:**
   - `@pytest.mark.integration` — mark all tests as integration tests

5. **File length:** ≥30 lines of code

Example stub:
```python
def test_append_sulfation_history_single_entry(battery_model_temp_file, sample_sulfation_entry):
    model = battery_model_temp_file
    # TODO: Implement assertion
    pass
```
  </action>
  <verify>
    <automated>python3 -m pytest tests/test_sulfation_persistence.py --collect-only 2>&1 | grep -c "test_"</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/test_sulfation_persistence.py` exists and is readable
    - File contains `import pytest` statement
    - File contains `from src.model import BatteryModel` statement
    - File contains function `def battery_model_temp_file()` (fixture)
    - File contains ≥7 test functions (test_append_*, test_prune_*, test_backward_compatibility_*)
    - File imports `from src.model import BatteryModel` without error: `python3 -c "from tests.test_sulfation_persistence import *; import sys; sys.exit(0)"`
    - pytest --collect-only shows ≥7 tests collected
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Create test_health_endpoint_v16.py scaffold</name>
  <files>tests/test_health_endpoint_v16.py</files>
  <read_first>
    - src/monitor_config.py (understand write_health_endpoint() function signature and all parameters)
    - tests/test_monitor.py (reference test patterns for write_health_endpoint testing)
    - .planning/phases/16-persistence-observability/16-RESEARCH.md (see Example 3: health.json schema extension)
  </read_first>
  <action>
Create `tests/test_health_endpoint_v16.py` with:

1. **Imports:**
   - `pytest`, `json`, `tempfile`, `pathlib.Path`
   - `from src.monitor_config import write_health_endpoint`
   - `from datetime import datetime, timezone`

2. **Fixtures:**
   - `health_endpoint_temp_file()` — creates temporary health.json file path, returns Path object
   - `baseline_health_params()` — returns dict with baseline parameters: soc_percent=75.5, is_online=True, poll_latency_ms=0.3, capacity_ah_measured=6.8, capacity_ah_rated=7.2, capacity_confidence=0.95, capacity_samples_count=5, capacity_converged=True

3. **Test Cases (stub implementations):**
   - `test_write_health_endpoint_creates_file()` — verify write_health_endpoint() creates health.json file
   - `test_health_endpoint_includes_v16_sulfation_fields()` — verify health.json contains: sulfation_score, sulfation_score_confidence, days_since_deep, ir_trend_rate, recovery_delta
   - `test_health_endpoint_includes_v16_roi_fields()` — verify health.json contains: cycle_roi, cycle_budget_remaining, scheduling_reason, next_test_timestamp
   - `test_health_endpoint_includes_v16_discharge_fields()` — verify health.json contains: last_discharge_timestamp, natural_blackout_credit
   - `test_health_endpoint_nulls_when_sulfation_not_provided()` — verify health.json allows sulfation_score=None without error
   - `test_health_endpoint_preserves_v20_fields()` — verify health.json includes all v2.0 fields (last_poll, current_soc_percent, online, daemon_version, poll_latency_ms, capacity fields)
   - `test_health_endpoint_iso8601_timestamps()` — verify last_poll and last_discharge_timestamp use ISO8601 format
   - `test_health_endpoint_unix_timestamp()` — verify last_poll_unix field contains integer unix timestamp

4. **Markers:**
   - `@pytest.mark.integration` — mark all tests

5. **File length:** ≥30 lines

  </action>
  <verify>
    <automated>python3 -m pytest tests/test_health_endpoint_v16.py --collect-only 2>&1 | grep -c "test_"</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/test_health_endpoint_v16.py` exists and is readable
    - File contains `import pytest` statement
    - File contains `from src.monitor_config import write_health_endpoint` statement
    - File contains function `def health_endpoint_temp_file()` (fixture)
    - File contains ≥8 test functions
    - pytest --collect-only shows ≥8 tests collected
    - File imports `from tests.test_health_endpoint_v16 import *` without error
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Create test_journald_sulfation_events.py scaffold</name>
  <files>tests/test_journald_sulfation_events.py</files>
  <read_first>
    - tests/test_logging.py (understand JournalHandler mocking pattern used in existing tests)
    - .planning/phases/16-persistence-observability/16-RESEARCH.md (see Pattern 4: journald structured events, Example 4)
  </read_first>
  <action>
Create `tests/test_journald_sulfation_events.py` with:

1. **Imports:**
   - `pytest`, `logging`, `json`
   - `from unittest.mock import patch, MagicMock`
   - `from datetime import datetime, timezone`
   - `from io import StringIO`

2. **Fixtures:**
   - `mock_journal_handler()` — create and configure mocked JournalHandler (follow pattern from test_logging.py)
   - `discharge_event_data()` — returns dict with discharge event fields: event_type='discharge_complete', event_reason='natural', duration_seconds=1200, depth_of_discharge=0.75, sulfation_score=0.45, cycle_roi=0.52

3. **Test Cases (stub implementations):**
   - `test_discharge_complete_logged_to_journald()` — verify logger.info('Discharge complete', extra={...}) is called
   - `test_journald_event_includes_event_type_field()` — verify 'event_type': 'discharge_complete' in extra dict
   - `test_journald_event_includes_event_reason_field()` — verify 'event_reason' in extra dict (value: 'natural' | 'test_initiated')
   - `test_journald_event_includes_discharge_metrics()` — verify extra dict contains: duration_seconds, depth_of_discharge, sulfation_score, cycle_roi
   - `test_journald_event_timestamp_field()` — verify timestamp field exists (ISO8601 format)
   - `test_journald_event_structured_format()` — verify all extra fields are serializable to JSON
   - `test_journald_query_by_event_type()` — verify journalctl -o json query can filter by EVENT_TYPE=discharge_complete

4. **Markers:**
   - `@pytest.mark.integration`

5. **File length:** ≥30 lines

  </action>
  <verify>
    <automated>python3 -m pytest tests/test_journald_sulfation_events.py --collect-only 2>&1 | grep -c "test_"</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/test_journald_sulfation_events.py` exists and is readable
    - File contains `import pytest` statement
    - File contains `from unittest.mock import patch, MagicMock` statement
    - File contains ≥7 test functions
    - pytest --collect-only shows ≥7 tests collected
    - File is syntactically valid: `python3 -m py_compile tests/test_journald_sulfation_events.py`
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: Create test_discharge_event_logging.py scaffold</name>
  <files>tests/test_discharge_event_logging.py</files>
  <read_first>
    - src/model.py (BatteryModel class, append_discharge_event method pattern)
    - src/discharge_handler.py (understand where discharge events are triggered)
    - tests/test_monitor.py (reference test structure)
  </read_first>
  <action>
Create `tests/test_discharge_event_logging.py` with:

1. **Imports:**
   - `pytest`, `json`, `tempfile`, `pathlib.Path`
   - `from src.model import BatteryModel`
   - `from src.discharge_handler import DischargeHandler` (if importable; if circular, use mock)
   - `from datetime import datetime, timezone`

2. **Fixtures:**
   - `battery_model_with_discharge_events()` — creates BatteryModel with pre-populated discharge_events array
   - `sample_discharge_event()` — returns dict with: timestamp, event_reason, duration_seconds, depth_of_discharge, measured_capacity_ah, cycle_roi

3. **Test Cases (stub implementations):**
   - `test_append_discharge_event_to_model()` — verify append_discharge_event() method exists
   - `test_discharge_event_schema_required_fields()` — verify discharge_event has: timestamp, event_reason, duration_seconds, depth_of_discharge, measured_capacity_ah, cycle_roi
   - `test_discharge_event_reason_values()` — verify event_reason accepts 'natural' | 'test_initiated'
   - `test_discharge_event_persisted_in_model_json()` — verify discharge_events array persists after model.save()
   - `test_discharge_event_timestamp_format()` — verify timestamp is ISO8601 string
   - `test_prune_discharge_events_keeps_last_30()` — verify _prune_discharge_events(keep_count=30) preserves only last 30
   - `test_discharge_events_queryable_by_reason()` — verify discharge_events can be filtered by event_reason in model.data['discharge_events']

4. **Markers:**
   - `@pytest.mark.integration`

5. **File length:** ≥30 lines

  </action>
  <verify>
    <automated>python3 -m pytest tests/test_discharge_event_logging.py --collect-only 2>&1 | grep -c "test_"</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/test_discharge_event_logging.py` exists and is readable
    - File contains `import pytest` statement
    - File contains `from src.model import BatteryModel` statement
    - File contains ≥7 test functions
    - pytest --collect-only shows ≥7 tests collected
    - File imports without error: `python3 -c "from tests.test_discharge_event_logging import *; import sys; sys.exit(0)"`
  </acceptance_criteria>
</task>

</tasks>

<verification>
After all tasks complete:

1. All four test files exist in `tests/` directory
2. No import errors when running: `python3 -m pytest tests/test_sulfation_persistence.py tests/test_health_endpoint_v16.py tests/test_journald_sulfation_events.py tests/test_discharge_event_logging.py --collect-only`
3. Total test count ≥28 (7+8+7+7 minimum)
4. All test files are syntactically valid Python
5. No circular imports or missing dependencies
</verification>

<success_criteria>
All four test scaffold files created with:
- ✅ Proper pytest structure (fixtures, test functions, markers)
- ✅ Correct imports (no circular dependencies)
- ✅ Stub test cases (ready for Wave 1+ implementation)
- ✅ Coverage of all Phase 16 integration requirements (SULF-05, RPT-01, RPT-02, RPT-03)
- ✅ Total ≥28 test functions across all four files
- ✅ All files syntactically valid and importable
- ✅ Nyquist Rule satisfied: Wave 0 complete, Wave 1+ can proceed
</success_criteria>

<output>
After completion, create `.planning/phases/16-persistence-observability/16-01-SUMMARY.md`

Include:
- Tasks completed (4 scaffolds)
- Test count per file (7+8+7+7)
- Nyquist Rule status (Wave 0 complete)
- Next steps: Wave 1 (model.json schema extension)
</output>
