---
phase: 03-virtual-ups-safe-shutdown
plan: 04
type: execute
wave: 3
depends_on: [03-03]
files_modified: [systemd/ups-battery-monitor.service, config/dummy-ups.conf]
autonomous: true
requirements: [SHUT-01, SHUT-02, SHUT-03]
must_haves:
  truths:
    - "systemd service ensures /dev/shm is available before daemon starts"
    - "NUT dummy-ups is configured to read from /dev/shm/ups-virtual.dev"
    - "upsmon can connect to virtual UPS and receive LB signal"
    - "Graceful shutdown coordination: LB flag only fires when time_rem < threshold"
  artifacts:
    - path: "systemd/ups-battery-monitor.service"
      provides: "Service unit with After=sysinit.target dependency"
      contains: "After=sysinit.target, Type=simple, ExecStart="
    - path: "config/dummy-ups.conf"
      provides: "NUT configuration snippet for dummy-ups device"
      contains: "[cyberpower-virtual] driver=dummy-ups port=/dev/shm/ups-virtual.dev mode=dummy-once"
  key_links:
    - from: "systemd service"
      to: "/dev/shm tmpfs"
      via: "After=sysinit.target ensures basic system up"
      pattern: "After=sysinit\\.target"
    - from: "NUT upsd"
      to: "dummy-ups driver"
      via: "reads /dev/shm/ups-virtual.dev on /etc/nut/ups.conf load"
      pattern: "driver = dummy-ups"
    - from: "upsmon"
      to: "LB flag"
      via: "monitors ups.status field, triggers LOWBATT notify + SHUTDOWNCMD when LB present"
      pattern: "MONITOR.*cyberpower-virtual@localhost"
---

<objective>
Update systemd service and provide NUT configuration snippet to ensure safe daemon startup and virtual UPS integration with NUT ecosystem.

Purpose: Guarantee /dev/shm is available when daemon starts; document how to register virtual UPS with NUT; establish shutdown coordination via LB flag.

Output: systemd service with sysinit.target dependency; NUT configuration snippet; verified integration with existing NUT/upsmon setup.
</objective>

<execution_context>
@/home/j2h4u/.claude/get-shit-done/workflows/execute-plan.md
@/home/j2h4u/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-virtual-ups-safe-shutdown/03-RESEARCH.md (Pitfalls: /dev/shm availability, NUT config conflicts)
@CONTEXT.md (NUT configuration, upsmon FINALDELAY)
@.planning/phases/01-foundation-nut-integration-core-infrastructure/01-05-SUMMARY.md (existing systemd service)

# Wave 2 completion
@.planning/phases/03-virtual-ups-safe-shutdown/03-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update systemd service with After=sysinit.target dependency</name>
  <files>systemd/ups-battery-monitor.service</files>
  <action>
    Extend existing systemd/ups-battery-monitor.service with sysinit.target dependency to ensure /dev/shm is mounted and ready.

    Edit the service file:
    1. Find the [Unit] section
    2. Update After= line to include sysinit.target (if not already present)
       - Old: After= (or empty)
       - New: After=sysinit.target (or After=sysinit.target nut-server.service if nut dependency exists)
       - Reasoning: sysinit.target guarantees basic system services (filesystems, tmpfs) are online before daemon starts

    3. Verify other settings remain unchanged:
       - Type=simple (foreground daemon)
       - ExecStart=/usr/local/bin/ups-battery-monitor (or equivalent path from Phase 1)
       - StandardOutput=journal (logging to journald)
       - Restart=on-failure
       - RestartSec=10

    4. No changes to service logic, just startup dependency ordering

    Context: From RESEARCH.md Pitfall 2, daemon should verify /dev/shm exists at startup. This After= dependency + daemon-level check provides defense-in-depth.

    File should look like:
    ```ini
    [Unit]
    Description=UPS Battery Monitor with Virtual UPS
    Documentation=file:///home/j2h4u/repos/j2h4u/ups-battery-monitor/README.md
    After=sysinit.target nut-server.service
    Wants=nut-server.service

    [Service]
    Type=simple
    ExecStart=/usr/local/bin/ups-battery-monitor
    StandardOutput=journal
    StandardError=journal
    Restart=on-failure
    RestartSec=10

    [Install]
    WantedBy=multi-user.target
    ```

    Note: If the service doesn't exist yet (from Phase 1), create it. If it exists, update the After= line.
  </action>
  <verify>
    <automated>grep -E "After=|sysinit" systemd/ups-battery-monitor.service | head -1</automated>
  </verify>
  <done>systemd service updated: After=sysinit.target ensures /dev/shm is available. Service unit is syntactically valid.</done>
</task>

<task type="auto">
  <name>Task 2: Create NUT dummy-ups configuration snippet</name>
  <files>config/dummy-ups.conf</files>
  <action>
    Create config/dummy-ups.conf with NUT configuration block for virtual UPS device.

    This file documents the NUT configuration needed to register the virtual UPS. Installation (Phase 5) will merge this into /etc/nut/ups.conf.

    File contents:
    ```bash
    # Configuration block to add to /etc/nut/ups.conf
    # This registers the virtual UPS that reads corrected metrics from /dev/shm/ups-virtual.dev
    # Added by ups-battery-monitor Phase 3

    [cyberpower-virtual]
    driver = dummy-ups
    port = /dev/shm/ups-virtual.dev
    mode = dummy-once
    desc = "Virtual UPS proxy with corrected battery metrics from calculated model"
    ```

    Explanation of parameters:
    - [cyberpower-virtual]: Device name (matches references in upsmon.conf)
    - driver = dummy-ups: NUT dummy-ups driver reads .dev file
    - port = /dev/shm/ups-virtual.dev: Path to virtual device file written by daemon
    - mode = dummy-once: Re-parse file on timestamp change (vs dummy-loop which polls; more CPU efficient)
    - desc: Human-readable description

    Installation notes (for Phase 5):
    - This block is appended to /etc/nut/ups.conf (or inserted before real UPS block)
    - Real UPS block [cyberpower] remains unchanged
    - After installation, upsmon.conf needs updating to MONITOR cyberpower-virtual instead of cyberpower
      (This is Phase 5 work, document for later)

    Error cases to document in file:
    - If /dev/shm/ups-virtual.dev doesn't exist → dummy-ups will fail to start upsd
      (Daemon writes it every 10 seconds; verify daemon is running)
    - If mode is wrong → NUT may not re-read updates
    - If device name conflicts with existing UPS → upsd parse error
  </action>
  <verify>
    <automated>grep -E "driver|port|mode" config/dummy-ups.conf | wc -l</automated>
  </verify>
  <done>config/dummy-ups.conf created with dummy-ups configuration block. Parameters documented. Ready for Phase 5 installation.</done>
</task>

<task type="auto">
  <name>Task 3: Document shutdown coordination in CONTEXT.md</name>
  <files>CONTEXT.md</files>
  <action>
    Add section to CONTEXT.md documenting Phase 3 shutdown coordination mechanism for future reference.

    Add after "## Хранение состояния" section (or at end of relevant section):
    ```markdown
    ## Shutdown Coordination (Phase 3)

    ### Virtual UPS Proxy Flow

    1. **Daemon monitors battery state** (Phase 1-2):
       - Polls real UPS every 10 sec
       - Calculates SoC from voltage, runtime from Peukert law

    2. **Daemon computes ups.status override** (Phase 3):
       - ONLINE (input.voltage ≈230V, mains present) → emit "OL"
       - BLACKOUT_REAL (input.voltage ≈0V, no mains) + time_rem < 5min threshold → emit "OB DISCHRG LB"
       - BLACKOUT_TEST (battery test with mains present) → emit "OB DISCHRG" (no LB, calibration data OK)

    3. **Daemon writes virtual UPS file** (Phase 3):
       - Write to /dev/shm/ups-virtual.dev (tmpfs, zero SSD wear)
       - Format: NUT standard key-value (VAR lines)
       - Atomic write: tempfile → fsync → rename
       - Every poll cycle (10 sec)

    4. **NUT dummy-ups reads virtual device**:
       - dummy-ups driver loads /dev/shm/ups-virtual.dev (reads every timestamp change due to mode=dummy-once)
       - Provides metrics to upsmon and Grafana

    5. **upsmon receives LB signal**:
       - Monitors ups.status field
       - When "LB" flag present, triggers LOWBATT notify event
       - Executes SHUTDOWNCMD for graceful shutdown
       - Shutdown does not happen before time_rem actually expires (safety margin: LB fires at < threshold)

    ### Shutdown Threshold Configuration

    Environment variable: `UPS_MONITOR_SHUTDOWN_THRESHOLD_MIN` (default: 5 minutes)
    - Set to minutes before battery depletion when LB flag should be raised
    - Must be long enough for shutdown to complete (FINALDELAY + script execution time)
    - Shorter threshold = more runtime available, but riskier if shutdown takes longer than expected
    - Recommended: 5 minutes (allows ~3-4 min shutdown + 1 min safety margin)
    - Calibration mode (Phase 6): reduce to 1 minute to collect data to battery cutoff

    ### Event Classification

    - **ONLINE**: UPS on mains (input.voltage ~230V, status="OL")
    - **BLACKOUT_REAL**: UPS on battery, no mains (input.voltage ~0V, status="OB DISCHRG")
    - **BLACKOUT_TEST**: Battery test with mains present (input.voltage ~230V, status="OB DISCHRG")

    Distinction prevents false shutdown triggers during intentional battery tests.
    ```

    This adds context for future developers/operators about the shutdown mechanism.
  </action>
  <verify>
    <automated>grep -c "Shutdown Coordination" CONTEXT.md</automated>
  </verify>
  <done>CONTEXT.md updated with Phase 3 shutdown coordination documentation. Future reference clear.</done>
</task>

</tasks>

<verification>
After Wave 3 completion:
1. systemd service has After=sysinit.target dependency (ensures /dev/shm ready)
2. NUT dummy-ups configuration documented and ready for Phase 5 installation
3. Shutdown coordination mechanism fully documented
4. Service unit is syntactically valid (systemctl --dry-run syntax-check passes)
5. Ready for manual verification with live NUT daemon (Phase 5)
</verification>

<success_criteria>
- systemd/ups-battery-monitor.service updated with After=sysinit.target
- config/dummy-ups.conf created with complete NUT configuration block
- CONTEXT.md extended with Phase 3 shutdown coordination documentation
- Service unit is valid (grep finds After=sysinit pattern)
- Configuration block contains all required dummy-ups parameters
- Documentation explains event classification and threshold behavior
</success_criteria>

<output>
After completion, create `.planning/phases/03-virtual-ups-safe-shutdown/03-04-SUMMARY.md` with:
- systemd service update summary
- NUT configuration snippet location and content
- Documentation additions
- Ready for Phase 5: Installation and live integration testing with NUT
</output>
