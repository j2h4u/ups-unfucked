---
phase: 03-virtual-ups-safe-shutdown
plan: 01
type: execute
wave: 0
depends_on: []
files_modified: [tests/test_virtual_ups.py, src/virtual_ups.py]
autonomous: true
requirements: [VUPS-01, VUPS-02]
must_haves:
  truths:
    - "Virtual UPS metrics can be written atomically to tmpfs without SSD wear"
    - "All UPS fields transparently pass through except 3 overrides"
    - "File is written in NUT-compatible format (VAR lines)"
  artifacts:
    - path: "src/virtual_ups.py"
      provides: "write_virtual_ups_dev() function for tmpfs output"
      exports: ["write_virtual_ups_dev", "compute_ups_status_override"]
    - path: "tests/test_virtual_ups.py"
      provides: "Unit tests for tmpfs writing and field handling"
      min_lines: 150
  key_links:
    - from: "monitor.py"
      to: "virtual_ups.py"
      via: "import write_virtual_ups_dev, call in polling loop"
      pattern: "write_virtual_ups_dev\\(metrics\\)"
    - from: "virtual_ups.py"
      to: "/dev/shm/ups-virtual.dev"
      via: "atomic tempfile → fsync → os.replace()"
      pattern: "os\\.replace\\(tmp_path, virtual_ups_path\\)"
---

<objective>
Create foundational infrastructure for Phase 3: atomic virtual UPS file writing to tmpfs and test stubs for all Phase 3 behaviors.

Purpose: Establish the tmpfs writing pattern that enables transparent data source switching for NUT without SSD wear. Tests ensure format compliance and fail fast on critical behaviors.

Output: src/virtual_ups.py with write_virtual_ups_dev() and compute_ups_status_override() functions; comprehensive test file with stubs for all 7 requirements.
</objective>

<execution_context>
@/home/j2h4u/.claude/get-shit-done/workflows/execute-plan.md
@/home/j2h4u/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-virtual-ups-safe-shutdown/03-RESEARCH.md
@.planning/phases/03-virtual-ups-safe-shutdown/03-VALIDATION.md

# Phase 2 implementations already complete
@.planning/phases/02-battery-model-state-estimation-event-classification/02-06-SUMMARY.md

# Existing modules for reference
@src/model.py (atomic write pattern)
@src/event_classifier.py (EventType enum)
@tests/conftest.py (pytest fixtures)
</context>

<tasks>

<task type="auto">
  <name>Task 0: Create test stubs for Phase 3 requirements</name>
  <files>tests/test_virtual_ups.py</files>
  <action>
    Create pytest test file with stubbed test functions for all 7 Phase 3 requirements (VUPS-01 through SHUT-03).

    Test structure (copy from VALIDATION.md):
    - test_write_to_tmpfs (VUPS-01): Metrics written to /dev/shm/ups-virtual.dev
    - test_passthrough_fields (VUPS-02): All real UPS fields pass through except 3 overrides
    - test_field_overrides (VUPS-03): battery.runtime, battery.charge, ups.status correctly overridden
    - test_nut_format_compliance (VUPS-04): File written in NUT format (VAR lines), readable by dummy-ups
    - test_lb_flag_threshold (SHUT-01): ups.status includes "LB" when time_rem < threshold
    - test_configurable_threshold (SHUT-02): Shutdown threshold configurable via environment variable
    - test_calibration_mode_threshold (SHUT-03): Calibration mode threshold can be overridden

    Each test: pass statements only (stubs). No implementation yet.
    Import statements: pytest, tempfile, Path, unittest.mock.
    Use conftest.py existing fixtures (mock_socket from Phase 1).

    File structure:
    - Docstrings explaining what each test validates
    - Parametrize for threshold variations if applicable
    - Comment placeholders for assertion logic (filled in Wave 1-2)
  </action>
  <verify>
    <automated>pytest tests/test_virtual_ups.py --collect-only -q 2>&1 | grep "test_" | wc -l</automated>
  </verify>
  <done>tests/test_virtual_ups.py exists with 7 test stubs, all collect successfully</done>
</task>

<task type="auto">
  <name>Task 1: Implement virtual_ups.py with write_virtual_ups_dev() function</name>
  <files>src/virtual_ups.py</files>
  <action>
    Create new module src/virtual_ups.py with atomic tmpfs writing capability.

    Implement:
    1. write_virtual_ups_dev(metrics: dict, ups_name: str = "cyberpower") → None
       - Accepts dict of {field_name: value} to write
       - Builds NUT format content: "VAR {ups_name} {key} {value}\n" per line
       - Atomic write pattern (from Phase 1 model.py):
         * Create tempfile in /dev/shm (same tmpfs mount)
         * Write content
         * fsync to ensure durability
         * os.replace() to atomically rename
       - Handles Path creation: Path('/dev/shm/ups-virtual.dev').parent.mkdir(parents=True, exist_ok=True)
       - Logs error and re-raises exception on write failure (do NOT swallow)
       - Type hints: metrics: dict[str, Any], ups_name: str

    2. compute_ups_status_override(event_type: EventType, time_rem_minutes: float, shutdown_threshold_minutes: int) → str
       - STUB ONLY for Wave 1 (returns "OL" placeholder)
       - Logic from RESEARCH.md Pattern 2:
         * ONLINE → "OL"
         * BLACKOUT_TEST → "OB DISCHRG"
         * BLACKOUT_REAL + time_rem >= threshold → "OB DISCHRG"
         * BLACKOUT_REAL + time_rem < threshold → "OB DISCHRG LB"
       - Implementation in Wave 1/2, not now

    Imports:
    - import os
    - import tempfile
    - from pathlib import Path
    - from typing import Any, Dict
    - from src.event_classifier import EventType (for function stub signature)
    - import logging

    Logging:
    - Log successful write: "Virtual UPS metrics written at {timestamp}"
    - Log errors: "Failed to write virtual UPS metrics: {e}"

    No main() or daemon integration yet (that's Wave 3).
  </action>
  <verify>
    <automated>python -c "from src.virtual_ups import write_virtual_ups_dev, compute_ups_status_override; print('✓ Module loads')"</automated>
  </verify>
  <done>src/virtual_ups.py exists with write_virtual_ups_dev() and compute_ups_status_override() function stubs. Functions have correct signatures and docstrings. Module imports successfully.</done>
</task>

<task type="auto">
  <name>Task 2: Implement test_write_to_tmpfs and test_nut_format_compliance</name>
  <files>tests/test_virtual_ups.py</files>
  <action>
    Implement test functions for VUPS-01 (write to tmpfs) and VUPS-04 (NUT format compliance).

    test_write_to_tmpfs:
    - Arrange: Create dict of test metrics {battery.voltage: "12.5", ups.load: "20", ...}
    - Act: Call write_virtual_ups_dev(metrics)
    - Assert:
      * File exists at /dev/shm/ups-virtual.dev
      * File content matches NUT format (line per VAR: "VAR cyberpower <key> <value>")
      * All metrics present in file
    - Use tempfile.TemporaryDirectory to mock /dev/shm if needed for test isolation (or monkeypatch Path)
    - Verify atomic write doesn't leave partial files

    test_nut_format_compliance:
    - Arrange: Create metrics dict with various types (int, str, float for voltage)
    - Act: Call write_virtual_ups_dev()
    - Assert:
      * Each line matches regex: "^VAR cyberpower [a-z.]+[0-9]+ [a-zA-Z0-9.]+$"
      * Specific fields present: battery.voltage, battery.charge, ups.status, ups.load
      * Lines are sorted or in expected order
      * File mtime changed (timestamp update detected)

    Use pytest fixtures from conftest.py (mock_socket) for UPS data if needed.

    Tips:
    - Use tmpfile + os.replace pattern to verify atomicity
    - Read file back and parse to ensure format correctness
    - Test with edge cases: special characters (none in our format), very large values
  </action>
  <verify>
    <automated>pytest tests/test_virtual_ups.py::test_write_to_tmpfs tests/test_virtual_ups.py::test_nut_format_compliance -v</automated>
  </verify>
  <done>Both tests pass. File is written atomically to tmpfs. Format is NUT-compliant (VAR lines). Content is complete and readable.</done>
</task>

</tasks>

<verification>
After Wave 0 completion:
1. All 7 test stubs collect successfully
2. write_virtual_ups_dev() writes to tmpfs atomically (test_write_to_tmpfs passes)
3. Output format is NUT-compliant (test_nut_format_compliance passes)
4. compute_ups_status_override() function exists with correct signature (stub)
5. No errors on module import: `from src.virtual_ups import *`
</verification>

<success_criteria>
- src/virtual_ups.py created with both functions (one fully implemented, one stub)
- tests/test_virtual_ups.py created with all 7 test stubs + 2 implemented tests
- All Phase 1-2 tests still pass: `pytest tests/ -v` shows no regressions
- Tests pass: test_write_to_tmpfs, test_nut_format_compliance
- Tests pending (stubs): test_passthrough_fields, test_field_overrides, test_lb_flag_threshold, test_configurable_threshold, test_calibration_mode_threshold
</success_criteria>

<output>
After completion, create `.planning/phases/03-virtual-ups-safe-shutdown/03-01-SUMMARY.md` with:
- Task completion summary (which tests pass, which stubs remain)
- write_virtual_ups_dev() implementation status
- Next wave dependencies: Plan 02 needs compute_ups_status_override() implemented
- Any issues discovered (e.g., tmpfs permissions, file format edge cases)
</output>
