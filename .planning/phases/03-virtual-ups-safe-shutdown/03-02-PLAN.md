---
phase: 03-virtual-ups-safe-shutdown
plan: 02
type: execute
wave: 1
depends_on: [03-01]
files_modified: [src/virtual_ups.py, tests/test_virtual_ups.py]
autonomous: true
requirements: [VUPS-03, VUPS-04, SHUT-01, SHUT-02, SHUT-03]
must_haves:
  truths:
    - "compute_ups_status_override() correctly emits LB flag when time_rem < threshold"
    - "Shutdown threshold is configurable via environment variable UPS_MONITOR_SHUTDOWN_THRESHOLD_MIN"
    - "Calibration mode can override threshold to ~1 minute"
    - "Field overrides (battery.runtime, battery.charge, ups.status) tested in isolation"
  artifacts:
    - path: "src/virtual_ups.py"
      provides: "compute_ups_status_override() fully implemented, handles all EventTypes"
      exports: ["compute_ups_status_override"]
    - path: "tests/test_virtual_ups.py"
      provides: "Tests for field overrides and status arbiter logic"
      contains: "test_field_overrides, test_lb_flag_threshold, test_configurable_threshold, test_calibration_mode_threshold"
  key_links:
    - from: "compute_ups_status_override()"
      to: "EventType enum"
      via: "match against EventType.ONLINE, BLACKOUT_REAL, BLACKOUT_TEST"
      pattern: "if event_type == EventType"
    - from: "status override logic"
      to: "shutdown_threshold_minutes parameter"
      via: "compare time_rem_minutes < threshold"
      pattern: "time_rem_minutes < shutdown_threshold_minutes"
---

<objective>
Implement field override logic and shutdown threshold arbitration. Tests verify that the daemon can correctly determine when to signal LOW_BATTERY based on remaining runtime and configurable thresholds.

Purpose: Enable safe shutdown coordination by having the daemon compute correct ups.status values that upsmon will respond to, with configurable safety margins.

Output: compute_ups_status_override() fully implemented; tests for all override behaviors pass; threshold configuration validated.
</objective>

<execution_context>
@/home/j2h4u/.claude/get-shit-done/workflows/execute-plan.md
@/home/j2h4u/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/03-virtual-ups-safe-shutdown/03-RESEARCH.md
@src/event_classifier.py (EventType enum)
@src/monitor.py (SHUTDOWN_THRESHOLD_MINUTES configuration)

# Wave 0 completion
@.planning/phases/03-virtual-ups-safe-shutdown/03-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement compute_ups_status_override() with all EventType cases</name>
  <files>src/virtual_ups.py</files>
  <action>
    Replace stub with full implementation of compute_ups_status_override().

    Logic (from RESEARCH.md Pattern 2):
    - EventType.ONLINE: return "OL"
    - EventType.BLACKOUT_TEST: return "OB DISCHRG" (no LB flag, allow calibration data collection)
    - EventType.BLACKOUT_REAL with time_rem_minutes >= shutdown_threshold_minutes: return "OB DISCHRG"
    - EventType.BLACKOUT_REAL with time_rem_minutes < shutdown_threshold_minutes: return "OB DISCHRG LB" (signal LOW_BATTERY)
    - Unknown/else: return "OL" (safe default)

    Type signature:
    ```python
    def compute_ups_status_override(
        event_type: EventType,
        time_rem_minutes: float,
        shutdown_threshold_minutes: int
    ) -> str:
    ```

    Implementation notes:
    - No logging here (logging in daemon when status changes)
    - All branches return valid NUT status strings
    - Docstring explains LB flag semantics: "LB flag signals to upsmon that LOW_BATTERY condition is detected; upsmon will execute SHUTDOWNCMD"
    - Consider edge cases: negative time_rem (shouldn't happen but handle gracefully), time_rem exactly equals threshold (use < not <=)
    - Test vector: time_rem=5.0, threshold=5 → "OB DISCHRG" (not LB yet), time_rem=4.9 → "OB DISCHRG LB"
  </action>
  <verify>
    <automated>python -c "from src.virtual_ups import compute_ups_status_override; from src.event_classifier import EventType; assert compute_ups_status_override(EventType.ONLINE, 10, 5) == 'OL'; assert compute_ups_status_override(EventType.BLACKOUT_REAL, 4, 5) == 'OB DISCHRG LB'; print('✓ Function logic correct')"</automated>
  </verify>
  <done>compute_ups_status_override() implemented. All 4 event type branches tested. Returns correct NUT status strings per event type and time_rem threshold.</done>
</task>

<task type="auto">
  <name>Task 2: Implement test_field_overrides and test_passthrough_fields</name>
  <files>tests/test_virtual_ups.py</files>
  <action>
    Implement two tests for field handling (VUPS-02 and VUPS-03 coverage).

    test_field_overrides:
    - Arrange: Create dict with 3 override fields (battery.runtime, battery.charge, ups.status) + 5 passthrough fields
    - Act: Call write_virtual_ups_dev(metrics) where metrics dict has override values
    - Assert:
      * Three fields in output file are exactly as provided (not from real UPS)
      * Values: battery.runtime as "600" (integer seconds), battery.charge as "87" (integer percent), ups.status as "OB DISCHRG LB"
      * Verify no modification or transformation happened to these fields
    - Use temporary directory to isolate test file writes
    - Parse output file and extract VAR lines for each field

    test_passthrough_fields:
    - Arrange: Create realistic metrics dict (voltages, load, input.voltage, etc.)
    - Act: Call write_virtual_ups_dev(metrics) with mixed override + passthrough fields
    - Assert:
      * All passthrough fields (battery.voltage, ups.load, input.voltage, device.mfr, device.model, etc.) appear in output
      * Passthrough values are unchanged from input dict
      * Only 3 fields (battery.runtime, battery.charge, ups.status) would be overridden if present
      * Verify count: if 10 metrics provided, 10 VAR lines in output
    - Test with realistic UPS data (from conftest.py fixtures or hardcoded)

    Implementation tips:
    - Mock /dev/shm for test isolation using monkeypatch.setenv('TMPDIR', tmpdir) or patch Path
    - Parse output file line by line: split "VAR cyberpower <key> <value>"
    - Use dict comprehension to extract values: {key: value for line in content.split('\n') if 'VAR' in line}
  </action>
  <verify>
    <automated>pytest tests/test_virtual_ups.py::test_field_overrides tests/test_virtual_ups.py::test_passthrough_fields -v</automated>
  </verify>
  <done>Both tests pass. Override fields are written as provided. Passthrough fields are unchanged. All metrics appear in output file.</done>
</task>

<task type="auto">
  <name>Task 3: Implement test_lb_flag_threshold and test_configurable_threshold</name>
  <files>tests/test_virtual_ups.py</files>
  <action>
    Implement threshold-related tests (SHUT-01 and SHUT-02 coverage).

    test_lb_flag_threshold:
    - Arrange:
      * Default threshold = 5 minutes (from monitor.py SHUTDOWN_THRESHOLD_MINUTES)
      * Create test cases: time_rem values [6, 5, 4.9, 0] with default threshold
    - Act: Call compute_ups_status_override(EventType.BLACKOUT_REAL, time_rem, 5) for each case
    - Assert:
      * time_rem=6 → "OB DISCHRG" (no LB)
      * time_rem=5 → "OB DISCHRG" (no LB, threshold is <, not <=)
      * time_rem=4.9 → "OB DISCHRG LB" (LB flag set)
      * time_rem=0 → "OB DISCHRG LB" (LB flag set)
    - Parametrize test with multiple threshold values to verify threshold behavior

    test_configurable_threshold:
    - Arrange: Multiple threshold values (1, 3, 5, 10 minutes)
    - Act: For each threshold, call compute_ups_status_override(EventType.BLACKOUT_REAL, time_rem=threshold-0.1, threshold)
    - Assert: All cases return "OB DISCHRG LB" (LB fires when time_rem < threshold, regardless of threshold value)
    - Verify that threshold parameter actually controls LB firing (not hardcoded)
    - Test edge case: threshold=0 → time_rem=0 (negative) should fire LB or not?
      * Decision: time_rem < 0 is unphysical, but LB should still fire (any negative time = shutdown now)

    Implementation tips:
    - Use pytest.mark.parametrize for multiple threshold values
    - Test both sides of threshold boundary: time_rem = threshold + epsilon and threshold - epsilon
    - Use monkeypatch or environment variable if later integrating with monitor.py (here just function call)
  </action>
  <verify>
    <automated>pytest tests/test_virtual_ups.py::test_lb_flag_threshold tests/test_virtual_ups.py::test_configurable_threshold -v</automated>
  </verify>
  <done>Both tests pass. LB flag fires correctly at threshold boundary. Threshold parameter is configurable and respected by compute_ups_status_override().</done>
</task>

<task type="auto">
  <name>Task 4: Implement test_calibration_mode_threshold</name>
  <files>tests/test_virtual_ups.py</files>
  <action>
    Implement test for calibration mode (SHUT-03), which reduces threshold to ~1 minute.

    test_calibration_mode_threshold:
    - Arrange:
      * Normal threshold = 5 minutes
      * Calibration threshold = 1 minute (or 0 minute for deepest calibration)
      * Test cases: time_rem values [2, 1, 0.9]
    - Act:
      * Call compute_ups_status_override(EventType.BLACKOUT_REAL, time_rem, normal_threshold=5)
      * Call compute_ups_status_override(EventType.BLACKOUT_REAL, time_rem, calibration_threshold=1)
    - Assert:
      * With normal threshold: time_rem=2 → "OB DISCHRG" (no LB)
      * With calibration threshold: time_rem=2 → "OB DISCHRG" (no LB, since 2 >= 1)
      * With calibration threshold: time_rem=0.9 → "OB DISCHRG LB" (LB fires)
      * Verify that lower threshold allows system to run longer before shutdown (collecting more data)
    - Note: Actual calibration mode flag will be in Phase 6; here we test the threshold override capability

    Implementation tips:
    - Parametrize with [1, 0] thresholds to test both "~1 minute" and "0 minute" scenarios
    - Document that Phase 6 will add the --calibration-mode flag
    - Threshold reduction is the only Phase 3 aspect of calibration; Phase 6 adds the flag and data collection
  </action>
  <verify>
    <automated>pytest tests/test_virtual_ups.py::test_calibration_mode_threshold -v</automated>
  </verify>
  <done>Test passes. Calibration threshold can be set lower than normal shutdown threshold. LB flag fires correctly at reduced threshold.</done>
</task>

</tasks>

<verification>
After Wave 1 completion:
1. compute_ups_status_override() fully implemented with all branches tested
2. Tests for field overrides pass (fields correctly overridden and passed through)
3. Tests for threshold logic pass (LB flag fires at correct boundaries)
4. Threshold parameter is configurable and respected
5. All Phase 1-2 tests still pass (no regressions)
6. Ready for Wave 2: integration into monitor.py polling loop
</verification>

<success_criteria>
- compute_ups_status_override() implemented with all 4 EventType cases
- All 5 new tests pass: test_field_overrides, test_passthrough_fields, test_lb_flag_threshold, test_configurable_threshold, test_calibration_mode_threshold
- LB flag boundary correctly tested (time_rem < threshold, not <=)
- Threshold parameter actually used in decisions (not hardcoded)
- Calibration threshold scenario tested
- Full test suite (`pytest tests/ -v`) shows no regressions
</success_criteria>

<output>
After completion, create `.planning/phases/03-virtual-ups-safe-shutdown/03-02-SUMMARY.md` with:
- All 5 tests pass summary
- compute_ups_status_override() implementation complete
- Threshold boundary behavior verified
- Ready for Wave 2: monitor.py integration task
</output>
