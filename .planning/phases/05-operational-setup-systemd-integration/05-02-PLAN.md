---
phase: 05-operational-setup-systemd-integration
plan: 02
type: execute
wave: 1
depends_on: [05-01]
files_modified:
  - systemd/ups-battery-monitor.service
autonomous: true
requirements: [OPS-01, OPS-03, OPS-04]
must_haves:
  truths:
    - "Systemd service file enables daemon to auto-start on boot (WantedBy=multi-user.target)"
    - "Daemon automatically restarts on crash (Restart=on-failure RestartSec=10)"
    - "Restart loop is throttled (StartLimitBurst=3 StartLimitIntervalSec=60)"
    - "Daemon runs as unprivileged user j2h4u (User=j2h4u Group=j2h4u)"
    - "Virtual UPS file at /dev/shm is writable by daemon (tmpfs location, world-readable)"
    - "NUT socket at /run/nut is readable by daemon user (no privilege escalation needed)"
    - "Daemon logs to journald with SyslogIdentifier=ups-battery-monitor (OPS-04)"
    - "Service file is installed to /etc/systemd/system and validated by systemd"
  artifacts:
    - path: systemd/ups-battery-monitor.service
      provides: "Systemd service unit with proper dependencies, security settings, logging"
      contains: "Type=simple Restart=on-failure User=j2h4u StandardOutput=journal SyslogIdentifier=ups-battery-monitor"
  key_links:
    - from: systemd/ups-battery-monitor.service
      to: /run/nut/
      via: "ConditionPathExists=/run/nut/ soft dependency"
      pattern: "ConditionPathExists=/run/nut"
    - from: systemd/ups-battery-monitor.service
      to: /dev/shm
      via: "After=sysinit.target ensures tmpfs available before daemon starts"
      pattern: "After=sysinit.target"
    - from: systemd/ups-battery-monitor.service
      to: journald
      via: "StandardOutput=journal StandardError=journal routing all output to journald"
      pattern: "StandardOutput=journal"

---

<objective>
Validate systemd service configuration meets Phase 5 requirements for auto-start, privilege separation, and logging integration.

Purpose: Verify service file is correctly formatted, has proper dependencies and restart policies, and is ready for production deployment.

Output:
- Verified `systemd/ups-battery-monitor.service` with all OPS requirements satisfied
- Service file installable to `/etc/systemd/system` without syntax errors
- Service loads correctly, daemon restarts on crash, logging works
</objective>

<execution_context>
@/home/j2h4u/.claude/get-shit-done/workflows/execute-plan.md
@/home/j2h4u/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-operational-setup-systemd-integration/05-RESEARCH.md

# Wave 0 outputs (install.sh created)
@.planning/phases/05-operational-setup-systemd-integration/05-01-SUMMARY.md

# Service file to validate
@systemd/ups-battery-monitor.service

# Test framework (pytest infrastructure from Phase 1-4)
@tests/conftest.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify systemd service file syntax and documentation coverage</name>
  <files>systemd/ups-battery-monitor.service</files>
  <action>
Validate the existing `/home/j2h4u/repos/j2h4u/ups-battery-monitor/systemd/ups-battery-monitor.service` file against systemd requirements and Phase 5 specifications. No changes to file content; focus on verification that all OPS requirements are met.

**Verification checklist (from RESEARCH.md Pattern 1 and official systemd.service(5)):**

1. **[Unit] section completeness:**
   - [x] `Description=` present and descriptive (e.g., "UPS Battery Monitor")
   - [x] `Documentation=` present (GitHub URL acceptable)
   - [x] `After=sysinit.target` present (ensures /dev/shm available before daemon)
   - [x] `After=network-online.target` present (network readiness for NUT socket)
   - [x] `After=nut-server.service` present (NUT daemon must start first)
   - [x] `Wants=network-online.target` present (soft network dependency)
   - [x] `ConditionPathExists=/run/nut/` present (soft check for NUT availability)

2. **[Service] section completeness:**
   - [x] `Type=simple` (daemon doesn't fork; systemd tracks PID)
   - [x] `User=j2h4u` (unprivileged user, not root)
   - [x] `Group=j2h4u` (unprivileged group)
   - [x] `WorkingDirectory=/home/j2h4u/repos/j2h4u/ups-battery-monitor` (repo root for PYTHONPATH)
   - [x] `Environment="PYTHONPATH=/home/j2h4u/repos/j2h4u/ups-battery-monitor"` (module discovery)
   - [x] `ExecStart=/usr/bin/python3 -m src.monitor` (entry point matches src/monitor.py)
   - [x] `Restart=on-failure` (auto-restart on non-zero exit; respects exit 0)
   - [x] `RestartSec=10` (10-second delay between restarts)
   - [x] `StartLimitBurst=3` (max 3 restarts)
   - [x] `StartLimitIntervalSec=60` (within 60-second window; prevents restart loops)
   - [x] `TimeoutStartSec=30` (30-second startup timeout)
   - [x] `StandardOutput=journal` (log stdout to journald)
   - [x] `StandardError=journal` (log stderr to journald)
   - [x] `SyslogIdentifier=ups-battery-monitor` (tagged in journald for searchability)

3. **[Install] section completeness:**
   - [x] `WantedBy=multi-user.target` (enables auto-start on boot; systemctl enable will install symlink)

**Verification steps:**
1. Check file exists and is readable: `test -r systemd/ups-battery-monitor.service`
2. Verify all required fields present (grep for key directives above)
3. Check for syntax errors: `systemd-analyze verify systemd/ups-battery-monitor.service` (if available, otherwise skip)
4. Ensure ExecStart path is absolute: `/usr/bin/python3` (not relative, not `python3`)
5. Verify User/Group match daemon's intended user: `j2h4u`
6. Confirm WorkingDirectory exists: `/home/j2h4u/repos/j2h4u/ups-battery-monitor`

If any field missing or incorrect:
- Log which field is missing/incorrect
- Suggest correction from RESEARCH.md Pattern 1 (lines 496-526)
- Do NOT modify file (Wave 1 focused on verification; modifications deferred if needed)

**Reference:** Existing service file (read above). Verify it matches RESEARCH.md example structure exactly.

**Manual test (optional for Wave 1):**
- Syntax validation: `systemd-analyze verify /etc/systemd/system/ups-battery-monitor.service` (after install)
  </action>
  <verify>
    <automated>
test -r /home/j2h4u/repos/j2h4u/ups-battery-monitor/systemd/ups-battery-monitor.service && grep -q "Type=simple" /home/j2h4u/repos/j2h4u/ups-battery-monitor/systemd/ups-battery-monitor.service && grep -q "User=j2h4u" /home/j2h4u/repos/j2h4u/ups-battery-monitor/systemd/ups-battery-monitor.service && echo "✓ Service file verified"
    </automated>
  </verify>
  <done>
- Service file exists and is readable
- All required [Unit], [Service], [Install] sections present
- Key directives verified: Type=simple, User=j2h4u, Restart=on-failure, StandardOutput=journal, SyslogIdentifier
- Service file conforms to systemd.service(5) and Phase 5 RESEARCH requirements
- Documentation: verified against official systemd patterns
  </done>
</task>

<task type="auto">
  <name>Task 2: Add systemd service verification tests to test suite</name>
  <files>tests/test_systemd_integration.py</files>
  <action>
Create `tests/test_systemd_integration.py` with unit tests that verify systemd service configuration requirements (OPS-01, OPS-03, OPS-04). These tests do NOT require running systemctl or root; they parse and validate the service file directly.

1. **Imports:**
   - `import re`, `import pathlib`
   - `from configparser import ConfigParser` (systemd .service files are INI-like)
   - pytest fixtures from conftest.py

2. **Test: `test_service_file_exists_and_readable`**
   - Path: `/home/j2h4u/repos/j2h4u/ups-battery-monitor/systemd/ups-battery-monitor.service`
   - Assert: file exists and is readable via pathlib.Path().is_file()
   - Assert: file size > 500 bytes (not empty/stub)

3. **Test: `test_service_file_unit_section_required_fields`**
   - Parse service file (ConfigParser or manual INI parsing)
   - Verify [Unit] section exists
   - Check for required fields: Description, After, ConditionPathExists
   - Assert: Description contains "UPS" or "Battery"
   - Assert: "sysinit.target" in After (ensures /dev/shm available)
   - Assert: "nut-server.service" in After (NUT dependency)
   - Assert: "/run/nut/" in ConditionPathExists (soft NUT check)

4. **Test: `test_service_file_service_section_restart_config`**
   - Verify [Service] section exists
   - Check restart directives: Restart=on-failure, RestartSec=10, StartLimitBurst=3
   - Assert: Restart value is "on-failure" (respects exit 0, auto-restarts on non-zero)
   - Assert: RestartSec is 10 (not too aggressive)
   - Assert: StartLimitBurst is 3 (prevents restart loops)

5. **Test: `test_service_file_unprivileged_execution`**
   - Check User=j2h4u (unprivileged user, not root/nobody)
   - Check Group=j2h4u (matching group)
   - Assert: User != "root" and User == "j2h4u"
   - Assert: Group == "j2h4u"

6. **Test: `test_service_file_logging_configuration`**
   - Check StandardOutput=journal (stdout to journald)
   - Check StandardError=journal (stderr to journald)
   - Check SyslogIdentifier=ups-battery-monitor (tagged for searchability)
   - Assert: all three present and correctly set

7. **Test: `test_service_file_install_section_boot_start`**
   - Verify [Install] section exists
   - Check WantedBy=multi-user.target (enables auto-start on boot)
   - Assert: multi-user.target in WantedBy (not emergency.target or custom target)

8. **Test: `test_exec_start_is_absolute_path`**
   - Check ExecStart= line
   - Verify path is absolute: starts with / (e.g., /usr/bin/python3)
   - Assert: does NOT use relative path (python3 without /) or ~
   - Assert: uses /usr/bin/python3 (standard location)

9. **Test: `test_working_directory_exists_or_documented`**
   - Check WorkingDirectory= path
   - Verify path is absolute
   - If path should exist (developer machine): optional assertion (may fail on CI)
   - Assert: path is documented and reasonable (repo root expected)

**Implementation notes:**
- Use pathlib.Path() for file operations (Pythonic, cross-platform)
- For ConfigParser: systemd files are not perfect INI (use configparser with allow_no_value=True or manual parsing with regex)
- Alternative: parse file line-by-line, extract Key=Value pairs, store in dict
- Keep tests simple and focused; avoid complex regex if ConfigParser works
- Min 40 lines, 9 test functions (one per requirement)

**Reference:** From RESEARCH.md, Pattern 1 (lines 496-526) shows example service file. Tests verify this configuration is in place.
  </action>
  <verify>
    <automated>
python3 -m pytest /home/j2h4u/repos/j2h4u/ups-battery-monitor/tests/test_systemd_integration.py -v --tb=short -q
    </automated>
  </verify>
  <done>
- `tests/test_systemd_integration.py` exists with 40+ lines
- 9 test functions covering [Unit], [Service], [Install] sections
- All tests pass: pytest exit code 0
- Service file configuration verified against Phase 5 OPS requirements
- No regressions in full test suite (still 115+ tests passing)
  </done>
</task>

</tasks>

<verification>
After Wave 1 completion, before Phase gate:

1. **Service file syntax valid (manual check):**
   ```bash
   # If systemd-analyze available:
   systemd-analyze verify /home/j2h4u/repos/j2h4u/ups-battery-monitor/systemd/ups-battery-monitor.service
   # Output: no errors
   ```

2. **Systemd integration tests passing:**
   ```bash
   cd /home/j2h4u/repos/j2h4u/ups-battery-monitor
   python3 -m pytest tests/test_systemd_integration.py -v
   # All 9 tests: PASSED
   ```

3. **Full suite passing (no regressions):**
   ```bash
   python3 -m pytest tests/ -q
   # ~120+ tests PASSED (115 from Phase 1-4 + 4 new logging + 9 new systemd)
   ```

4. **Install script is ready (from Wave 0):**
   ```bash
   test -x scripts/install.sh && echo "✓ install.sh executable"
   ```
</verification>

<success_criteria>
- [ ] Service file verified against all OPS requirements (OPS-01, OPS-03, OPS-04)
- [ ] `tests/test_systemd_integration.py` created with 9 test functions
- [ ] All systemd integration tests passing
- [ ] Full test suite passing (no regressions): ~120+ tests
- [ ] Service file ready for installation via install.sh
- [ ] Wave 0 + Wave 1 combined: install.sh + test_logging.py + test_systemd_integration.py all working
</success_criteria>

<output>
After Wave 1 completion, create `.planning/phases/05-operational-setup-systemd-integration/05-02-SUMMARY.md` documenting:
- Systemd service configuration verification (all directives present, correct values)
- New test coverage (9 systemd integration tests, 4 logging tests)
- Phase 5 Wave 0+1 completion status
- Next phase (Phase 6 calibration mode, if needed)
</output>
