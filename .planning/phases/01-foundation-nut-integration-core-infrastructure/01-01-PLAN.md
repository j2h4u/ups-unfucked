---
phase: 01-foundation-nut-integration-core-infrastructure
plan: 01
type: auto
wave: 0
depends_on: []
files_modified:
  - tests/conftest.py
  - tests/test_nut_client.py
  - tests/test_ema.py
  - tests/test_model.py
autonomous: true
requirements: [DATA-01, DATA-02, DATA-03, MODEL-01, MODEL-02, MODEL-04]
user_setup: []

must_haves:
  truths:
    - "Test fixtures exist and can mock NUT upsd responses"
    - "All test files are discoverable by pytest with no import errors"
    - "Test framework installed and runnable within 5 seconds"
    - "Socket timeout patterns verified with mock data"
    - "EMA math verified algebraically before implementation"
  artifacts:
    - path: "tests/conftest.py"
      provides: "Shared pytest fixtures (mock NUT responses, temp model paths)"
      exports: ["mock_upsd_response", "temporary_model_path"]
    - path: "tests/test_nut_client.py"
      provides: "Test stubs for DATA-01 (socket communication, timeout, reconnect)"
      exports: ["test_continuous_polling", "test_socket_timeout"]
    - path: "tests/test_ema.py"
      provides: "Test stubs for DATA-02 (EMA convergence, stabilization gate)"
      exports: ["test_ema_convergence", "test_stabilization_gate"]
    - path: "tests/test_model.py"
      provides: "Test stubs for DATA-03, MODEL-01, MODEL-02, MODEL-04"
      exports: ["test_ir_compensation", "test_model_load_save", "test_vrla_lut_init", "test_atomic_write"]
  key_links:
    - from: "All test files"
      to: "tests/conftest.py"
      via: "pytest fixture imports"
      pattern: "from conftest import"
    - from: "conftest.py"
      to: "Mock NUT protocol"
      via: "Standard TCP response format (text-based key-value)"
      pattern: "VAR cyberpower battery\\.voltage"

---

<objective>
Create test infrastructure and test stubs for Phase 1. Establish pytest fixtures for mocking NUT upsd responses, create test file scaffolds with failing tests, and verify test framework is runnable within fast feedback loops.

Purpose: Fast verification cycles (30-second feedback) during implementation. Enables red-green-refactor workflow per phase validation strategy.

Output: Four test files (conftest.py + three test modules) with passing fixtures, failing test stubs mapped to all Phase 1 requirements, and pytest installed.
</objective>

<execution_context>
@/home/j2h4u/.claude/get-shit-done/workflows/execute-plan.md
@/home/j2h4u/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-foundation-nut-integration-core-infrastructure/01-RESEARCH.md
@.planning/phases/01-foundation-nut-integration-core-infrastructure/01-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install pytest framework and create conftest.py with NUT mocking fixtures</name>
  <files>tests/conftest.py, tests/__init__.py</files>
  <action>
    1. Install test framework: `pip install pytest pytest-cov` (if not present)

    2. Create tests/__init__.py (empty, marks directory as Python package)

    3. Create tests/conftest.py with shared pytest fixtures:
       - `mock_upsd_response()`: Function that returns realistic NUT protocol responses
         Sample: "VAR cyberpower battery.voltage 13.4\n"
         Include responses for: battery.voltage, ups.load, ups.status, input.voltage
       - `temporary_model_path()`: pytest fixture that yields temp file path for model.json
         Use tempfile.NamedTemporaryFile with suffix='.json', delete on cleanup
       - `mock_socket_factory()`: Factory that creates mock socket objects with configurable responses
         Mock should support: `connect()`, `sendall()`, `recv()`, `close()`, `settimeout()`
         Include error scenarios: timeout, connection refused, malformed responses

       Do NOT implement NUT client or EMA yet. Fixtures are infrastructure only.

    Fixtures should enable tests like:
    ```python
    def test_socket_timeout(mock_socket_factory):
        sock = mock_socket_factory(scenario='timeout_after_2_sec')
        # Test code will use this mock
    ```
  </action>
  <verify>
    <automated>
      pytest tests/conftest.py -v 2>&1 | grep -q "collected 0 items" && echo "PASS: conftest.py loads without errors"
    </automated>
  </verify>
  <done>
    - tests/__init__.py created and empty
    - tests/conftest.py created with three fixtures (mock_upsd_response, temporary_model_path, mock_socket_factory)
    - `pytest tests/conftest.py -v` runs without import errors
    - pytest and pytest-cov installed (verify with `python -m pytest --version`)
  </done>
</task>

<task type="auto">
  <name>Task 2: Create test_nut_client.py stub with failing tests for DATA-01 (socket communication and timeout handling)</name>
  <files>tests/test_nut_client.py</files>
  <action>
    Create tests/test_nut_client.py with skeleton test functions (will fail until implementation exists):

    1. `test_continuous_polling(mock_socket_factory)`:
       - Test title: Daemon successfully reads from upsc; handles 100 consecutive polls without dropped samples
       - Arrange: Create mock_socket_factory configured for 100 successful responses
       - Act: Call hypothetical `NUTClient.get_ups_vars()` 100 times in a loop
       - Assert: All 100 responses received without exception, no data loss
       - Expected to FAIL at this point (NUTClient doesn't exist yet)

    2. `test_socket_timeout(mock_socket_factory)`:
       - Test title: Socket timeout prevents hanging on NUT upsd crash
       - Arrange: Create mock_socket_factory with 'timeout' scenario
       - Act: Call hypothetical `NUTClient.get_ups_vars()` with 2-sec timeout
       - Assert: Raises socket.timeout exception (not hang), recovers on retry
       - Expected to FAIL (NUTClient doesn't exist yet)

    3. `test_connection_refused(mock_socket_factory)`:
       - Test title: Daemon handles connection refused gracefully
       - Arrange: Create mock_socket_factory with 'connection_refused' scenario
       - Act: Call hypothetical `NUTClient.get_ups_vars()`
       - Assert: Catches socket.error, logs error, does not crash
       - Expected to FAIL (NUTClient doesn't exist yet)

    Each test imports from conftest (use fixtures from Task 1). Do NOT implement NUTClient yet.
    Do NOT make tests pass. Goal is to establish test file structure and see them fail consistently.
  </action>
  <verify>
    <automated>
      pytest tests/test_nut_client.py -v 2>&1 | grep -E "(test_continuous_polling|test_socket_timeout|test_connection_refused)" | grep -q "FAILED\|ERROR" && echo "PASS: Test stubs created and failing as expected"
    </automated>
  </verify>
  <done>
    - tests/test_nut_client.py created with 3 test functions
    - All tests fail (expected: NameError or AttributeError for missing NUTClient)
    - Tests import mock_socket_factory fixture successfully
    - Test names match requirements in 01-VALIDATION.md
  </done>
</task>

<task type="auto">
  <name>Task 3: Create test_ema.py stub with failing tests for DATA-02 (EMA convergence and stabilization)</name>
  <files>tests/test_ema.py</files>
  <action>
    Create tests/test_ema.py with skeleton test functions:

    1. `test_ema_convergence()`:
       - Test title: EMA reaches 90% convergence within 5 samples (α ≈ 0.08, window 120s at 10-sec interval)
       - Arrange: Create hypothetical `EMABuffer(window_sec=120, poll_interval_sec=10)`
       - Act: Add 5 constant values (all 13.0) to EMA buffer
       - Assert: After 5 samples, EMA.value > 12.85 (≈90% convergence to 13.0)
       - Expected to FAIL (EMABuffer doesn't exist yet)

    2. `test_stabilization_gate()`:
       - Test title: EMA stabilized flag is False for first 2 samples, True from sample 3 onward
       - Arrange: Create hypothetical `EMABuffer(...)`
       - Act: Add samples one by one, check .stabilized property after each
       - Assert:
         - stabilized == False after samples 1, 2
         - stabilized == True after samples 3, 4, 5
       - Expected to FAIL (EMABuffer doesn't exist yet)

    3. `test_ema_ring_buffer_memory()`:
       - Test title: Ring buffer holds at least 120+ seconds of data (120sec window / 10sec interval = 12+ samples, +10 headroom = 22+ slots)
       - Arrange: Create hypothetical `EMABuffer(window_sec=120, poll_interval_sec=10)`
       - Act: Add 22+ samples
       - Assert: Buffer size stays bounded (maxlen works), no unbounded growth
       - Expected to FAIL (EMABuffer doesn't exist yet)

    Do NOT implement EMABuffer yet. Tests should fail consistently.
  </action>
  <verify>
    <automated>
      pytest tests/test_ema.py -v 2>&1 | grep -E "(test_ema_convergence|test_stabilization_gate|test_ema_ring_buffer)" | grep -q "FAILED\|ERROR" && echo "PASS: EMA test stubs created and failing as expected"
    </automated>
  </verify>
  <done>
    - tests/test_ema.py created with 3 test functions
    - All tests fail (expected: NameError for missing EMABuffer)
    - Tests do NOT import from main source (no src/ema_ring_buffer.py yet)
    - Test names match requirements in 01-VALIDATION.md
  </done>
</task>

<task type="auto">
  <name>Task 4: Create test_model.py stub with failing tests for DATA-03, MODEL-01, MODEL-02, MODEL-04 (IR compensation, model persistence, VRLA LUT, atomic writes)</name>
  <files>tests/test_model.py</files>
  <action>
    Create tests/test_model.py with skeleton test functions:

    1. `test_ir_compensation()`:
       - Test title: IR compensation formula applies correctly
       - Arrange: V_ema = 12.0, L_ema = 50, L_base = 20, k = 0.01
       - Act: Call hypothetical `ir_compensate(V_ema=12.0, L_ema=50, L_base=20, k=0.01)`
       - Assert: Result = 12.0 + 0.01*(50-20) = 12.3
       - Expected to FAIL (function doesn't exist yet)

    2. `test_model_load_save(temporary_model_path)`:
       - Test title: model.json loads successfully from disk; malformed JSON triggers error
       - Arrange: Use temporary_model_path fixture, write valid JSON
       - Act: Call hypothetical `BatteryModel(temporary_model_path).load()`
       - Assert: Successfully loads, parses JSON structure
       - Then: Write malformed JSON, call load()
       - Assert: Raises json.JSONDecodeError or similar
       - Expected to FAIL (BatteryModel doesn't exist yet)

    3. `test_vrla_lut_init()`:
       - Test title: Standard VRLA LUT initializes with correct voltage→SoC mapping
       - Arrange: Create hypothetical `BatteryModel()` with no file (triggers default init)
       - Act: Check model.data['lut'] contains expected points
       - Assert:
         - Entry for 13.4V with SoC 1.0 exists
         - Entry for 10.5V with SoC 0.0 exists (anchor point)
         - All entries have 'source' field (standard/measured/anchor)
       - Expected to FAIL (BatteryModel doesn't exist yet)

    4. `test_atomic_write(temporary_model_path)`:
       - Test title: Atomic write (tempfile + fsync + os.replace) succeeds; no temp files left
       - Arrange: Create test data dict, use temporary_model_path
       - Act: Call hypothetical `atomic_write_json(temporary_model_path, data)`
       - Assert:
         - File written to temporary_model_path
         - No .tmp files left in directory (atomic rename completed)
         - Reread file, verify data matches
       - Expected to FAIL (atomic_write_json doesn't exist yet)

    Do NOT implement BatteryModel or ir_compensate yet. Tests fail as expected.
  </action>
  <verify>
    <automated>
      pytest tests/test_model.py -v 2>&1 | grep -E "(test_ir_compensation|test_model_load_save|test_vrla_lut|test_atomic_write)" | grep -q "FAILED\|ERROR" && echo "PASS: Model test stubs created and failing as expected"
    </automated>
  </verify>
  <done>
    - tests/test_model.py created with 4 test functions
    - All tests fail (expected: NameError for missing implementations)
    - test_model_load_save and test_atomic_write use temporary_model_path fixture
    - Test names match requirements in 01-VALIDATION.md
  </done>
</task>

<task type="auto">
  <name>Task 5: Verify test framework and all stubs are discoverable and runnable</name>
  <files></files>
  <action>
    Verify pytest can discover and run all stubs (they will fail, but should fail gracefully):

    1. Run quick unit tests: `pytest tests/test_nut_client.py tests/test_ema.py -v`
       Expected output: 6 tests collected, all FAILED or ERROR (no AttributeError during collection)

    2. Run full suite: `pytest tests/ -v`
       Expected output: 13 tests collected (3 + 3 + 4 + 3 from conftest mocks), all FAILED or ERROR

    3. Verify no import errors: `python -c "from tests.conftest import *"`
       Should succeed

    4. Check test file syntax: `python -m py_compile tests/test_*.py`
       Should complete without errors

    If any file has syntax error or import issue, fix immediately.

    Goal: Establish baseline where all test files exist, are syntactically valid, are discoverable by pytest, and fail predictably.
  </action>
  <verify>
    <automated>
      (pytest tests/ -v --co 2>&1 | grep -q "13 test" || pytest tests/ -v --co 2>&1 | grep -q "test session starts") && echo "PASS: Test discovery successful"
    </automated>
  </verify>
  <done>
    - All test files (conftest.py, test_nut_client.py, test_ema.py, test_model.py) exist and are syntactically valid
    - pytest can discover all 13+ tests without import errors
    - All tests fail as expected (missing implementations)
    - No .pyc artifacts or __pycache__ pollution in test directory
  </done>
</task>

</tasks>

<verification>
After Wave 0 completes:
- [ ] `pytest tests/ --co` lists 13+ tests without errors
- [ ] `pytest tests/test_nut_client.py tests/test_ema.py -v` runs in <5 sec
- [ ] All tests fail with clear error messages (NameError, AttributeError, ImportError, not syntax errors)
- [ ] `python -m pytest --version` confirms pytest 8.0+ installed
- [ ] No temp files left in tests/ directory after test run
</verification>

<success_criteria>
- Test framework (pytest 8.0+) installed and verified
- Test fixtures created (mock_upsd_response, temporary_model_path, mock_socket_factory)
- Test stubs created for all Phase 1 requirements (DATA-01, DATA-02, DATA-03, MODEL-01, MODEL-02, MODEL-04)
- All tests discoverable by pytest and failing as expected (not crashing during discovery)
- Fast feedback loop established (~30 sec for test discovery and run)
- Next wave (Wave 1) can start implementing without fixture changes
</success_criteria>

<output>
After execution, create `.planning/phases/01-foundation-nut-integration-core-infrastructure/01-01-SUMMARY.md` with:
- Test framework installed version (pytest --version)
- Test discovery output (pytest --co | head -20)
- List of fixtures created (mock_upsd_response, temporary_model_path, mock_socket_factory)
- Baseline test run (pytest tests/ -v | grep -E "FAILED|passed|ERROR")
- Next wave dependencies (Wave 1 can begin without conftest.py changes)
</output>
