---
phase: 15-foundation
plan: 04
type: execute
wave: 3
depends_on: [02, 03]
files_modified:
  - tests/test_sulfation_offline_harness.py
autonomous: true
requirements: [SULF-06]

must_haves:
  truths:
    - "User can run integration tests validating sulfation/ROI functions with year-simulation discharge curves"
    - "Synthetic discharge scenarios (healthy, degraded, high temp) produce expected sulfation scores"
    - "Offline harness proves both modules work with realistic (but synthetic) battery data"
  artifacts:
    - path: "tests/test_sulfation_offline_harness.py"
      provides: "Integration test harness using year_simulation.py discharge curves"
      test_cases: ["test_sulfation_with_year_simulation_healthy", "test_sulfation_with_year_simulation_degraded"]
  key_links:
    - from: "tests/test_sulfation_offline_harness.py"
      to: "tests/test_year_simulation.py"
      via: "import and reuse discharge curve fixtures"
      pattern: "from tests.test_year_simulation import"
---

<objective>
Create integration tests for sulfation and ROI modules using synthetic discharge curves from the existing year-simulation test harness. These tests validate that the pure functions work correctly with realistic battery state transitions, without requiring real UPS hardware or daemon integration.

Purpose: Bridge gap between unit tests (simple inputs) and system tests (daemon integration)
Output: Integration test harness with synthetic discharge scenarios
</objective>

<execution_context>
@/home/j2h4u/.claude/get-shit-done/workflows/execute-plan.md
@/home/j2h4u/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/15-foundation/15-RESEARCH.md (Example 1: test_sulfation_with_year_simulation)

# Year-simulation test harness (from tests/test_year_simulation.py)

Provides synthetic discharge curves for testing without real hardware. Integration tests can reuse these fixtures to validate sulfation scoring against realistic battery state transitions.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create test_sulfation_offline_harness.py integration tests</name>
  <files>tests/test_sulfation_offline_harness.py</files>
  <read_first>
    - tests/test_year_simulation.py (understand discharge curve fixtures, battery simulation patterns)
    - src/battery_math/sulfation.py (functions to test)
    - src/battery_math/cycle_roi.py (functions to test)
    - .planning/phases/15-foundation/15-RESEARCH.md (Example 1: offline harness test structure)
  </read_first>
  <action>
Create tests/test_sulfation_offline_harness.py with integration tests:

1. **Module docstring**: "Integration tests for sulfation.py using year-simulation discharge curves — synthetic but realistic battery scenarios"

2. **Test scenarios** (2 test methods):

   a. **test_sulfation_with_year_simulation_healthy_battery**:
      - Use year_simulation to simulate healthy battery (SoH near 1.0) with regular discharges
      - Simulate 30 days of operation with weekly deep discharges
      - Track days_since_deep, SoH before/after, IR trend across multiple discharges
      - After 25 days idle (no discharge), compute sulfation_score
      - Assert sulfation_score increases with idle time (days_since_deep effect dominant)
      - Assert sulfation_score < 0.5 for healthy battery

   b. **test_sulfation_with_year_simulation_degraded_battery**:
      - Use year_simulation with degraded battery (SoH < 0.7)
      - Simulate poor recovery (recovery_delta low) post-discharge
      - Simulate IR trend increase over time (dR/dt > 0)
      - Compute sulfation_score with high idle time, poor recovery, high IR trend
      - Assert sulfation_score > 0.6 (combination of signals → high score)

3. **Helper functions** (optional, for clarity):
   - _simulate_discharge_cycle(days_idle, soh_before, soh_after, ir_trend) → (days_since_deep, recovery_delta, ir_trend)
   - _compute_sulfation_for_scenario(...) → score

4. **Assertions**:
   - Sulfation score is in valid range [0.0, 1.0]
   - Healthy battery has lower score than degraded
   - Score increases with idle time (if other factors constant)
   - Score increases with poor recovery signal
   - Score increases with IR drift

5. **No mocking**: Use actual year_simulation discharge data or parametric synthetic discharges
  </action>
  <verify>
    <automated>python3 -m pytest tests/test_sulfation_offline_harness.py -v</automated>
  </verify>
  <acceptance_criteria>
    - File: tests/test_sulfation_offline_harness.py
    - Contains 2 integration test methods
    - Both tests pass (exit code 0)
    - Tests use synthetic discharge data (from year_simulation or parametric)
    - Tests validate sulfation score against realistic battery scenarios
    - No real hardware, no daemon, no I/O
    - Assertions confirm score behavior across healthy/degraded battery states
  </acceptance_criteria>
  <done>Integration test harness created with 2 scenario-based tests</done>
</task>

</tasks>

<verification>
After task complete:

```bash
# Run integration tests
python3 -m pytest tests/test_sulfation_offline_harness.py -v

# Verify all sulfation/ROI tests pass
python3 -m pytest tests/test_sulfation.py tests/test_cycle_roi.py tests/test_sulfation_offline_harness.py -v --tb=short

# Summary
echo "Unit tests: $(grep -c 'def test_' tests/test_sulfation.py) + $(grep -c 'def test_' tests/test_cycle_roi.py)"
echo "Integration tests: $(grep -c 'def test_' tests/test_sulfation_offline_harness.py)"
```
</verification>

<success_criteria>
Wave 3 complete when:
- [ ] tests/test_sulfation_offline_harness.py exists with 2 integration tests
- [ ] Both tests pass (healthy + degraded scenarios)
- [ ] Tests use synthetic discharge curves (realistic but offline)
- [ ] Combined with unit tests: 17 unit + 2 integration = 19 total new tests
- [ ] All 19 tests pass (exit code 0)
- [ ] Ready for Phase 16+ integration into daemon
</success_criteria>

<output>
After completion, create `.planning/phases/15-foundation/15-04-SUMMARY.md` documenting:
- Integration test scenarios (healthy vs degraded battery)
- How synthetic discharge curves validate sulfation scoring
- Test coverage summary: 8 unit (sulfation) + 6 unit (ROI) + 3 unit (INSTCMD) + 2 integration = 19 tests
</output>
