---
phase: 21-extract-dischargecollector
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/discharge_handler.py
  - tests/test_discharge_handler.py
autonomous: true
requirements:
  - ARCH-06

must_haves:
  truths:
    - "_score_and_persist_sulfation is decomposed into three independently callable methods"
    - "_compute_sulfation_metrics returns a data dict with all pre-computed values or None-scored dict on error"
    - "_persist_sulfation_and_discharge writes sulfation_history and discharge_event to model and grants blackout credit"
    - "_log_discharge_complete emits the structured journald event with all fields"
    - "update_battery_health orchestrates compute -> persist -> log in sequence"
  artifacts:
    - path: "src/discharge_handler.py"
      provides: "Three split methods replacing _score_and_persist_sulfation monolith"
      contains: "_compute_sulfation_metrics"
    - path: "tests/test_discharge_handler.py"
      provides: "Unit tests for each split method"
      contains: "test_compute_sulfation_metrics"
  key_links:
    - from: "src/discharge_handler.py"
      to: "update_battery_health"
      via: "_compute_sulfation_metrics -> _persist_sulfation_and_discharge -> _log_discharge_complete orchestration"
      pattern: "_compute_sulfation_metrics.*_persist_sulfation.*_log_discharge"
---

<objective>
Split `_score_and_persist_sulfation` in DischargeHandler into three independently testable methods.

Purpose: The current 95-line monolith mixes compute, persist, and log concerns. Splitting enables independent unit testing of each responsibility and cleaner maintenance.
Output: Three private methods in discharge_handler.py, new unit tests in test_discharge_handler.py.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-extract-dischargecollector/21-CONTEXT.md
@.planning/phases/21-extract-dischargecollector/21-RESEARCH.md

@src/discharge_handler.py
@tests/test_discharge_handler.py

<interfaces>
<!-- From src/discharge_handler.py — current method to split -->
def _score_and_persist_sulfation(
    self,
    soh_new: float,
    soh_delta: float,
    discharge_buffer: DischargeBuffer,
    discharge_trigger: str,
    capacity_ah_ref: Optional[float] = None,
) -> None:
    # Lines 230-325: compute sulfation_state + roi, set last_* state,
    # pre-compute rounded values, append_sulfation_history,
    # append_discharge_event, logger.info discharge_complete,
    # _grant_blackout_credit

def _grant_blackout_credit(self, discharge_trigger: str, depth_of_discharge: float) -> None:
    # Lines 327-347: already separate — called from persist method

<!-- Helper methods used by compute -->
def _calculate_days_since_deep(self) -> Optional[float]:   # line 570
def _estimate_ir_trend(self) -> float:                      # line 598
def _estimate_dod_from_buffer(self, discharge_buffer) -> float:  # line 673
def _estimate_cycle_budget(self) -> float:                  # line 695
def _assess_sulfation_confidence(self, days_since_deep, ir_trend_rate) -> str:  # line 705

<!-- External functions called -->
from src.battery_math.sulfation import compute_sulfation_score
from src.battery_math.cycle_roi import compute_cycle_roi
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Split _score_and_persist_sulfation into three methods and add unit tests</name>
  <files>src/discharge_handler.py, tests/test_discharge_handler.py</files>
  <read_first>
    - src/discharge_handler.py (lines 230-347 — the monolith + _grant_blackout_credit)
    - tests/test_discharge_handler.py (existing tests to avoid conflicts)
    - src/battery_math/sulfation.py (compute_sulfation_score signature)
    - src/battery_math/cycle_roi.py (compute_cycle_roi signature)
  </read_first>
  <behavior>
    - Test: _compute_sulfation_metrics returns dict with all 15 required keys when compute_sulfation_score and compute_cycle_roi succeed
    - Test: _compute_sulfation_metrics returns dict with sulfation_state=None and roi=None when compute_sulfation_score raises ValueError
    - Test: _compute_sulfation_metrics sets self.last_sulfation_score, self.last_cycle_roi, etc. from computed values
    - Test: _persist_sulfation_and_discharge calls battery_model.append_sulfation_history with correct dict shape
    - Test: _persist_sulfation_and_discharge calls battery_model.append_discharge_event with correct dict shape
    - Test: _persist_sulfation_and_discharge calls _grant_blackout_credit with discharge_trigger and dod from data dict
    - Test: _log_discharge_complete calls logger.info with event_type='discharge_complete' and all expected extra keys
    - Test: _log_discharge_complete handles None sulfation_score and None roi gracefully (logs None values)
  </behavior>
  <action>
Split `_score_and_persist_sulfation` (lines 230-325) into three private methods. Keep the same signature on the caller (`update_battery_health` already calls it).

**Method 1: `_compute_sulfation_metrics`**
Signature: `_compute_sulfation_metrics(self, soh_new: float, soh_delta: float, discharge_buffer: DischargeBuffer, discharge_trigger: str, capacity_ah_ref: Optional[float] = None) -> dict`

Contains:
- Lines 244-249: compute `now_iso`, `days_since_deep`, `ir_trend_rate`, `depth_of_discharge`, `cycle_budget`
- Lines 250-269: try/except calling `compute_sulfation_score()` and `compute_cycle_roi()`
- Lines 271-278: set `self.last_*` state fields
- Lines 280-287: pre-compute rounded values
- Return a dict with keys: `now_iso`, `sulfation_state`, `roi`, `sulfation_score_r`, `days_since_deep_r`, `ir_trend_r`, `recovery_delta_r`, `discharge_duration`, `dod_r`, `roi_r`, `soh_new`, `soh_delta`, `discharge_trigger`, `capacity_ah_ref`, `confidence_level`

**Method 2: `_persist_sulfation_and_discharge`**
Signature: `_persist_sulfation_and_discharge(self, data: dict) -> None`

Contains:
- Lines 289-299: `self.battery_model.append_sulfation_history({...})` using data dict values
- Lines 301-308: `self.battery_model.append_discharge_event({...})` using data dict values
- Line 325: `self._grant_blackout_credit(data['discharge_trigger'], data['dod_r'])` — note: use the unrounded `depth_of_discharge` for the 0.90 threshold comparison. Store raw `depth_of_discharge` in data dict too.

**Method 3: `_log_discharge_complete`**
Signature: `_log_discharge_complete(self, data: dict) -> None`

Contains:
- Lines 310-323: `logger.info('Discharge complete', extra={...})` using data dict values

**Update `_score_and_persist_sulfation`** to become the orchestrator:
```python
def _score_and_persist_sulfation(self, soh_new, soh_delta, discharge_buffer, discharge_trigger, capacity_ah_ref=None):
    data = self._compute_sulfation_metrics(soh_new, soh_delta, discharge_buffer, discharge_trigger, capacity_ah_ref)
    self._persist_sulfation_and_discharge(data)
    self._log_discharge_complete(data)
```

The existing `_score_and_persist_sulfation` signature is unchanged — callers are unaffected.

**Tests in `tests/test_discharge_handler.py`:**
Add a new test class `TestSulfationMethodSplit` with:
- Factory helper that creates a DischargeHandler with mocked BatteryModel and minimal config (similar to existing `TestDischargeClassification` setup)
- Mock `compute_sulfation_score` and `compute_cycle_roi` using `patch` to control return values
- Test `_compute_sulfation_metrics` returns dict with all 15 keys
- Test `_compute_sulfation_metrics` on ValueError returns dict with `sulfation_state=None`, `roi=None`
- Test `_persist_sulfation_and_discharge` calls `append_sulfation_history` and `append_discharge_event` once each
- Test `_persist_sulfation_and_discharge` calls `_grant_blackout_credit` with correct args
- Test `_log_discharge_complete` emits logger.info with `event_type='discharge_complete'`
- Test `_log_discharge_complete` handles None sulfation values

Important: `depth_of_discharge` (unrounded) must be in the data dict alongside `dod_r` (rounded) because `_grant_blackout_credit` uses the unrounded value for its `>= 0.90` threshold check. Add key `depth_of_discharge` to the data dict.
  </action>
  <verify>
    <automated>python3 -m pytest tests/test_discharge_handler.py -x -v 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "_compute_sulfation_metrics\|_persist_sulfation_and_discharge\|_log_discharge_complete" src/discharge_handler.py` returns at least 6 (3 defs + 3 calls in orchestrator)
    - `grep "def _compute_sulfation_metrics" src/discharge_handler.py` finds the method
    - `grep "def _persist_sulfation_and_discharge" src/discharge_handler.py` finds the method
    - `grep "def _log_discharge_complete" src/discharge_handler.py` finds the method
    - `grep "data = self._compute_sulfation_metrics" src/discharge_handler.py` confirms orchestrator pattern
    - `grep "self._persist_sulfation_and_discharge(data)" src/discharge_handler.py` confirms orchestrator call
    - `grep "self._log_discharge_complete(data)" src/discharge_handler.py` confirms orchestrator call
    - `grep -c "test_compute_sulfation_metrics\|test_persist_sulfation\|test_log_discharge_complete" tests/test_discharge_handler.py` returns at least 5 (test methods)
    - `python3 -m pytest tests/test_discharge_handler.py -x` exits 0
    - `python3 -m pytest tests/ -x --tb=short -q` exits 0 (full regression)
  </acceptance_criteria>
  <done>_score_and_persist_sulfation is decomposed into compute/persist/log methods, each independently tested. Full test suite passes.</done>
</task>

</tasks>

<verification>
- `python3 -m pytest tests/test_discharge_handler.py -x -v` — all new split-method tests pass
- `python3 -m pytest tests/ -x --tb=short -q` — full suite regression (528+ tests pass)
- `grep -c "def _score_and_persist_sulfation\|def _compute_sulfation_metrics\|def _persist_sulfation_and_discharge\|def _log_discharge_complete" src/discharge_handler.py` — returns 4 (all four methods exist)
</verification>

<success_criteria>
- _score_and_persist_sulfation is a 3-line orchestrator calling compute, persist, log
- Each of the three new methods is independently unit tested
- No behavior change: existing tests that call _score_and_persist_sulfation or update_battery_health still pass
- 528+ tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/21-extract-dischargecollector/21-01-SUMMARY.md`
</output>
