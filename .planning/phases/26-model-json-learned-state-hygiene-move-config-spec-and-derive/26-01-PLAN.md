---
phase: 26-model-json-learned-state-hygiene-move-config-spec-and-derive
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/battery_math/constants.py
  - src/model.py
  - src/monitor.py
  - scripts/battery-health.py
  - tests/test_model.py
autonomous: true
requirements: [HYG-01, HYG-02]
must_haves:
  truths:
    - "src/battery_math/constants.py defines NOMINAL_VOLTAGE = 12.0"
    - "ModelState TypedDict no longer declares full_capacity_ah_ref; KNOWN_STATE_KEYS (derived from ModelState) no longer contains it"
    - "BatteryModel.__init__ accepts capacity_ah (default RATED_CAPACITY_AH) stored as self.capacity_ah; get_capacity_ah() returns self.capacity_ah and never reads self.state"
    - "monitor.py constructs BatteryModel with capacity_ah=config.capacity_ah and no longer assigns self.battery_model.state['full_capacity_ah_ref']"
    - "get_nominal_voltage() returns NOMINAL_VOLTAGE and get_nominal_power_watts() returns NOMINAL_POWER_WATTS; the persisted physics dict written by _sync_physics_to_state contains only peukert_exponent, ir_compensation, rls_state"
    - "_default_vrla_lut seeds neither full_capacity_ah_ref nor physics.nominal_voltage / physics.nominal_power_watts; a freshly generated state loads clean through _reject_unknown_state_keys"
    - "all existing callers of get_capacity_ah/get_nominal_voltage/get_nominal_power_watts still receive correct values; full pytest green; ruff + vulture clean"
  artifacts:
    - path: src/battery_math/constants.py
      provides: "NOMINAL_VOLTAGE constant (single source of truth for 12.0 nominal battery voltage)"
    - path: src/model.py
      provides: "BatteryModel with config-injected capacity_ah and constants-sourced nominal voltage/power; physics blob split so only learned keys persist"
    - path: src/monitor.py
      provides: "battery model construction injecting config.capacity_ah without state round-trip"
  key_links:
    - from: src/monitor.py
      to: src/model.py
      via: "BatteryModel(model_path, capacity_ah=config.capacity_ah)"
      pattern: "BatteryModel\\("
    - from: src/model.py
      to: src/battery_math/constants.py
      via: "get_nominal_voltage/get_nominal_power_watts return constants"
      pattern: "NOMINAL_VOLTAGE|NOMINAL_POWER_WATTS"
---

<objective>
Move category ① config/spec OUT of persisted model.json: stop persisting full_capacity_ah_ref (source
it from config at runtime via a BatteryModel.capacity_ah attribute) and stop persisting
physics.nominal_voltage / physics.nominal_power_watts (source them from constants.py). Split the mixed
physics blob so the learned keys (peukert_exponent, ir_compensation, rls_state) keep persisting
(HYG-01, HYG-02).

Purpose: these three values are never learned — full_capacity_ah_ref is already overwritten from
config.capacity_ah on every start (monitor.py:129), and nominal voltage/power are fixed hardware spec.
Persisting them makes model.json carry non-learned data and falsely implies the on-disk copy is
authoritative. After this plan, model.json holds no config/spec; the getters return the same values as
before, sourced live.

Output: a NOMINAL_VOLTAGE constant, a capacity_ah-injected BatteryModel, a slimmed physics persistence
path, and updated tests — all callers unaffected, all computed values identical.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/26-model-json-learned-state-hygiene-move-config-spec-and-derive/26-CONTEXT.md
@.planning/phases/26-model-json-learned-state-hygiene-move-config-spec-and-derive/26-RESEARCH.md
@src/model.py
@src/monitor.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add NOMINAL_VOLTAGE and source nominal voltage/power from constants (HYG-02)</name>
  <files>src/battery_math/constants.py, src/model.py</files>
  <read_first>
    - src/battery_math/constants.py (RATED_CAPACITY_AH, NOMINAL_POWER_WATTS — add NOMINAL_VOLTAGE here)
    - src/model.py (import line 14; PhysicsParams 105-118 nominal_voltage/nominal_power_watts fields; _sync_physics_from_state 289-317 reads 310-311; _sync_physics_to_state 319-332 writes 323-324; get_nominal_voltage 500-501; get_nominal_power_watts 503-504; _default_vrla_lut physics seed lines 460-461)
    - .planning/phases/26-model-json-learned-state-hygiene-move-config-spec-and-derive/26-RESEARCH.md (section 2)
  </read_first>
  <action>
    In src/battery_math/constants.py add NOMINAL_VOLTAGE = 12.0 immediately after
    NOMINAL_POWER_WATTS = 425.0, with a one-line comment: VRLA 12V nominal battery voltage, single
    source of truth replacing the scattered magic 12.0.
    In src/model.py import NOMINAL_VOLTAGE alongside the existing constants import (line 14).
    Change get_nominal_voltage() to return NOMINAL_VOLTAGE and get_nominal_power_watts() to return
    NOMINAL_POWER_WATTS (direct constant reads; no longer read self.physics fields).
    In _sync_physics_from_state remove the nominal_voltage= and nominal_power_watts= args to the
    PhysicsParams(...) construction (lines 310-311).
    In _sync_physics_to_state remove the "nominal_voltage" and "nominal_power_watts" entries (lines
    323-324) so the persisted physics dict is exactly {peukert_exponent, ir_compensation, rls_state}.
    In PhysicsParams (105-118) remove the nominal_voltage and nominal_power_watts fields. If any
    remaining reference breaks, fix the reference (do not re-add the field).
    In _default_vrla_lut remove the "nominal_voltage": 12.0 and "nominal_power_watts": NOMINAL_POWER_WATTS
    entries from the seeded physics dict (lines 460-461).
    Do NOT touch peukert_exponent, ir_compensation, or rls_state — they stay persisted.
  </action>
  <verify>
    <automated>set -o pipefail; uv run pytest tests/test_sag_tracker.py tests/test_runtime_calculator.py -q</automated>
  </verify>
  <done>NOMINAL_VOLTAGE exists in constants; getters return constant values; persisted physics dict contains only the three learned keys; default seed omits the two spec keys; sag/runtime math tests green.</done>
</task>

<task type="auto">
  <name>Task 2: Inject capacity_ah from config; drop full_capacity_ah_ref from schema and state (HYG-01)</name>
  <files>src/model.py, src/monitor.py</files>
  <read_first>
    - src/model.py (ModelState full_capacity_ah_ref decl line 34; KNOWN_STATE_KEYS 64; __init__ 195-211; get_capacity_ah 653-656; _validate_and_clamp_fields full_capacity_ah_ref block 383-393; _default_vrla_lut full_capacity_ah_ref seed line 456)
    - src/monitor.py (_init_battery_model_and_estimators ~124-145: BatteryModel(model_path) line 128, state["full_capacity_ah_ref"] = config.capacity_ah line 129)
    - .planning/phases/26-model-json-learned-state-hygiene-move-config-spec-and-derive/26-RESEARCH.md (section 1)
  </read_first>
  <action>
    In src/model.py: add a capacity_ah parameter to BatteryModel.__init__ with default RATED_CAPACITY_AH
    (signature: def __init__(self, model_path=None, capacity_ah: float = RATED_CAPACITY_AH)); set
    self.capacity_ah = capacity_ah before self.load(). Change get_capacity_ah() to return
    self.capacity_ah (remove the self.state.get("full_capacity_ah_ref", RATED_CAPACITY_AH) read and its
    docstring reference to the persisted key). Remove the full_capacity_ah_ref entry from the ModelState
    TypedDict (line 34) — KNOWN_STATE_KEYS is derived from ModelState so it updates automatically.
    Remove the full_capacity_ah_ref validation/repair block in _validate_and_clamp_fields (383-393).
    Remove the "full_capacity_ah_ref": RATED_CAPACITY_AH seed from _default_vrla_lut (line 456).
    In src/monitor.py _init_battery_model_and_estimators: construct
    self.battery_model = BatteryModel(model_path, capacity_ah=config.capacity_ah) and DELETE the
    self.battery_model.state["full_capacity_ah_ref"] = config.capacity_ah line (129). Leave the rest of
    the init (install-date, save-if-missing, estimator wiring via get_capacity_ah()) unchanged.
  </action>
  <verify>
    <automated>set -o pipefail; ! rg -n "full_capacity_ah_ref" src/ && uv run pytest tests/test_monitor.py -q -k "capacity or model or init"</automated>
  </verify>
  <done>BatteryModel takes capacity_ah, get_capacity_ah() returns it without touching state; full_capacity_ah_ref is gone from ModelState, validation, default seed, and monitor.py; rg finds no full_capacity_ah_ref under src/; monitor capacity/init tests green.</done>
</task>

<task type="auto">
  <name>Task 3: Update battery-health.py capacity source and fix/remove broken model tests (HYG-01, HYG-02)</name>
  <files>scripts/battery-health.py, tests/test_model.py</files>
  <read_first>
    - scripts/battery-health.py (lines 30 docstring; 92 capacity_ah = model_data.get("capacity_ah") or model_data.get("full_capacity_ah_ref"); 157 same pattern)
    - tests/test_model.py (get_capacity_ah==7.2 asserts at 109,120,136,258; any full_capacity_ah_ref validation tests; physics nominal_voltage/nominal_power_watts persistence asserts if present)
    - config.toml (capacity_ah key — commented default 7.2; the script must source rated capacity from config or RATED_CAPACITY_AH, not from model.json which no longer carries it)
  </read_first>
  <action>
    In scripts/battery-health.py: model.json no longer carries full_capacity_ah_ref (nor capacity_ah).
    Replace both `model_data.get("capacity_ah") or model_data.get("full_capacity_ah_ref")` expressions
    (lines 92 and 157) with a read of the rated capacity from config.toml's capacity_ah, falling back to
    the RATED_CAPACITY_AH default (7.2) when unset. Add a tiny helper that loads config.toml (reuse the
    same search order as monitor_config.load_config: CONFIG_DIR/config.toml then repo config.toml) and
    returns capacity_ah, or inline a minimal tomllib read with a 7.2 default. Update the docstring line
    30 that says "capacity/SoH: model.json soh, capacity_ah / full_capacity_ah_ref" to "capacity/SoH:
    model.json soh; rated capacity from config.toml capacity_ah (default 7.2)".
    In tests/test_model.py: the get_capacity_ah()==7.2 assertions (109,120,136,258) still hold because
    the default capacity_ah is RATED_CAPACITY_AH (7.2) — verify they construct BatteryModel without an
    overriding capacity_ah; if any test previously seeded full_capacity_ah_ref in the on-disk fixture to
    assert get_capacity_ah reflects it, rewrite it to pass capacity_ah=<value> to the constructor and
    assert get_capacity_ah()==<value>. Delete any test that specifically asserts full_capacity_ah_ref is
    persisted/validated/clamped in model.json (dead behavior). Delete or rewrite any test asserting
    physics.nominal_voltage / physics.nominal_power_watts round-trips through model.json (they no longer
    persist); keep tests asserting get_nominal_voltage()/get_nominal_power_watts() return 12.0/425.0.
    Add one positive test: BatteryModel(path, capacity_ah=9.0).get_capacity_ah() == 9.0 and saving then
    reloading does NOT reintroduce a full_capacity_ah_ref key in the file.
  </action>
  <verify>
    <automated>set -o pipefail; uv run pytest tests/test_model.py -q && python3 scripts/battery-health.py >/dev/null 2>&1; echo "battery-health exit=$?"</automated>
  </verify>
  <done>battery-health.py sources rated capacity from config.toml (no model.json full_capacity_ah_ref/capacity_ah read); test_model.py has no asserts on persisted full_capacity_ah_ref or persisted nominal voltage/power, retains getter-value coverage, and gains a capacity_ah injection test; full test_model.py green; battery-health.py runs without error.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| config.toml → daemon | capacity_ah now reaches the model only via constructor injection, not via persisted state |
| daemon → model.json | persisted physics dict and top-level keys shrink (schema consumed by tools/tests) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-01 | Tampering | a stale full_capacity_ah_ref / physics spec key resident in a deployed model.json | accept | Single-host, no backward-compat (REQUIREMENTS Out of Scope: state regenerates). The strict loader (Phase 25) will REJECT the old file on next start — handled by the Plan 02 deploy strip note. No migration code here. |
| T-26-02 | Denial of Service | get_capacity_ah / get_nominal_* still referenced by an unseen caller after the source change | mitigate | rg across src + full pytest + vulture gate before commit; getters keep identical signatures and return values |
| T-26-SC | Tampering | npm/pip/cargo installs | accept | No package installs — in-place refactor of existing code only |
</threat_model>

<verification>
- `rg -n "full_capacity_ah_ref" src/` returns nothing; `rg -n "full_capacity_ah_ref" scripts/` returns nothing.
- A saved-then-reloaded fresh model.json has physics keys exactly {peukert_exponent, ir_compensation, rls_state} and no top-level full_capacity_ah_ref.
- `get_capacity_ah()` returns the injected capacity_ah (7.2 default; 9.0 when injected); `get_nominal_voltage()`==12.0; `get_nominal_power_watts()`==425.0.
- `_reject_unknown_state_keys` passes on a freshly generated default state (no removed key leaks back via the seed).
- `set -o pipefail; uv run pytest` green; `uvx vulture src` clean; `uvx ruff check src tests scripts` clean.
- battery-health.py runs and prints rated capacity sourced from config.toml.
</verification>

<success_criteria>
- full_capacity_ah_ref, physics.nominal_voltage, physics.nominal_power_watts no longer persisted.
- capacity_ah sourced from config via constructor; nominal voltage/power sourced from constants.
- All getter callers unaffected; computed values identical for fixed inputs.
- Tests updated/removed for the dropped persistence; suite green; vulture/ruff clean.
</success_criteria>

<output>
Create `.planning/phases/26-model-json-learned-state-hygiene-move-config-spec-and-derive/26-01-SUMMARY.md` when done.
</output>
