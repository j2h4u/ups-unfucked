---
phase: 08-architecture-foundation
plan: 00
type: execute
wave: 0
depends_on: []
files_modified: [tests/test_monitor.py, tests/conftest.py]
autonomous: true
requirements: []
must_haves:
  truths:
    - "CurrentMetrics dataclass fixture created and reusable across test suite"
    - "Config dataclass fixture created with typical test values"
    - "Wave 0 test stubs exist for ARCH-01, ARCH-02 (ARCH-03 is automatic)"
  artifacts:
    - path: "tests/conftest.py"
      provides: "current_metrics_fixture() and config_fixture() for test reuse"
      exports: ["current_metrics_fixture", "config_fixture"]
    - path: "tests/test_monitor.py"
      provides: "test stubs for dataclass validation (Wave 1 fills in assertions)"
      contains: ["test_current_metrics_dataclass", "test_config_dataclass", "test_config_immutability"]
  key_links:
    - from: "tests/conftest.py"
      to: "tests/test_monitor.py"
      via: "pytest fixtures"
      pattern: "import.*conftest|@pytest.fixture"
---

<objective>
Prepare Wave 0 test infrastructure for Phase 8 dataclass refactoring.

Purpose: Establish fixture contracts and test stubs so Wave 1 tasks (ARCH-01, ARCH-02, ARCH-03) can focus on implementation without building test infrastructure.

Output: Reusable fixtures + test stubs in place
</objective>

<execution_context>
@/home/j2h4u/.claude/get-shit-done/workflows/execute-plan.md
@/home/j2h4u/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-architecture-foundation/08-RESEARCH.md
@.planning/phases/08-architecture-foundation/08-VALIDATION.md

# Test fixtures and existing test structure
@tests/conftest.py
@tests/test_monitor.py
</context>

<tasks>

<task type="auto">
  <name>Task 0.1: Create CurrentMetrics and Config fixtures in conftest.py</name>
  <files>tests/conftest.py</files>
  <action>
Add two pytest fixtures to tests/conftest.py (at end of file, after existing fixtures):

1. `current_metrics_fixture()` — returns a CurrentMetrics dataclass instance with default test values:
   - soc: 0.75
   - battery_charge: 75.0
   - time_rem_minutes: 30.0
   - event_type: EventType.ONLINE
   - transition_occurred: False
   - shutdown_imminent: False
   - ups_status_override: None
   - previous_event_type: EventType.ONLINE
   - timestamp: datetime.now()

   Import EventType from src.event_classifier in conftest.py if not already present. Once ARCH-01 is done (Wave 1), tests will import CurrentMetrics from src.monitor.

2. `config_fixture()` — returns a Config dataclass instance with typical test values:
   - ups_name: 'test-cyberpower'
   - polling_interval: 10
   - reporting_interval: 60
   - nut_host: 'localhost'
   - nut_port: 3493
   - nut_timeout: 2.0
   - shutdown_minutes: 5
   - soh_alert_threshold: 0.80
   - model_dir: tmp_path / 'test_model'
   - config_dir: tmp_path / 'test_config'
   - runtime_threshold_minutes: 20
   - reference_load_percent: 20.0
   - ema_window_sec: 120

   Use pytest's tmp_path fixture as parameter to config_fixture for temporary directories. Once ARCH-02 is done (Wave 1), tests will import Config from src.monitor.

Both fixtures should be marked with @pytest.fixture. Document with docstrings explaining their purpose for test reuse.
  </action>
  <verify>
    <automated>pytest tests/conftest.py -v 2>&1 | grep -E "(PASSED|FAILED|ERROR)" || echo "Conftest loads successfully"</automated>
  </verify>
  <done>
Both fixtures exist in tests/conftest.py, properly marked with @pytest.fixture, docstrings complete. Running conftest.py shows no syntax errors.
  </done>
</task>

<task type="auto">
  <name>Task 0.2: Create test stubs for dataclass validation (ARCH-01, ARCH-02, ARCH-03)</name>
  <files>tests/test_monitor.py</files>
  <action>
Add three test function stubs to tests/test_monitor.py (at end of existing test functions):

1. `test_current_metrics_dataclass()` — will validate CurrentMetrics instantiation
   - Stub: `pytest.skip("Wave 1: ARCH-01 — CurrentMetrics dataclass not yet created")`
   - Comment: Once Wave 1 creates CurrentMetrics dataclass, replace skip with assertions

2. `test_config_dataclass()` — will validate Config instantiation
   - Stub: `pytest.skip("Wave 1: ARCH-02 — Config dataclass not yet created")`
   - Comment: Once Wave 1 creates Config dataclass, replace skip with assertions

3. `test_config_immutability()` — will validate frozen=True semantics
   - Stub: `pytest.skip("Wave 1: ARCH-02 — Config dataclass not yet created")`
   - Comment: Once Wave 1 creates Config dataclass, test will attempt field mutation and expect FrozenInstanceError

Include docstrings with the test purpose (e.g., "Verify CurrentMetrics has all required fields with correct types").
  </action>
  <verify>
    <automated>pytest tests/test_monitor.py::test_current_metrics_dataclass -v 2>&1 | grep -E "(SKIPPED|PASSED)"</automated>
  </verify>
  <done>
Three test stubs exist in tests/test_monitor.py with pytest.skip() markers. Running pytest shows SKIPPED status for all three (expected for Wave 0).
  </done>
</task>

<task type="auto">
  <name>Task 0.3: Commit Wave 0 test infrastructure</name>
  <files>tests/conftest.py, tests/test_monitor.py</files>
  <action>
Run git add and commit the fixtures and test stubs:

```bash
cd /home/j2h4u/repos/j2h4u/ups-battery-monitor
git add tests/conftest.py tests/test_monitor.py
git commit -m "test(08-00): add test fixtures and stubs for Phase 8 dataclass refactoring

- Add current_metrics_fixture() to conftest.py for reusable test instances
- Add config_fixture() to conftest.py for Config dataclass testing
- Add test_current_metrics_dataclass(), test_config_dataclass(), test_config_immutability() stubs
- All stubs marked with pytest.skip('Wave 1: ...') pending dataclass implementation

Nyquist compliant: ✅ All stubs have skip() markers; Phase gate not blocked.
"
```

Verify commit succeeded with `git log --oneline -1`.
  </action>
  <verify>
    <automated>git -C /home/j2h4u/repos/j2h4u/ups-battery-monitor log --oneline -1 | grep -q "08-00" && echo "Commit succeeded"</automated>
  </verify>
  <done>
Wave 0 commit created; conftest.py and test_monitor.py updated in git history.
  </done>
</task>

</tasks>

<verification>
Wave 0 complete when:
- [ ] `tests/conftest.py` has `current_metrics_fixture()` and `config_fixture()`
- [ ] `tests/test_monitor.py` has three test stubs (all SKIPPED)
- [ ] All files committed to git
- [ ] `pytest tests/test_monitor.py -v` shows 3 SKIPPED tests
</verification>

<success_criteria>
- Test fixtures created and importable from conftest.py
- Test stubs created with pytest.skip() markers
- No existing tests broken
- All Wave 0 changes committed
</success_criteria>

<output>
After completion, create `.planning/phases/08-architecture-foundation/08-00-SUMMARY.md` with fixture definitions and test stub list.
</output>
