#!/usr/bin/env python3
"""Record or verify the read-only Slice-0 physical NUT capability baseline."""

import sys
from pathlib import Path

sys.path.insert(0, str(Path(__file__).resolve().parent.parent))

from src.telemetry_capability_cli import main  # noqa: E402, I001


if __name__ == "__main__":
    raise SystemExit(main())
